<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- A stored field that depends on today cannot notice that today changed.
             `job_at_risk` is True partly because an MO deadline has PASSED — a comparison
             against today, and today is not a field, so it cannot appear in @api.depends.
             Odoo only recomputes a stored field when a declared dependency is written, so
             a job that goes late overnight keeps yesterday's answer and silently drops
             off the Install Risk board. That is the same false all-clear this release
             exists to remove, arriving through the back door.

             Hourly, because "is it late" is read throughout the working day. Staggered off
             the half-hour readiness cron so the two do not co-fire on a 2-core host. -->
        <record id="cron_retick_time_sensitive_readiness" model="ir.cron">
            <field name="name">Southbrook: re-tick date-driven job readiness</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="state">code</field>
            <field name="code">model._cron_retick_time_sensitive_readiness()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(minutes=7))"/>
        </record>

    </data>
</odoo>
