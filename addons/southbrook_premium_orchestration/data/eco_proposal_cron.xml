<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="cron_propose_eco_for_high_freq_overrides" model="ir.cron">
            <field name="name">Southbrook: Propose ECOs for High-Frequency Cut-Spec Overrides</field>
            <field name="model_id" ref="model_southbrook_cut_spec_override"/>
            <field name="state">code</field>
            <field name="code">model._cron_propose_eco_for_high_frequency_overrides()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now().replace(hour=4, minute=0, second=0, microsecond=0) + relativedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>
