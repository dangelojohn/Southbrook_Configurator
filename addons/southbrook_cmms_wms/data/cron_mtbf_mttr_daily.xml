<?xml version="1.0" encoding="utf-8"?>
<!--
  Daily 23:00 cron — generates one MTBF/MTTR report per active piece of
  maintenance.equipment. Reports populate stored compute fields on
  create, so list / graph views stay current without manual recompute.

  No `numbercall` / `doall` fields — they were removed from ir.cron in v19.
-->
<odoo>
    <data noupdate="1">

        <record id="cron_southbrook_cmms_mtbf_mttr_daily"
                model="ir.cron">
            <field name="name">Southbrook CMMS: Daily MTBF/MTTR sweep</field>
            <field name="model_id" ref="model_southbrook_cmms_mtbf_mttr_report"/>
            <field name="state">code</field>
            <field name="code">model._cron_generate_daily_reports()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>
