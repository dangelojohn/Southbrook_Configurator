<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">

    <!-- v19: res.groups no longer carries the legacy module-category -->
    <!-- link; the module surface picks the category via the manifest -->
    <!-- "category" key. -->

    <record id="group_mes_user" model="res.groups">
      <field name="name">MES / User</field>
      <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <record id="group_mes_manager" model="res.groups">
      <field name="name">MES / Manager</field>
      <field name="implied_ids" eval="[(4, ref('group_mes_user'))]"/>
    </record>

  </data>
</odoo>
