<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="slide_04_ecos" model="slide.slide">
            <field name="name">Engineering Change Orders — When to Raise One, What Each State Means</field>
            <field name="channel_id" ref="course_plm_design"/>
            <field name="slide_category">article</field>
            <field name="sequence">10</field>
            <field name="description">ECO Engineer / Designer responsible for revising cabinet BoMs, cut specs, or construction rules.</field>
            <field name="html_content"><![CDATA[<h1 id="engineering-change-orders-when-to-raise-one-what-each-state-means">Engineering Change Orders — When to Raise One, What Each State Means</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the designer or shop lead who notices that something on a cabinet
needs to change — a drawing error, a vendor substitution, a customer
revision, a tolerance the floor can't actually hit — and you have to
move that change through engineering review without breaking the
manufacturing orders already in flight. Every change you make through
PLM rides a paper trail; this lesson is the practical shape of that
trail and what each click commits to the database.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>Sign in at <strong>southbrookcabinetry.space/odoo</strong> with your PLM user
account, then:</p>
<blockquote>
<p><strong>Southbrook PLM → Change Orders</strong></p>
</blockquote>
<p>Approvers (the people who can push an ECO past the Under Review gate
or hit Apply) also see:</p>
<blockquote>
<p><strong>Southbrook PLM → Configuration → ECO Types</strong>
<strong>Southbrook PLM → Configuration → ECO Stages</strong></p>
</blockquote>
<p>These two configuration screens are where the seeded stage pipeline
and the four ECO types live. Approvers rarely touch them — they're
admin-shaped — but they're worth knowing about because if a colleague
adds a new stage and forgets to set <code>approval_required</code>, the gate
silently disappears.</p>
<p>There's also a one-click bridge from the rest of the platform: a
small flag-icon button on every line of a sale order's Order Builder
grid (Sales → Quotations → form view → order lines list) calls
<code>sale.order.line.action_raise_eco()</code> and opens a draft ECO pre-filled
with the cabinet's BoM, the order/line/product cited in the title,
and <code>target_kind=bom</code> already selected. Use it.</p>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>When you open <strong>Change Orders</strong>, you land on a kanban grouped by
<strong>Stage</strong> (<code>stage_id</code> on <code>southbrook.eco</code>). Each card shows:</p>
<ul>
<li><strong>Reference</strong> (<code>name</code> on <code>southbrook.eco</code>) — auto-generated by an
  <code>ir.sequence</code> (e.g. <code>ECO/2026/0017</code>). Read-only; assigned on create.</li>
<li><strong>Title</strong> (<code>title</code>) — your one-line summary. This is the field that
  shows up in chatter notifications and on the MO's "Governing ECO"
  link, so write it like the headline of a Jira ticket.</li>
<li><strong>Type</strong> (<code>eco_type_id</code> → <code>southbrook.eco.type</code>) — one of the four
  seeded categories: <strong>Template BoM Revision</strong>, <strong>Cut-Geometry
  Revision</strong>, <strong>Construction-Rule Change</strong>, <strong>Engineering Document
  Update</strong>. The type drives a hidden <code>target_kind</code> selection
  (<code>bom</code> / <code>cut_spec</code> / <code>rule</code> / <code>document</code>) which in turn drives
  which target field the form shows.</li>
<li><strong>Target Kind badge</strong> — derived (<code>target_kind</code> is a related/stored
  field). Tells you at a glance what apply will actually do.</li>
<li><strong>Priority</strong> (<code>priority</code>) — Normal / High / Urgent.</li>
<li><strong>Responsible</strong> (<code>user_id</code>) — defaults to you on create.</li>
</ul>
<p>Open a card and you land on the form. The form's notebook has two
pages:</p>
<ul>
<li><strong>Target tab</strong> — one of four fields is visible depending on the type:</li>
<li><code>bom_id</code> (m2o to <code>mrp.bom</code>) when type is BoM Revision. Required.</li>
<li><code>cut_spec_id</code> (m2o to <code>southbrook.cut.spec</code>) when type is
    Cut-Geometry Revision. Required.</li>
<li><code>git_ref</code> (Char) when type is Construction-Rule Change. Required —
    the rule lives in <code>config_rules.xml</code> and git is the system of
    record; the ECO carries the commit SHA or PR URL so the audit
    trail still captures it.</li>
<li>
<p><code>document_ids</code> (m2m to <code>ir.attachment</code>) when type is Engineering
    Document Update. At least one attachment required.</p>
</li>
<li>
<p><strong>Description tab</strong> (<code>description</code>, HTML) — what's changing, why, and
  the engineering rationale. This is what an auditor or a future
  designer reads when they ask "why did we do this in 2026?"</p>
</li>
</ul>
<p>Header buttons (visible only while <code>state == open</code>):</p>
<ul>
<li><strong>Approve</strong> — stamps <code>approver_id</code> and <code>approval_date</code>, advances
  one non-terminal stage. PLM Approver group only.</li>
<li><strong>Apply Change</strong> — runs the per-kind handler, flips <code>state</code> to
  <code>applied</code>, lands the ECO in the Applied stage. PLM Approver only.</li>
<li><strong>Next Stage</strong> — moves to the next non-terminal stage in sequence.</li>
<li><strong>Reject</strong> — flips to <code>rejected</code>, lands in the Rejected stage.</li>
<li><strong>Reset to Draft</strong> (visible when state isn't open) — clears
  approval bookkeeping and sends the ECO back to the first
  non-terminal stage. Used after a reject when you want another try.</li>
</ul>
<p>A <code>web_ribbon</code> widget marks the form <strong>Applied</strong> (green) or
<strong>Rejected</strong> (red) once it lands in a terminal state.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p><strong>1. When to raise an ECO at all.</strong></p>
<p>Raise one whenever you change something that's already governed by
PLM. The four shapes are:</p>
<ul>
<li><strong>Drawing error / customer revision</strong> that changes a template BoM
  → ECO type <strong>Template BoM Revision</strong>. Pick the target <code>mrp.bom</code>,
  describe the change, attach the marked-up drawing (PDF or DWG)
  in the Documents tab.</li>
<li><strong>Material substitution</strong> that changes the geometric inputs
  every cabinet reads (a panel thickness, a reveal, the rabbet
  depth, the toe-kick height) → ECO type <strong>Cut-Geometry Revision</strong>.
  First create a draft <code>southbrook.cut.spec</code> with the new values
  (see lesson 4.2), then point the ECO at it via <code>cut_spec_id</code>.</li>
<li><strong>Manufacturability fix</strong> that lives in code (a <code>config_rules.xml</code>
  exclusion, a Python computed field, a series→door restriction)
  → ECO type <strong>Construction-Rule Change</strong>. The code change lands
  via a git PR; the ECO carries the PR URL or commit SHA in
  <code>git_ref</code> and exists <em>only</em> to make the audit trail complete.</li>
<li><strong>Document-only update</strong> (a refreshed vendor cut sheet, a new
  hardware spec PDF, an updated shop drawing) → ECO type
  <strong>Engineering Document Update</strong>. Attach the document(s) and apply.</li>
</ul>
<p>If the change doesn't touch a template BoM, the parametric cut spec,
a construction rule, or an engineering document — you don't need an
ECO. Updating attribute display order on a product template is a
plain <code>write</code>, not a PLM event.</p>
<p><strong>2. Drafting (the loop).</strong></p>
<ul>
<li>From <strong>Change Orders</strong>, click <strong>New</strong>. The kanban pre-creates
  the record in the <strong>Draft</strong> stage (<code>sequence=10</code> on
  <code>southbrook.eco.stage</code>).</li>
<li>Pick the <strong>Type</strong>. Watch the form react — the Target tab swaps
  to the right field.</li>
<li>Fill the target. For a BoM ECO, browse to the canonical template
  BoM (the cabinet's active version). For a cut-spec ECO, the
  draft cut spec you authored separately.</li>
<li>Write the <strong>Title</strong> like a commit message: imperative, terse,
  scoped. "Bump base-cabinet back panel from 6.35 mm to 9 mm" beats
  "back panel change."</li>
<li>Fill the <strong>Description</strong> with the why. Auditor-ready.</li>
<li>Save. The reference (<code>ECO/YYYY/NNNN</code>) gets assigned now.</li>
</ul>
<p><strong>3. Moving it through the pipeline.</strong></p>
<p>The seeded stages are:</p>
<table>
<thead>
<tr>
<th>Sequence</th>
<th>Name</th>
<th>Flags</th>
<th>What gates here</th>
</tr>
</thead>
<tbody>
<tr>
<td>10</td>
<td><strong>Draft</strong></td>
<td>—</td>
<td>Anyone can edit. No effect committed.</td>
</tr>
<tr>
<td>20</td>
<td><strong>Under Review</strong></td>
<td><code>approval_required=True</code></td>
<td>Leaving this stage requires PLM Approver rights.</td>
</tr>
<tr>
<td>30</td>
<td><strong>Approved</strong></td>
<td>—</td>
<td>The change has approval but the handler hasn't run yet.</td>
</tr>
<tr>
<td>40</td>
<td><strong>Applied</strong></td>
<td><code>is_applied_stage=True</code>, <code>is_final=True</code>, <code>fold=True</code></td>
<td>Terminal. The handler ran; effect committed.</td>
</tr>
<tr>
<td>50</td>
<td><strong>Rejected</strong></td>
<td><code>is_rejected_stage=True</code>, <code>is_final=True</code>, <code>fold=True</code></td>
<td>Terminal. No effect committed.</td>
</tr>
</tbody>
</table>
<p>The <code>state</code> field (<code>open</code> / <code>applied</code> / <code>rejected</code>) tracks the
lifecycle outcome separately from the configurable kanban stage. You
get both because shop leads sometimes want to add a custom
"Awaiting Vendor Quote" or "Customer Approved" stage between Under
Review and Approved without breaking the apply handlers — those
look at <code>state</code>, the kanban looks at <code>stage_id</code>.</p>
<ul>
<li><strong>Click Next Stage</strong> to move Draft → Under Review. No
  approver rights needed.</li>
<li><strong>Click Approve</strong> (Approver group only) at Under Review to advance
  past the gate. This stamps <code>approver_id</code> and <code>approval_date</code> and
  moves you to Approved.</li>
<li>
<p><strong>Click Apply Change</strong> (Approver group only) at Approved. This is
  the irreversible click. It runs the per-kind handler:</p>
</li>
<li>
<p><strong>BoM ECO</strong> (<code>_apply_bom</code>): <code>sudo()</code>-copies the target BoM to
    a new <code>southbrook_version</code>, archives the prior version
    (<code>active=False</code>), links the new one as <code>new_bom_id</code>, and posts a
    chatter line citing both versions.</p>
</li>
<li><strong>Cut-spec ECO</strong> (<code>_apply_cut_spec</code>): calls
    <code>southbrook.cut.spec.action_activate()</code> on the candidate, which
    flips it to <code>state=active</code> and supersedes the prior active
    spec. Exactly one cut spec can be active at any time — enforced
    by the <code>_check_single_active</code> constraint.</li>
<li><strong>Rule ECO</strong> (<code>_apply_rule</code>): record-only; chatter cites the
    <code>git_ref</code>. The rule itself lands when the PR merges.</li>
<li><strong>Document ECO</strong> (<code>_apply_document</code>): record-only; chatter
    counts the attached documents. Refuses to apply with zero
    attachments.</li>
</ul>
<p>After the handler, the ECO writes <code>state=applied</code>, stamps
  <code>applied_date</code> (and <code>approver_id</code>/<code>approval_date</code> if a prior
  Approve click hadn't already), and lands in the Applied stage.</p>
<p><strong>4. Rejecting or withdrawing.</strong></p>
<ul>
<li><strong>Reject</strong> is the right action on an ECO that shouldn't ship. It
  flips <code>state</code> to <code>rejected</code> and lands in the Rejected stage. It
  cannot be used on an already-applied ECO — the apply handler has
  already mutated the database, and Reject is not an undo. The
  system raises a UserError if you try.</li>
<li><strong>Withdrawal</strong> — there isn't a separate "Withdrawn" state. Use
  Reject with a chatter note explaining the withdrawal. If you
  later want to try the same change, click <strong>Reset to Draft</strong> to
  send it back to the first non-terminal stage; this clears
  <code>applied_date</code>, <code>approver_id</code>, and <code>approval_date</code> so the next
  pass through stamps cleanly.</li>
</ul>
<p><strong>5. End-of-shift housekeeping.</strong></p>
<ul>
<li>Filter the kanban with <strong>My ECOs</strong> (<code>user_id = uid</code>). Any cards
  stuck in Under Review for more than a day are blocking someone.</li>
<li>Cards in Approved without an Apply click are the danger zone —
  the change has been signed off but the BoM hasn't versioned yet,
  so MOs released after the approval are still pulling the old
  BoM. Apply or reset.</li>
</ul>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I clicked Apply on a BoM ECO and now an MO that's already in
flight is showing the old BoM version on its form."</strong></p>
<p>That's correct — and intentional. The Apply handler copies the BoM
to a new version and archives the old one, but MOs already confirmed
hold a pointer to the BoM that was active at the time of confirm.
The MOs in flight finish on the spec they were planned against; the
new version takes effect on the <em>next</em> MO created from this product
template. If the change is so urgent that in-flight MOs must follow
the new spec, you have two options: cancel and re-create the MOs
against the new BoM (planner's call), or raise a follow-up
work-order amendment manually on each affected MO. The system will
not silently re-base them.</p>
<p><strong>"I rejected an ECO that had actually been Applied yesterday — now
the audit trail says 'rejected' but the BoM is still on the new
version."</strong></p>
<p>You can't. The system blocks Reject on an applied ECO with
<code>UserError("ECO %s is already applied — its change is committed…")</code>.
The check exists because an early demo allowed this and produced a
record that lied about the outcome. If you actually need to undo
an applied change, raise a <em>new</em> BoM ECO whose target is the new
version and whose effect rolls the values back to the original.
That's the only honest way — and the audit trail captures both
events.</p>
<p><strong>"I tried to drag an ECO straight into the Applied column on the
kanban to skip the buttons."</strong></p>
<p>The <code>write</code> method blocks it. The terminal-stage guard checks that
any direct write of <code>stage_id</code> to a stage flagged <code>is_final</code> must
arrive in the same <code>write()</code> call with a matching <code>state</code> value
(<code>applied</code> or <code>rejected</code>). A kanban drag writes only <code>stage_id</code>, so
it raises a UserError that points you back at Apply or Reject. The
guard exists because skipping the buttons skips the per-kind
handlers — your BoM doesn't version, your cut spec doesn't activate
— and the ECO record ends up in a paradoxical
<code>stage=Applied, state=open</code> shape. Click the buttons.</p>
<p><strong>"I clicked Apply but the BoM ECO failed with 'AccessError: cannot
create mrp.bom'."</strong></p>
<p>This was a real install-time bug. The fix is to ensure your PLM
Approver group has been seeded correctly — the apply handler does
<code>old.sudo().copy(...)</code> because PLM Approver isn't necessarily a
Manufacturing Administrator, and the approval-gate authorisation
is what authorises the BoM mutation. If you still hit this on
your install, check that
<code>southbrook_plm.group_southbrook_plm_approver</code> is assigned to your
user and ask an admin to upgrade the module.</p>
<p><strong>"I rejected an ECO, then realised I shouldn't have. I tried to
Apply it but got an error."</strong></p>
<p>Correct path: click <strong>Reset to Draft</strong>. That moves the ECO back
to the first non-terminal stage with <code>state=open</code> and clears the
approval bookkeeping. Then re-approve and apply. The reset itself
posts a chatter line so the audit trail captures the reversal
explicitly — Apply refuses to silently revive a rejected ECO
because rejection is a deliberate decision worth a paper trail.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>Every action you take writes to <code>southbrook.eco</code> (the change order
itself) and posts a <code>mail.thread</code> message — the form inherits
<code>mail.thread</code> and <code>mail.activity.mixin</code>, so chatter is the audit
substrate.</p>
<p>The Apply handler's branching is determined by
<code>eco_type_id.target_kind</code> (a stored related field). For
<strong>BoM</strong> kind, the handler does <code>old.sudo().copy({...})</code> on
<code>mrp.bom</code>, bumping a custom <code>southbrook_version</code> integer the
estimating addon adds to <code>mrp.bom</code>; the prior BoM is archived
(<code>active=False</code>) but never deleted, so any MO that references it
still resolves. For <strong>cut_spec</strong> kind, the handler delegates to
<code>southbrook.cut.spec.action_activate()</code>, which both flips the
candidate to <code>state=active</code> and writes <code>state=superseded</code> on the
prior active spec — keeping the "exactly one active" invariant
that the cut-math seam <code>mrp_bom._get_cut_constants()</code> relies on.</p>
<p>The kanban-stage write guard lives in <code>southbrook.eco.write()</code>. It
intercepts any write that touches <code>stage_id</code>, checks two things —
(a) is the destination stage <code>approval_required</code>? if so, require
the PLM Approver group; (b) is the destination stage <code>is_final</code>?
if so, require the matching <code>state</code> value to be in the same write
— and raises UserError otherwise. Both rules are why Apply and
Reject co-write <code>state</code> and <code>stage_id</code> together rather than
separately.</p>
<p>The line-to-ECO bridge button on the order builder
(<code>sale.order.line.action_raise_eco</code>) opens a new ECO with
<code>default_eco_type_id</code>, <code>default_bom_id</code>, <code>default_title</code>, and
<code>default_description</code> set in the action context, so creating an
ECO about a specific sale-order line is one click + Save.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> A customer calls and asks you to switch their full-overlay
hinges to inset on three already-confirmed MOs. You raise a BoM
ECO and click Apply. The customer calls back the next day to
confirm the inset hinges have shipped — but the MOs you saw on the
floor today were still showing the old BoM. What's going on?</p>
<blockquote>
<p>Apply versioned the <em>template BoM</em>, but MOs confirmed before the
Apply hold a pointer to the BoM revision that was active at
confirm time. The new revision applies to the <em>next</em> MO created
from this product template, not retroactively. To push the
change through the in-flight MOs, you have to cancel and recreate
them (planner's call) or amend each MO's work orders manually.
The platform won't silently re-base in-flight production.</p>
</blockquote>
<p><strong>2.</strong> You start drafting a Cut-Geometry Revision ECO to bump the
shelf ventilation gap from 12.7 mm to 15 mm. The form's Target tab
is asking you for a "Candidate Cut Spec." You don't have one —
you've never authored a cut spec record. What's the right next step?</p>
<blockquote>
<p>Save the ECO as a draft, then go to <strong>Southbrook PLM → Cut
Specifications</strong>, click <strong>New</strong>, fill in the new constant
values (carry over all the others from the currently-active
spec), save it in <code>state=draft</code>. Come back to the ECO, point
<code>cut_spec_id</code> at the new draft, and then advance the ECO
through Under Review → Approved → Apply. Apply will call
<code>action_activate</code> on the candidate and supersede the previous
active spec. The cut spec is the artifact; the ECO is the
governance around activating it.</p>
</blockquote>
<p><strong>3.</strong> You try to drag an ECO card from the Approved column
straight to the Applied column on the kanban. The system refuses
with a UserError. Why does the guard exist, and what's the right
action instead?</p>
<blockquote>
<p>Dragging would write only <code>stage_id</code>, leaving <code>state=open</code> while
the kanban shows Applied — paradoxical and worse, the apply
handler (BoM copy / cut-spec activate / etc.) wouldn't run, so
the engineering effect <em>isn't actually committed</em>. The guard
blocks the bypass; the right action is to click the <strong>Apply
Change</strong> button in the header, which runs the handler AND
co-writes both <code>stage_id</code> and <code>state</code> in a single <code>write()</code> call.</p>
</blockquote>
<p><strong>4.</strong> A PLM Approver rejected an ECO that was actually correct;
the engineer has just convinced them it should have been applied.
Walk through the exact clicks to land the change cleanly without
losing the audit trail.</p>
<blockquote>
<p>Open the rejected ECO. Click <strong>Reset to Draft</strong> — this clears
<code>applied_date</code>, <code>approver_id</code>, <code>approval_date</code>, sends the ECO
to the first non-terminal stage, and posts a "Reset to draft
by X" chatter line. Click <strong>Next Stage</strong> to move it back to
Under Review. The Approver clicks <strong>Approve</strong> (stamps fresh
approval), then <strong>Apply Change</strong>. The chatter now shows both
the original reject AND the reset AND the new approval/apply —
a complete, honest history.</p>
</blockquote>
<p><strong>5.</strong> You're cleaning up a backlog and notice three Construction-
Rule ECOs that have been sitting at Approved for two weeks. You
check git — the PRs they reference were merged the same day each
ECO was approved. What should you do?</p>
<blockquote>
<p>Apply them. The Construction-Rule apply handler is record-only —
it posts a chatter line citing the <code>git_ref</code> and flips state to
Applied. It doesn't <em>cause</em> the code change (git does that); it
closes the audit loop so an outside auditor can see that
commit ABC123 was reviewed, approved, and acknowledged-as-shipped
by PLM. Leaving these at Approved indefinitely leaves the audit
trail incomplete — the merged code is live in production but
PLM hasn't formally acknowledged it.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>Authoring a <code>southbrook.cut.spec</code> draft from scratch — lesson 4.2.</li>
<li>The FreeCAD bridge that re-renders artifacts after a BoM ECO
  is applied — lesson 4.3.</li>
<li>The AI-design Gemini analysis flow (room photo → designer
  confirms dimensions) — lesson 4.4.</li>
<li>The release gate that decides whether a kitchen project can
  hand off to production at all — lesson 2.2.</li>
<li>Native Odoo <code>mail.thread</code> mechanics — Odoo's own training.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_04_cut_specs" model="slide.slide">
            <field name="name">Authoring a Cut Spec Sheet — What Fields Drive the Operator&#x27;s Job</field>
            <field name="channel_id" ref="course_plm_design"/>
            <field name="slide_category">article</field>
            <field name="sequence">20</field>
            <field name="description">ECO Engineer authoring a parametric cut specification for the cabinet panel-cut math.</field>
            <field name="html_content"><![CDATA[<h1 id="authoring-a-cut-spec-sheet-what-fields-drive-the-operators-job">Authoring a Cut Spec Sheet — What Fields Drive the Operator's Job</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the engineer who owns the NF14 cut constants — the panel
thicknesses, the reveal, the rabbet depth, the toe-kick height — and
you have to revise one of them without breaking the cabinets already
in production. A cut spec record isn't a per-cabinet drawing; it's
a single set of geometric constants that <em>every</em> template BoM reads
through the <code>_get_cut_constants()</code> seam. Get one number wrong here
and every base cabinet in the shop cuts wrong tomorrow. This lesson
is the practical reality of authoring one and the gotchas you'll hit.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<blockquote>
<p><strong>Southbrook PLM → Cut Specifications</strong></p>
</blockquote>
<p>You'll see one record marked <strong>Active</strong> (green decoration), zero or
more <strong>Draft</strong> records, and zero or more <strong>Superseded</strong> records
(muted decoration). The active spec is the one the cut math is
currently using; drafts are candidates waiting for an ECO to
activate them; supersededs are the audit trail.</p>
<p>ECOs that reference any cut spec live in the standard ECO list at
<strong>Southbrook PLM → Change Orders</strong>, but a small flag-icon smart
button on each cut spec's form (visible when `southbrook_eco_count</p>
<blockquote>
<p>0`) takes you directly to the ECOs that have proposed or activated
this spec.</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>The form on <code>southbrook.cut.spec</code> has four field groups, all in
millimetres:</p>
<ul>
<li><strong>Name</strong> (<code>name</code>) — free text. Convention: cite the workbook
  revision that produced these values, e.g. <em>2026 Workbook Rev B</em>.</li>
<li><strong>State</strong> (<code>state</code>) — <code>draft</code> / <code>active</code> / <code>superseded</code>. Statusbar
  widget at the top. <strong>You never write this field directly.</strong> The
  Activate button (which only an Approver sees, and only when not
  already active) flips draft → active and supersedes the previous
  active. ECO Apply on a Cut-Geometry Revision does the same thing
  via the <code>action_activate</code> handler.</li>
</ul>
<p><strong>Carcass group:</strong></p>
<ul>
<li><strong>Box / Carcass Thickness</strong> (<code>box_th</code>, default 15.875) — carcass
  material thickness in millimetres. 5/8" melamine is the
  Signature-Series standard. Drives the inside-width and
  inside-depth subtractions on every panel.</li>
<li><strong>Back-Panel Thickness</strong> (<code>back_th</code>, default 6.35) — 1/4"
  hardboard. Read by the rabbet computation.</li>
<li><strong>Rabbet Depth</strong> (<code>rabbet</code>, default 6.35) — the back-panel capture
  groove routed into the sides, top, and bottom. By convention
  matches <code>back_th</code>; if you change <code>back_th</code> you almost always
  change <code>rabbet</code> to match.</li>
</ul>
<p><strong>Door group:</strong></p>
<ul>
<li><strong>Door Thickness</strong> (<code>door_th</code>, default 18.0) — 3/4" slab or
  five-piece. Used by the door-cut math and by hinge selection
  rules downstream.</li>
<li><strong>Door Reveal</strong> (<code>door_reveal</code>, default 3.0) — uniform gap on all
  four door edges. Drives every door's outside-dimension subtraction
  off the cabinet opening. The single highest-leverage value in the
  spec — bumping reveal by 1 mm changes every door on every cabinet
  in the shop.</li>
</ul>
<p><strong>Shelf group:</strong></p>
<ul>
<li><strong>Shelf Tolerance</strong> (<code>shelf_tol</code>, default 1.5) — hand-placement
  clearance subtracted from inside width. Bigger value = looser
  shelf, more rattle; smaller value = tighter, harder to place.</li>
<li><strong>Shelf Ventilation Gap</strong> (<code>shelf_vent_gap</code>, default 12.7) — 1/2"
  subtracted from shelf depth at the back so air circulates around
  contents. Distinct from <code>back_th</code> — even with a 6 mm back, you
  still want a 12.7 mm vent gap.</li>
</ul>
<p><strong>Toe-Kick group:</strong></p>
<ul>
<li><strong>Toe-Kick Height</strong> (<code>toekick_h</code>, default 101.6) — 4" standard,
  integrated into the side panels for base/sink/tall/vanity
  cabinets. Driven by ergonomics, not material — almost never
  changes.</li>
</ul>
<p><strong>Provenance:</strong></p>
<ul>
<li><strong>Note</strong> (<code>note</code>, Text) — where these values came from. The seed
  spec note reads "<em>NF14 reminder: the seed spec values are
  ASSUMED until the canonical #8 workbook lands.</em>" If you author
  a new spec, cite the workbook revision, the measurement event,
  and the person who took the measurement. Auditors read this.</li>
</ul>
<p><strong>Smart button:</strong></p>
<ul>
<li><strong>ECOs</strong> — count of <code>southbrook.eco</code> records referencing this
  spec via <code>cut_spec_id</code>. Click-through opens the ECO list
  filtered to this spec with <code>default_cut_spec_id</code> and
  <code>default_eco_type_id</code> pre-set, so creating a new ECO from this
  view is one click.</li>
</ul>
<h2 id="how-a-cut-spec-ties-to-a-bom-line-an-mo-and-the-operators-screen">How a cut spec ties to a BoM line, an MO, and the operator's screen</h2>
<p>The cut spec doesn't live on the BoM directly. Instead:</p>
<ol>
<li>
<p><strong>At BoM compute time</strong>, <code>mrp.bom._get_cut_constants()</code> reads
   the active spec via
   <code>self.env["southbrook.cut.spec"].sudo()._get_active()</code> and
   returns its <code>constants_dict()</code> — a mapping like
   <code>{"box_th": 15.875, "back_th": 6.35, …}</code>. The estimating
   module's panel-cut formulas consume this dict. When no spec is
   active (fresh install, or all superseded), the seam falls back
   to the module-level Python defaults via <code>super()</code>.</p>
</li>
<li>
<p><strong>At sale-order confirm time</strong>, the line captures a snapshot:
   <code>sale.order.line.southbrook_cut_spec_version_id</code> (m2o to
   <code>southbrook.cut.spec</code>) and <code>southbrook_bom_version</code> (Integer)
   are written by <code>_capture_southbrook_version_snapshots</code>, called
   from <code>sale.order.action_confirm</code>. The snapshot is the
   point-in-time the manufacturing chain reads — an ECO that
   activates a new spec mid-day does <strong>not</strong> retroactively change
   the panel cuts of orders confirmed before it. Two different
   MOs confirmed before and after the same Apply can legitimately
   use two different cut specs.</p>
</li>
<li>
<p><strong>At MO release</strong>, the work order's cut list — what lands on
   the CNC operator's screen as the panel-dimension labels — is
   derived from the line's BoM × line's cut-spec snapshot. The
   operator never sees the spec name; they see <em>outputs</em> (panel
   width 596.25 mm, depth 555.5 mm, rabbet 6.35 mm). The
   planner reading the MO sees the spec snapshot in a backend
   view so they know which constants the cut was computed
   against.</p>
</li>
<li>
<p><strong>The edge banding operator</strong> doesn't read the cut spec at
   all — they read the panel ID and the edge-tape side spec
   off the cut-spec label printed at CNC (lesson 1.7). Two
   panels from two different cut specs on the same shift are
   indistinguishable at the bander; the difference is in the
   sub-millimetre dimensions, which the bander pulls from the
   panel itself, not from a spec record.</p>
</li>
<li>
<p><strong>The assembler</strong> reads the same cut-spec label for the
   reveal — they need to know what gap to expect between the
   door and the cabinet face so they can spot a hinge-shim
   issue before the cabinet ships. The reveal value visible on
   the assembler's shop drawing IS <code>door_reveal</code> from the spec
   active at the time the MO was confirmed.</p>
</li>
</ol>
<h2 id="your-daily-flow">Your daily flow</h2>
<p><strong>1. Why you're authoring a new cut spec.</strong></p>
<p>A cut spec revision is the single highest-leverage change in the
entire PLM workflow. You author a new spec when:</p>
<ul>
<li>The material vendor substitutes 5/8" (15.875 mm) melamine with
  18 mm and you have to ship the change to production tomorrow.</li>
<li>A QA review of returns finds that 3 mm door reveal is showing
  too much shadow line and customer service wants 2 mm.</li>
<li>An ergonomics review for a senior-living account requires a
  taller toe-kick.</li>
<li>A workbook revision (Peter Tuschak signs off on a new spec
  table) lands and the seeded "assumed" defaults need to be
  replaced with measured values.</li>
</ul>
<p>You DO NOT author a new cut spec when:</p>
<ul>
<li>Only one cabinet template needs a different panel thickness —
  that's a BoM-kind ECO on that one template's BoM.</li>
<li>A construction rule changes (e.g. Contractor series no longer
  offers maple boxes) — that's a Construction-Rule ECO on the
  config_rules.xml file.</li>
<li>A vendor cut sheet changed but the geometry didn't — that's an
  Engineering Document ECO with the new PDF attached.</li>
</ul>
<p><strong>2. Authoring the draft.</strong></p>
<ul>
<li>From <strong>Cut Specifications</strong>, click <strong>New</strong>.</li>
<li>The form opens with all eight constants pre-filled from the
  hard-coded defaults (NOT from the currently-active spec —
  the form's <code>default</code> annotations come from the field
  definitions). <strong>This is the most common pitfall.</strong> If you
  only meant to change <code>door_reveal</code>, you need to copy across
  the other seven values from the active spec by hand before
  saving, otherwise you'll silently revert any of them that
  the active spec had bumped away from the seed defaults.</li>
<li>Set the <strong>Name</strong> to cite the workbook revision and the date —
  "Workbook Rev C — 2026-07-15" beats "new spec."</li>
<li>Fill the <strong>Note</strong> field with provenance: which workbook, who
  signed off, what changed from the prior spec, and why.</li>
<li>Save. The state is <strong>Draft</strong>.</li>
</ul>
<p><strong>3. Wiring the draft to an ECO.</strong></p>
<ul>
<li>Go to <strong>Southbrook PLM → Change Orders</strong>, click <strong>New</strong>.</li>
<li>Type: <strong>Cut-Geometry Revision</strong> (this is the
  <code>target_kind=cut_spec</code> ECO type).</li>
<li>Target tab: pick your draft cut spec in <code>cut_spec_id</code>.</li>
<li>Title and Description: cite the specific constants that
  changed AND the prior values. "Bump door_reveal 3.0 → 2.0 mm
  per Workbook Rev C; all other constants unchanged." A
  diff-style description is what an auditor wants.</li>
<li>Advance Draft → Under Review → Approved → Apply. (See
  lesson 4.1 for the gate semantics.)</li>
<li>Apply runs the <code>_apply_cut_spec</code> handler, which calls
  <code>action_activate()</code> on your draft. The draft flips to
  <code>state=active</code>; the previously-active spec flips to
  <code>state=superseded</code>. The chatter on both records captures the
  swap. The active-spec invariant
  (<code>_check_single_active</code>: at most one record with
  <code>state=active</code>) holds.</li>
</ul>
<p><strong>4. Validating the activation took effect.</strong></p>
<ul>
<li>Refresh <strong>Cut Specifications</strong>. Your draft now shows green
  (Active). The previous active shows muted (Superseded).</li>
<li>Open any cabinet template's mrp.bom form and compute a
  panel — the dimensions should reflect the new constants.
  (In practice you'll do this via the estimating addon's
  preview tab, not by hand-calling <code>_get_cut_constants</code>.)</li>
<li>Find a sale order that was confirmed before today and look
  at its order lines: the <code>southbrook_cut_spec_version_id</code>
  snapshot still points at the <em>old</em> spec. This is correct.
  The change ONLY affects orders confirmed from this point
  forward.</li>
</ul>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I created a new cut spec to change the door reveal, but when
the ECO applied, the shop's panel cuts went weird — three other
constants also moved back to their original defaults."</strong></p>
<p>You authored your draft starting from the default values
(15.875, 6.35, 6.35, 18.0, 3.0, 1.5, 12.7, 101.6) instead of
copying the currently-active spec's values forward. The active
spec had bumped <code>box_th</code> to 18.0 (for the new material vendor)
and <code>door_th</code> to 19.0 (for a heavier door style), but your
draft kept them at 15.875 and 18.0. When the ECO activated, the
shop floor started cutting with the seed defaults plus your
new reveal. Recovery: author a NEW draft cut spec that copies
the <em>previous</em> active spec's values forward AND keeps your
intended reveal change. Raise a new Cut-Geometry Revision ECO,
title it explicitly as a corrective ("Correct yesterday's
reveal-only revision to preserve box_th and door_th from
Workbook Rev B"), and apply. The chatter trail will read clean.</p>
<p><strong>"I tried to activate a draft cut spec directly from the form
button, and it worked — no ECO needed."</strong></p>
<p>The Activate button is gated to the PLM Approver group and IS
the same code path the ECO apply handler uses; it works because
the activation is technically just a record-write. <strong>But</strong> the
ECO trail is how an auditor traces <em>why</em> the spec changed. If
you activate without raising an ECO, the cut-spec record's
chatter will show "Activated as the live cut specification" but
nothing about the workbook revision, the approval, the
rationale. The convention is: always go through an ECO. The
direct-activate button exists as an admin escape hatch, not a
daily-flow tool. Set yourself a personal rule: only click it
if you're recovering from a corrupted ECO and a comment block
explains why.</p>
<p><strong>"My tolerance bands are tight — I set shelf_tol to 0.5 mm and
now every shelf is binding in the cabinet."</strong></p>
<p>Shelf tolerance is <em>hand-placement clearance</em>, not the
shelf-pin precision. 0.5 mm is below the dimensional accuracy
of the CNC's panel-saw kerf, never mind the carpenter's hand.
The seed default of 1.5 mm exists because below it, even a
perfectly-cut shelf binds against unavoidable carcass
variation. Recovery: raise a new Cut-Geometry Revision ECO,
bump <code>shelf_tol</code> back to at least 1.5 mm. The over-tight
shelves in production are a one-shift issue — sand them down
by 1 mm at the assembly station. Document the recovery in
the new ECO's description as a lesson.</p>
<p><strong>"I forgot to write the edge-tape side spec on the cut spec
record."</strong></p>
<p>The cut spec doesn't carry edge-tape side spec at all — that's
a per-cabinet, per-panel concern that lives on the BoM
(<code>mrp.bom.line</code> rows for each panel carry the edge-tape attribute
sub-records). The cut spec carries only the eight geometric
constants. If you find yourself looking for a side-spec field
on <code>southbrook.cut.spec</code>, you've reached for the wrong tool;
the right tool is a BoM-kind ECO on the specific cabinet
template.</p>
<p><strong>"I bumped <code>door_reveal</code> from 3.0 to 2.0 and the configurator's
3D preview still shows a 3 mm gap on existing quotes."</strong></p>
<p>Existing quotes — sale orders in draft state — read the cut
spec at render time, so they SHOULD reflect the new value.
Refresh the browser; if it's still wrong, the assets bundle
may be cached. For sale orders already CONFIRMED, the snapshot
(<code>southbrook_cut_spec_version_id</code>) pins them to the OLD spec,
which is correct behaviour — those orders are in production,
their panels are mid-cut, you don't want the rendered preview
to drift from the actual material on the floor.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>The cut spec is a versioned record store; the cut math is a
seam (<code>mrp.bom._get_cut_constants()</code>) that the PLM module
overrides to read the live spec. The override falls back to
<code>super()</code> (the code defaults defined in
<code>southbrook_estimating/models/mrp_bom.py</code> as module constants)
when no spec is active, so installing the PLM module on a
fresh install never silently changes cut output until the
first spec is activated.</p>
<p><code>action_activate</code> is the single state-mutation entry point.
It checks if <code>self</code> is already active (no-op), finds the prior
active via <code>_get_active()</code>, supersedes it with a chatter post
on both records, and writes <code>state=active</code> on <code>self</code>. The
"exactly one active" invariant is enforced by the
<code>@api.constrains("state")</code> constraint
<code>_check_single_active</code>, which queries
<code>search_count([("state", "=", "active")])</code> after every write
and raises ValidationError if it exceeds 1. (Implemented as
a Python constraint rather than a PostgreSQL partial-unique
index because the latter needs the <code>btree_gist</code> extension,
which isn't part of the standard PG image we ship.)</p>
<p>The cut-spec snapshot pattern on <code>sale.order.line</code> is the
load-bearing piece of the manufacturing immune system: a
line's
<code>_capture_southbrook_version_snapshots</code> runs at
<code>action_confirm</code>, writes the active spec's <code>id</code> to
<code>southbrook_cut_spec_version_id</code>, and the snapshot is
<em>idempotent</em> — re-confirming a line (which Odoo guards
against anyway) preserves the existing snapshot. Manufacturing
reads from the snapshot, never from the currently-active
spec, so mid-day ECO applies are safe.</p>
<p>The seam read is wrapped in <code>sudo()</code> because portal users
(customers fetching their order payload via
<code>/southbrook/api/order/&lt;id&gt;</code>) need the panel dimensions
computed, but <code>ir.model.access.csv</code> restricts cut-spec read
to the PLM User group. The portal user never sees the spec
record directly — only the derived panel dimensions — so the
sudo is safe.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> You raise a Cut-Geometry Revision ECO that bumps
<code>door_reveal</code> from 3.0 mm to 2.5 mm. You hit Apply at 11:00.
A sale order was confirmed at 09:00 this morning; a different
sale order is confirmed at 13:00. Which cut spec do their
respective MOs cut against?</p>
<blockquote>
<p>The 09:00 order's lines captured the OLD cut spec (door
reveal 3.0 mm) as their <code>southbrook_cut_spec_version_id</code>
snapshot at confirm time. Its MOs read from the snapshot,
not the live spec, and cut with reveal 3.0 mm — by design.
The 13:00 order's lines were confirmed after the activate
at 11:00, so their snapshot points at the NEW spec; their
MOs cut with reveal 2.5 mm. This is the whole point of the
snapshot pattern: two days of orders on two different specs
live side-by-side in the shop without interfering.</p>
</blockquote>
<p><strong>2.</strong> A vendor swap means melamine carcass material is now
18 mm instead of 15.875 mm. You author a draft cut spec
called "Vendor Swap — 18 mm Melamine," fill in only
<code>box_th = 18.0</code>, save, and raise an ECO. What goes wrong on
Apply, and what's the fix?</p>
<blockquote>
<p>Your draft set <code>box_th = 18.0</code> but inherited the seed
defaults for everything else: <code>back_th=6.35</code>, <code>rabbet=6.35</code>,
<code>door_th=18.0</code>, <code>door_reveal=3.0</code>, <code>shelf_tol=1.5</code>,
<code>shelf_vent_gap=12.7</code>, <code>toekick_h=101.6</code>. If the
currently-active spec had bumped <em>any</em> of those (say
<code>door_th=19.0</code> for a heavier door style), Apply will
activate your draft and lose that bump — every cabinet's
door cut goes back to 18 mm. Fix: open the currently-active
spec, screenshot every value, edit your draft to copy them
forward except <code>box_th</code>, save, and re-apply (or raise a
follow-up ECO that restores the lost values). The cut spec
is not a delta — it's a complete record. Always copy
forward.</p>
</blockquote>
<p><strong>3.</strong> Your operator escalates: "the shelves on yesterday's
base cabinets are too loose, they rattle when you close the
door." You check the active cut spec — <code>shelf_tol</code> is 3.0
mm. What's the right corrective ECO, and what risks are you
weighing?</p>
<blockquote>
<p>Raise a Cut-Geometry Revision ECO that drops <code>shelf_tol</code>
from 3.0 mm to the historical 1.5 mm default (the value
the seam used before any spec activation). Copy the other
seven constants forward unchanged. The risk of going below
1.5 mm: shelves may bind against carcass variation, which
at the panel-saw kerf precision is a coin-flip. Don't go
below 1.5 mm without a measurement event that demonstrates
the saw is holding tighter than that. The yesterday-shipped
cabinets that already left at 3.0 mm are a customer-service
issue, not a cut-spec issue — those carcasses are in homes,
and the spec snapshot on their MOs correctly records the
3.0 mm decision.</p>
</blockquote>
<p><strong>4.</strong> A junior designer hit the <strong>Activate</strong> button on a
draft cut spec without raising an ECO. The new spec is live;
the previous one is superseded; the audit trail shows
"Activated as the live cut specification." Should you
roll it back? Walk through the steps if so.</p>
<blockquote>
<p>The activation itself was a legitimate state move — the
Approver group is what gates the button, and they had it.
The problem is the missing ECO trail: an auditor reading
the cut-spec chatter has no "why" — no workbook revision
cited, no approval rationale. Don't roll back the values
if they're correct; instead, raise a <em>retroactive</em>
Cut-Geometry Revision ECO pointing at the now-active
spec, with a description like "Audit-completion ECO for
manual activation of <spec name> by <designer> at
<timestamp>; rationale: <workbook cite>." Apply it. The
handler is a no-op on an already-active spec (the
<code>if self.state == "active": return True</code> early-return),
so nothing changes geometrically — but the audit chatter
now references the ECO, closing the loop. Counsel the
junior to go through ECO next time.</p>
</blockquote>
<p><strong>5.</strong> You're explaining to a new edge-banding operator
why the panels they're banding today look identical to
yesterday's but the planner says "different cut spec." What
do you say?</p>
<blockquote>
<p>The panel they're physically handling has its dimensions
printed on the cut-spec label at CNC — those dimensions
ARE the difference. Yesterday's spec might have had
<code>back_th=6.35</code>; today's might have <code>back_th=9.0</code>. The
visible panel looks the same because it's the SIDE panel,
not the back, and side dimensions are dominated by box
thickness and reveal — both unchanged. The edge tape and
the banding routine are identical; the operator's job
doesn't change. The reason the planner cares is that the
sub-millimetre door reveal change shifts how the assembler
sees the door-to-cabinet gap, and the assembler will flag
a defect if it doesn't match the day's spec.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>Raising the ECO that activates a cut spec — lesson 4.1.</li>
<li>The FreeCAD render artifacts that update after a spec
  change — lesson 4.3.</li>
<li>Per-panel edge-tape side spec — that's on the BoM line,
  not the cut spec; see Course 1 (CNC + edge banding).</li>
<li>How the edge banding operator reads a cut-spec <em>label</em>
  on a physical panel — lesson 1.7.</li>
<li>Native Odoo <code>mrp.bom</code> mechanics — Odoo's own training.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_04_freecad_render" model="slide.slide">
            <field name="name">FreeCAD Bridge — Generating Renderings from a Configured Kitchen</field>
            <field name="channel_id" ref="course_plm_design"/>
            <field name="slide_category">article</field>
            <field name="sequence">30</field>
            <field name="description">Designer who reviews customer-facing renderings and shop-floor CAD artifacts before they go out.</field>
            <field name="html_content"><![CDATA[<h1 id="freecad-bridge-generating-renderings-from-a-configured-kitchen">FreeCAD Bridge — Generating Renderings from a Configured Kitchen</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the designer who looks at every customer-facing rendering
before it ships and every shop drawing before it goes to the CNC
operator. Sometimes the rendering is wrong — a door is on the wrong
side, a panel is undersized, the cabinet shows a corner-cut where the
sale order says square. You need to know whether that's a
configurator-input problem (fix the spec) or a rendering problem
(regenerate). This lesson is the practical reality of how a
manufacturing order causes a FreeCAD job to spawn, where the
artifacts land, and what to do when one's wrong.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<blockquote>
<p><strong>Manufacturing → Operations → Manufacturing Orders → [pick an MO] → CAD Artifacts</strong></p>
</blockquote>
<p>The bridge surface is a notebook page injected onto the MO form by
<code>southbrook_freecad_bridge</code>. The page is called <strong>CAD Artifacts</strong>
and lives next to Components, Work Orders, the rest of the native
MO tabs. It's visible to anyone with MO read access; the actions
on it (Regenerate CAD) are gated to the <strong>Manufacturing /
Manager</strong> group.</p>
<p>There is no separate "Renderings" menu. The artifact lives on the
MO that produced it — that's the unit of provenance, because two
MOs of the same cabinet template can cut to different cut specs
(see lesson 4.2) and therefore produce different geometry.</p>
<p>For backend admins who need to inspect the bridge service itself:</p>
<blockquote>
<p><strong>Settings → Technical → System Parameters</strong></p>
</blockquote>
<p>Three relevant parameters:</p>
<ul>
<li><code>freecad_bridge.enabled</code> — master kill switch. Defaults to
  <strong>false</strong> on install (the G2a opt-in gate). Until an admin flips
  this to <strong>true</strong>, MO confirms do NOT POST to the bridge, and
  the Regenerate CAD button raises UserError.</li>
<li><code>freecad_bridge.url</code> — service URL. Defaults to
  <code>http://southbrook-freecad-bridge:8000</code> (the docker network name).</li>
<li><code>freecad_bridge.secret</code> — shared secret matching the bridge
  service's <code>FREECAD_BRIDGE_SECRET</code> env var. The bridge rejects
  requests without it; Odoo's callback rejects requests with a
  mismatching <code>X-Bridge-Secret</code> header.</li>
</ul>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>The <strong>CAD Artifacts</strong> tab on <code>mrp.production</code> shows:</p>
<ul>
<li><strong>CAD Status</strong> (<code>x_cad_status</code> on <code>mrp.production</code>) — a Selection
  field rendered as a badge with four states:</li>
<li><strong>Pending</strong> (blue) — the MO has been confirmed but the bridge
    POST hasn't fired yet, or the bridge gate is off and the POST
    was suppressed. Default on a fresh MO.</li>
<li><strong>Rendering</strong> (yellow/warning) — the bridge accepted the job
    (HTTP 2xx on POST <code>/render</code>) and is currently producing the
    artifacts. The bridge runs renders asynchronously off the
    HTTP request, so this state can persist for tens of seconds
    to a couple of minutes.</li>
<li><strong>Done</strong> (green) — the bridge's background task completed and
    POSTed the callback (<code>/plm/cad_callback</code>) with attachment IDs;
    <code>x_cad_attachment_ids</code> is populated and the rendering is
    review-ready.</li>
<li>
<p><strong>Error</strong> (red) — either the POST to the bridge failed (network,
    auth, 5xx) or the bridge's background task came back with
    <code>status=error</code> in the callback. The chatter carries the
    specific error message.</p>
</li>
<li>
<p><strong>Governing ECO</strong> (<code>x_plm_eco_id</code> → <code>southbrook.eco</code>) — m2o to
  the Engineering Change Order whose approved revision of the
  template BoM this MO was built against. Empty when no
  PLM-governed BoM revision is in scope. Click-through to read
  the ECO's rationale and history.</p>
</li>
<li>
<p><strong>Regenerate CAD button</strong> — visible to Manufacturing Manager only.
  Triggers <code>action_regenerate_cad</code>, which re-runs
  <code>_post_cad_render_job</code> and flips status back through Pending →
  Rendering. Confirmation prompt warns that existing artifacts
  will be superseded.</p>
</li>
<li>
<p><strong>CAD Artifacts</strong> (<code>x_cad_attachment_ids</code>) — many2many to
  <code>ir.attachment</code>, rendered with the <code>many2many_binary</code> widget so
  each file appears as a download chip. The bridge typically
  produces 4 artifacts per cabinet:</p>
</li>
<li><strong>DXF</strong> (one per panel) — for the panel-saw / CNC nesting.</li>
<li><strong>SVG</strong> — shop drawing, customer-or-floor-facing.</li>
<li><strong>PDF</strong> — TechDraw orthographic three-view (front/side/top).</li>
<li><strong>STEP AP214</strong> — assembly STEP file for the assembler's
    reference and for any vendor needing CAD interchange.</li>
</ul>
<p>On the kanban (shop-floor) view of MOs, the same <code>x_cad_status</code>
field is shown as a colour-coded badge so the planner can see at
a glance which MOs are CAD-ready vs blocked.</p>
<h2 id="how-a-render-gets-triggered">How a render gets triggered</h2>
<p>There are two trigger paths and both go through the same Python
method (<code>mrp.production._post_cad_render_job</code>):</p>
<p><strong>1. MO confirm hook (automatic, opt-in).</strong></p>
<p>When <code>mrp.production.action_confirm()</code> runs, the Southbrook
extension fires after <code>super()</code>. For each newly-confirmed MO, it
checks <code>_g2a_enabled(env)</code> — the system parameter
<code>freecad_bridge.enabled</code> parsed as <code>true</code>/<code>1</code>/<code>yes</code>. If on, it
calls <code>_post_cad_render_job()</code>; if off, it logs the suppression
and returns. Exceptions in the POST are caught and logged but
never re-raised, because a bridge outage must NOT block MO
confirm — confirmation is the manufacturing system's load-bearing
event and cannot depend on a render service being up.</p>
<p><strong>2. Manager-clicked Regenerate.</strong></p>
<p>The Regenerate CAD button on the MO form calls
<code>action_regenerate_cad</code>. Unlike the on-confirm hook, this one
raises UserError loudly if the gate is off — a manager click
should surface the configuration problem, not silently no-op.</p>
<h2 id="what-odoo-sends-to-the-bridge">What Odoo sends to the bridge</h2>
<p><code>_spec_for_bridge</code> constructs the payload from the MO's product
template. The shape is:</p>
<pre><code class="language-json">{
  &quot;production_id&quot;: 4711,
  &quot;dimensions&quot;: {
    &quot;width_mm&quot;:  600.0,
    &quot;height_mm&quot;: 720.0,
    &quot;depth_mm&quot;:  580.0
  },
  &quot;family&quot;: &quot;base&quot;,
  &quot;door_count&quot;: 1,
  &quot;output_dir&quot;: &quot;/srv/output/mo/4711&quot;
}
</code></pre>
<p>Sources of each field:</p>
<ul>
<li><code>production_id</code> — <code>self.id</code>.</li>
<li><code>dimensions</code> — <code>x_default_width_mm</code>, <code>x_default_height_mm</code>,
  <code>x_default_depth_mm</code> on the product template. Sensible defaults
  (600/720/580 mm — a typical base cabinet) when the template
  hasn't set them.</li>
<li><code>family</code> — <code>x_cabinet_family</code> on the product template. Defaults
  to <code>"base"</code> if missing. Tells the bridge which master to
  invoke (<code>master_base.py</code>, <code>master_wall.py</code>, etc.).</li>
<li><code>door_count</code> — <code>x_default_door_count</code> on the product template
  (1, 2, or 0 for drawer banks).</li>
<li><code>output_dir</code> — where on the bridge filesystem to write the
  rendered artifacts. The bridge attaches them to Odoo by XML-RPC
  after the render completes, but the on-disk copy stays for
  troubleshooting.</li>
</ul>
<p>The POST is to <code>&lt;bridge_url&gt;/render</code> with header <code>X-Bridge-Secret</code>
and a 10-second connect timeout. The bridge responds with a job_id
+ <code>status: "queued"</code> and runs the actual FreeCAD render off-thread.</p>
<p>A future Module-2 enhancement will swap the template-defaults
above for the configurator's per-MO spec (so an MO for a 720
mm-wide base reads 720, not the template's 600 default). Until
that lands, the Phase-1 mapping is template-driven.</p>
<h2 id="the-bridge-job-state-machine">The bridge job state machine</h2>
<p>From <code>services/freecad_bridge/main.py</code> the job carries:</p>
<ul>
<li><strong>queued</strong> — bridge accepted the POST, job_id assigned, the
  background task has been scheduled. The bridge tells Odoo to
  flip MO status to <code>rendering</code> at this point.</li>
<li><strong>rendering</strong> — the background task is running <code>xvfb-run
  freecadcmd render_cabinet.py</code> against the parametric <code>.FCStd</code>
  master, generating DXF/SVG/PDF/STEP into the output_dir.</li>
<li><strong>done</strong> — render completed, artifacts uploaded to Odoo as
  <code>ir.attachment</code> records via XML-RPC, callback POSTed to
  <code>/plm/cad_callback</code> with <code>attachment_ids</code>.</li>
<li><strong>error</strong> — render failed at some stage (FreeCAD crashed,
  geometry validation rejected the panels, output files missing).
  Callback POSTed with <code>status: "error"</code> and an error string.</li>
</ul>
<p>The job table is in-memory (a Python dict) — when the bridge
process restarts, in-flight jobs are lost. This is intentional
for Module 2 scope; if a render is interrupted the MO sits at
<code>rendering</code> indefinitely until someone clicks Regenerate. A
persistent queue (Redis or RQ) is explicitly out of scope until
Module 4.</p>
<h2 id="where-the-rendered-artifacts-land">Where the rendered artifacts land</h2>
<p>Three places, in this order:</p>
<p><strong>1. On the bridge filesystem.</strong> <code>/srv/output/mo/&lt;production_id&gt;/</code>
inside the bridge container. DXF, SVG, PDF, STEP files. These are
useful for troubleshooting — <code>docker exec</code> into the bridge and
inspect — but are NOT what users see.</p>
<p><strong>2. As <code>ir.attachment</code> records in Odoo.</strong> The bridge's
background task uses XML-RPC (<code>ir.attachment.create</code>) to upload
each file's bytes as a separate attachment, captures the
returned IDs.</p>
<p><strong>3. Linked to the MO via <code>x_cad_attachment_ids</code>.</strong> The bridge's
callback POSTs <code>attachment_ids</code> to <code>/plm/cad_callback</code>; the
controller writes <code>x_cad_attachment_ids = [(6, 0, [...])]</code> on
the MO. The notebook tab's <code>many2many_binary</code> widget now shows
download chips for each.</p>
<p>Crucially: there is <strong>no separate "Renderings" model</strong> and <strong>no
customer-portal direct attach</strong> in the v1 surface. If a customer
needs the rendering, the dealer portal's installation-PDF export
embeds the orthographic SVG views inline via the
<code>/render_elevation</code> endpoint (a separate, synchronous bridge
route distinct from the async <code>/render</code>). The per-MO STEP and
DXF artifacts are shop-floor artifacts; the customer-facing
rendering is a different code path.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p><strong>1. Review queue — first thing in the morning.</strong></p>
<ul>
<li>Open <strong>Manufacturing → Manufacturing Orders</strong> filtered to
  <em>Status = Confirmed AND CAD Status = Pending or Rendering</em>.</li>
<li>A handful of yellow (Rendering) badges is normal for MOs
  confirmed in the last hour. A pile of yellow more than 30
  minutes old means a stuck job — check the bridge service
  health (see "Common mistakes").</li>
<li>Red (Error) badges are your priority queue.</li>
</ul>
<p><strong>2. Per-MO review (the loop).</strong></p>
<p>For each green (Done) MO from your shift:</p>
<ul>
<li>Open the MO, click the <strong>CAD Artifacts</strong> tab.</li>
<li>Download the PDF (orthographic three-view) and confirm it
  matches the sale order's spec — door count, hinge side,
  finished sides, family. The sale-order spec is what the
  customer agreed to; the rendering is what manufacturing will
  cut. They must match.</li>
<li>If they don't match, raise an ECO (lesson 4.1) — the BoM
  is wrong, not the rendering. Regenerating won't help.</li>
<li>If the rendering shows obvious geometry corruption (panels
  overlapping, dimensions impossible, blank pages), click
  <strong>Regenerate CAD</strong> to re-fire the bridge.</li>
</ul>
<p><strong>3. Handling Error states.</strong></p>
<ul>
<li>Open the MO, look at the chatter — the error message is
  there with prefix "CAD render request failed:" or "Bridge
  HTTP 500: …".</li>
<li>Common messages and what they mean:</li>
<li><code>bridge_secret_unset</code> — Odoo's <code>freecad_bridge.secret</code>
    parameter isn't set. Admin issue, not yours.</li>
<li><code>bridge HTTP 400</code> (<code>unknown_template</code>) — the cabinet
    template's <code>family</code> doesn't map to a <code>.FCStd</code> master file
    the bridge can find. PLM-side fix (BoM ECO to correct the
    family, or admin uploads a new master).</li>
<li><code>bridge HTTP 500</code> (<code>freecadcmd_exit_&lt;n&gt;</code>) — FreeCAD crashed
    on this geometry. Often a non-positive panel dimension
    indicates a bad cut spec. Check the active cut spec and
    the MO's dimensions; raise a Cut-Geometry ECO if the
    constants are wrong.</li>
<li><code>non_positive_panel:&lt;name&gt;</code> — the bridge's pre-render
    <code>/validate</code> step caught a panel with a zero or negative
    dimension. Same root cause as above.</li>
<li>
<p>Network/connection errors — the bridge service is down
    or the docker network is broken. Sysadmin issue.</p>
</li>
<li>
<p>Once you've understood the cause, click <strong>Regenerate CAD</strong> to
  retry. If the cause was on the PLM side (BoM or cut spec), fix
  the cause first via an ECO, wait for Apply, then regenerate.</p>
</li>
</ul>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I clicked Regenerate CAD and got 'FreeCAD bridge is disabled.'
What do I do?"</strong></p>
<p>The <code>freecad_bridge.enabled</code> system parameter is off. This is the
G2a gate — the safe default on install. Ask an admin to set it
to <code>true</code> at <strong>Settings → Technical → System Parameters</strong>. (Note:
the on-confirm hook silently no-ops in this state, but the
manual button raises UserError so you don't miss the
configuration gap.)</p>
<p><strong>"The MO has been at Rendering for 45 minutes and the floor
needs the DXF for the next CNC slot."</strong></p>
<p>A render that long usually means the bridge process died
mid-render and the in-memory job table cleared on restart. Click
<strong>Regenerate CAD</strong> to re-fire. If the new job also stalls,
escalate to sysadmin — the bridge service itself needs
investigation. <strong>Do not</strong> plan around the missing DXF by
hand-drafting one; the geometry source of truth is the bridge's
<code>southbrook_dims</code> module, and a hand-drafted panel will drift.</p>
<p><strong>"The rendering looks correct, but the customer is asking why
the door grain on the visual is horizontal when they ordered
vertical."</strong></p>
<p>The bridge renders parametric carcass geometry — it doesn't
apply material textures, grain direction, or finish colour.
Those are visualisation concerns the customer-facing webGL
configurator handles (see Course 5). The MO's CAD Artifacts
are shop drawings: panel dimensions, joinery, hinge positions.
If the customer is reviewing a rendering with material
detail, they're looking at a configurator screenshot, not a
bridge artifact. Escalate to whoever cut the screenshot.</p>
<p><strong>"I see a door on the wrong side of the rendering, but the
sale order spec is correct."</strong></p>
<p>The Phase-1 bridge reads template defaults
(<code>x_default_door_count</code>, <code>x_cabinet_family</code>) — NOT the
configurator's per-MO spec. If your cabinet template's default
is "hinge_side=L" and the order configured "hinge_side=R",
the rendering will show the template's L. This is a known
Phase-1 limitation that Module 2 closes by swapping in the
configurator's spec. Workaround until that lands: explain to
the customer that the SHOP DRAWING is to-scale and dimensioned
correctly; the visual orientation of the door swing is from the
template stock master. Don't regenerate — it won't change.</p>
<p><strong>"I raised an ECO that bumped the BoM and applied it. Should I
regenerate CAD on every in-flight MO of that template?"</strong></p>
<p>No. The CAD artifacts for an in-flight MO are valid as long as
the MO's <code>southbrook_bom_version</code> and
<code>southbrook_cut_spec_version_id</code> snapshots (captured at line
confirm time, see lesson 4.2) match what the artifacts were
rendered against. The MOs in flight cut to those snapshots, so
their existing CAD is correct. New MOs after the Apply will
render against the new BoM version on confirm. Regenerating
in-flight MOs would produce CAD that does NOT match the
material on the floor — confusing rather than helpful.</p>
<p><strong>"The render is wrong because the configurator-fed dimensions
don't match what the model master expects."</strong></p>
<p>That's a <em>parametric configurator</em> issue, not a render issue.
The bridge takes width/height/depth/family/door_count as input
and produces geometry. If the input is wrong (configurator gave
incorrect dimensions), regenerating won't help — fix the
configurator output via a Construction-Rule ECO if the rule
is wrong, or via a sale order edit if the user picked wrong.
If the input is correct but the model master misinterprets it,
that's a bridge-side bug — file it with the FreeCAD masters
team (the masters live in <code>services/freecad_bridge/cabinet_
masters/</code>).</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>When you click Confirm on an MO, this happens (assuming the
gate is on):</p>
<ol>
<li><code>mrp.production.action_confirm()</code> runs Odoo's stock
   confirm logic via <code>super()</code>.</li>
<li>The Southbrook override iterates the just-confirmed MOs and
   calls <code>_post_cad_render_job()</code> on each.</li>
<li><code>_post_cad_render_job</code> builds the spec via <code>_spec_for_bridge</code>
   (template-default driven), reads <code>freecad_bridge.url</code> and
   <code>freecad_bridge.secret</code> from ir.config_parameter, POSTs JSON
   to <code>&lt;url&gt;/render</code> with the secret in <code>X-Bridge-Secret</code>, with
   a 10-second timeout.</li>
<li>On 2xx, the MO's <code>x_cad_status</code> writes to <code>rendering</code> and
   the chatter posts a "CAD render job posted to bridge"
   message. On non-2xx or <code>requests.RequestException</code>, the MO
   writes to <code>error</code> and the chatter posts the failure detail.</li>
</ol>
<p>The bridge service runs the render asynchronously
(<code>BackgroundTasks</code> in FastAPI). When the render completes:</p>
<ol start="5">
<li>The bridge's background task uploads each output file as
   an <code>ir.attachment</code> via XML-RPC.</li>
<li>The bridge POSTs <code>/plm/cad_callback</code> on Odoo with
   <code>{production_id, status, attachment_ids}</code> and the
   <code>X-Bridge-Secret</code> header.</li>
<li>The <code>FreecadBridgeController.cad_callback</code> route validates
   the secret, parses the JSON, writes <code>x_cad_status = "done"</code>
   and <code>x_cad_attachment_ids = [(6, 0, [...])]</code> on the MO.</li>
<li>The endpoint is idempotent — the bridge can replay the
   callback safely and the MO converges on the same state. The
   attachments themselves are already in Odoo before the
   callback fires; the callback's only job is linking them.</li>
</ol>
<p>The geometric source of truth is <code>southbrook_dims</code> — a Python
module shared between Odoo and the bridge via a <code>/srv/shared</code>
mount and <code>PYTHONPATH</code>. Odoo's G1 test asserts the panel
formulas the configurator's <code>create_get_bom</code> produces match
<code>southbrook_dims.panel_cut_list(...)</code>. The bridge's
<code>render_cabinet.py</code> imports the same <code>panel_cut_list</code>. By
construction, the BoM's panels and the rendered geometry's
panels are computed by the same code — there's no drift, as
long as G1 stays green.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> An MO is sitting at CAD Status = Pending an hour after
confirm. You check the chatter — no error messages, no "CAD
render job posted" line either. What's the most likely cause,
and what's your next step?</p>
<blockquote>
<p>The <code>freecad_bridge.enabled</code> system parameter is off, so the
on-confirm hook silently no-op'd the POST. Pending is the
default state on a fresh MO and only flips to Rendering once
a POST succeeds. Confirm with sysadmin (Settings → Technical
→ System Parameters) — if the bridge is supposed to be on,
flip the gate to true; otherwise, accept that this MO will
never auto-render and click Regenerate CAD if you want it.</p>
</blockquote>
<p><strong>2.</strong> A customer's quote says hinge_side=R for a base cabinet,
but the rendering on the MO is showing the hinge on the left.
You confirm the MO's product template has <code>x_default_hinge_side
= "L"</code>. Should you regenerate CAD? Raise an ECO?</p>
<blockquote>
<p>Neither — yet. The Phase-1 bridge reads the template default,
not the configurator's per-MO spec, so the rendering is
correctly showing the template default even though the MO is
intended for the right-hinged variant. Regenerating produces
the same wrong rendering. The Module-2 fix swaps in the
configurator spec; until that lands, the workaround is to
mark the rendering as a stock-master visualisation and trust
the dimensioned shop drawing for the floor. Raising an ECO
wouldn't fix it either — the BoM and the cabinet's intent
are both correct; the limitation is in the bridge spec
mapping. File this as a Module-2 punch-list item, not as a
daily fire.</p>
</blockquote>
<p><strong>3.</strong> You see five MOs all at CAD Status = Error, all
confirmed in the last ten minutes, all with the chatter
message "Bridge HTTP 500: freecadcmd_exit_134". What
investigation path do you take?</p>
<blockquote>
<p>A common-cause failure across five recent MOs points at the
bridge service itself rather than per-MO geometry — exit
134 is SIGABRT, FreeCAD aborting at startup. Possibilities:
the bridge container OOM'd and restarted with a broken venv;
a new <code>.FCStd</code> master was uploaded with corrupted bytes; the
xvfb display server failed. Don't keep clicking Regenerate —
that adds load. Page sysadmin to inspect the bridge logs
(<code>docker logs southbrook-freecad-bridge</code>) and the
<code>/health</code> endpoint. Once the service is healthy again,
Regenerate the five MOs.</p>
</blockquote>
<p><strong>4.</strong> A BoM ECO applied at 14:00 bumped a panel formula. You
have two MOs of that template: MO/1234 was confirmed at 10:00
this morning, MO/1235 will be confirmed at 16:00. Should you
Regenerate CAD on either, and why?</p>
<blockquote>
<p>Regenerate neither — both for the same reason. MO/1234's
<code>southbrook_bom_version</code> snapshot captured the OLD version
at 10:00 confirm. Its existing CAD artifacts were rendered
against the old version and match the panels currently
being cut. Regenerating would render against the new
template, producing artifacts that DON'T match the actual
material on the floor — exactly the wrong thing. MO/1235
at 16:00 hasn't been confirmed yet; its CAD will render
automatically on confirm against the new version. The
bridge correctly handles both without manual intervention.</p>
</blockquote>
<p><strong>5.</strong> The customer service team is asking why their copy of
the rendering shows different door colours than what the
customer ordered. You open the MO's CAD Artifacts and confirm
the SVG matches the cabinet's geometry but the customer's
saved PDF has different colours. Where's the source of the
mismatch?</p>
<blockquote>
<p>The bridge's <code>/render</code> endpoint produces geometric shop
drawings — uncoloured DXF/SVG/PDF/STEP. Door colour, finish,
grain are visualisation concerns rendered by the
customer-facing configurator (Course 5), not by FreeCAD.
The PDF the customer saved was a configurator screenshot
export, not a bridge artifact. They're two different
rendering pipelines that share the same parametric
dimensions but different visualisation surfaces. Resolve by
pointing CS at the configurator-export path; the MO's CAD
Artifacts are correct for what they are.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>Raising an ECO when the bridge's rendering reveals a wrong
  BoM — lesson 4.1.</li>
<li>Authoring or revising a cut spec whose constants drive the
  panel geometry the bridge renders — lesson 4.2.</li>
<li>The AI-design Gemini room analysis that precedes the
  cabinet selection — lesson 4.4.</li>
<li>The dealer-portal installation-PDF export that uses
  <code>/render_elevation</code> synchronously — Course 6 (dealer portal).</li>
<li>Bridge service ops (container restarts, log inspection,
  master uploads) — Course 7 (sysadmin).</li>
<li>Native Odoo MO mechanics — Odoo's own training.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_04_ai_design" model="slide.slide">
            <field name="name">AI Design Assist — When to Use Gemini Room Analysis, When Not To</field>
            <field name="channel_id" ref="course_plm_design"/>
            <field name="slide_category">article</field>
            <field name="sequence">40</field>
            <field name="description">Designer working with a customer&#x27;s kitchen photo to seed the configurator&#x27;s room layout.</field>
            <field name="html_content"><![CDATA[<h1 id="ai-design-assist-when-to-use-gemini-room-analysis-when-not-to">AI Design Assist — When to Use Gemini Room Analysis, When Not To</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the designer who works the kitchen project from photo to
configurator input. A customer sends a photo of their existing
kitchen, you upload it to the project, Gemini does a room reading,
and <strong>you</strong> confirm every single dimensional field before the
configuration engine will touch it. The whole point of the contract
is that <em>Gemini guesses, you sign off</em>. Get that boundary wrong and
manufacturing will cut cabinets to a number Gemini hallucinated. This
lesson is the practical reality of when AI assist saves you a half
hour and when using it actively makes the job worse.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>Sign in at <strong>southbrookcabinetry.space/odoo</strong>, then:</p>
<blockquote>
<p><strong>Kitchen Workspace → Projects → [pick a project]</strong></p>
</blockquote>
<p>The project form is where you upload the photo and trigger
analysis. The OWL action button to call <code>analyze_photo()</code> lands
when the Phase-2 workspace UI ships; until then, the call is
invoked programmatically from the project form via a server
action. The analysis record itself lands on the project as
<code>ai_analysis_id</code> (m2o to <code>sb.kitchen.ai.analysis</code>) and the
detected appliances land as <code>appliance_ids</code> (one2many to
<code>sb.kitchen.appliance</code>).</p>
<p>Admins managing the AI surface also see:</p>
<blockquote>
<p><strong>Kitchen Workspace → AI Design → Prompt Templates</strong></p>
</blockquote>
<p>This is the versioned prompt-template store
(<code>sb.gemini.prompt.template</code>). Bumping a prompt revision doesn't
require a code deploy — the prompt body, model name, and
generation config are all data.</p>
<p>For admins inspecting the live integration:</p>
<blockquote>
<p><strong>Settings → Technical → System Parameters</strong></p>
</blockquote>
<ul>
<li><code>gemini.use_mock</code> — defaults to <code>True</code> so the addon installs
  without an API key and tests run offline. Set to <code>False</code> in
  production to call the real Gemini API.</li>
<li><code>gemini.api_key</code> — Vertex AI service-account-derived token (or
  AI Studio key for dev). Never echoed in logs.</li>
</ul>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>Once an analysis has landed, the project form's AI section shows:</p>
<p><strong><code>sb.kitchen.ai.analysis</code> record (linked via <code>ai_analysis_id</code>):</strong></p>
<ul>
<li><strong>Confirmed by Human</strong> (<code>confirmed_by_human</code>, Boolean, tracking)
  — the gate. Defaults to <strong>False</strong> on creation. Until you flip
  it to True via the Confirm button, <code>is_ready_for_config_engine()</code>
  returns False and Module 7 (the configuration engine) refuses
  to run.</li>
<li><strong>Confirmed by</strong> (<code>confirmed_by_user_id</code>, m2o <code>res.users</code>,
  readonly) — stamped when you click Confirm.</li>
<li><strong>Confirmed at</strong> (<code>confirmed_at</code>, Datetime, readonly) — stamped
  alongside the user.</li>
<li><strong>Raw Response (JSON)</strong> (<code>raw_response_json</code>, Text) — exactly
  what Gemini returned. Kept for audit and reprocessing. Don't
  edit by hand.</li>
<li><strong>Sink Detected</strong> (<code>sink_detected</code>, Boolean) — Gemini's best
  guess about whether the photo shows a sink. Read carefully:
  this is <em>detected</em>, not <em>confirmed</em>. If the photo shows a
  corner stovetop and Gemini called it a sink, this checkbox is
  wrong and you uncheck it before confirming the analysis.</li>
<li><strong>Window Count</strong> (<code>window_count</code>, Integer) — number of windows
  in the room.</li>
<li><strong>Room Door Count</strong> (<code>room_door_count</code>, Integer) — doors INTO
  the room, NOT cabinet doors. The label exists because new
  designers mistake one for the other every time.</li>
<li><strong>Floor Area (m²)</strong> (<code>floor_area_m2_approx</code>, Float) — Gemini's
  approximation. Six-digit precision is a UI accident, not a
  measurement claim.</li>
<li><strong>Ceiling Height (mm)</strong> (<code>ceiling_height_mm_approx</code>, Float) —
  the field Gemini is <em>worst</em> at; ceiling height is hard to
  triangulate from a single photo.</li>
<li><strong>Detected Appliances (JSON)</strong> (<code>detected_appliances_json</code>,
  Text) — the raw appliance list from Gemini, kept alongside the
  materialised <code>sb.kitchen.appliance</code> records for audit.</li>
<li><strong>Detected Dimensions Confidence (JSON)</strong>
  (<code>detected_dimensions_json</code>, Text) — Gemini's per-category
  confidence (<code>wall_lengths</code>, <code>appliance_widths</code>,
  <code>ceiling_height</code>), 0..1 floats.</li>
</ul>
<p><strong>Per-appliance form (<code>sb.kitchen.appliance</code>):</strong></p>
<ul>
<li><strong>Name</strong> (<code>name</code>, Char) — Gemini's free-text caption ("Gas
  range, 30\"" / "Single-basin sink").</li>
<li><strong>Type</strong> (<code>appliance_type</code>, Selection from <code>APPLIANCE_TYPES</code>:
  <code>stove</code> / <code>fridge</code> / <code>dishwasher</code> / <code>sink</code> / <code>microwave</code> /
  <code>oven_wall</code> / <code>hood</code> / <code>other</code>).</li>
<li><strong>Width (mm)</strong> (<code>width_mm</code>), <strong>Height (mm)</strong> (<code>height_mm</code>),
  <strong>Depth (mm)</strong> (<code>depth_mm</code>) — Gemini's approximations.
  Detected, not confirmed.</li>
<li><strong>Required Clearance (mm)</strong> (<code>requires_clearance_mm</code>, Integer)
  — minimum gap to adjacent cabinets. Stove/dishwasher typically
  30, fridge 50, sink 0.</li>
<li><strong>Position X / Y</strong> (<code>position_x</code>, <code>position_y</code>, Float 0..1) —
  relative position along the wall and depth axis.</li>
<li><strong>Confirmed by Human</strong> (<code>confirmed_by_human</code>, Boolean) — same
  gate as the analysis. Per-appliance, individually flipped.</li>
</ul>
<h2 id="the-detected-vs-confirmed-contract-g3-the-critical-distinction">The detected-vs-confirmed contract (G3, the critical distinction)</h2>
<p>The whole G3 contract exists to keep two failure modes off the
shop floor:</p>
<p><strong>Failure mode 1 — Manufacturing trusts a Gemini number.</strong>
Cabinets cut to a width Gemini estimated; the customer's kitchen
turns out to be 30 mm narrower; panels don't fit. <strong>Forbidden by
the contract.</strong> Every dimensional field carries
<code>confirmed_by_human</code> and the Configuration Engine refuses to run
while any required dimension is unconfirmed
(<code>is_ready_for_config_engine()</code> returns False).</p>
<p><strong>Failure mode 2 — Hallucinated fixtures.</strong>
Gemini reports <code>sink_detected: true</code> when the photo shows a
corner stovetop. Or it invents an appliance the photo doesn't
show. <strong>Detected by the contract</strong> because every appliance must
be designer-confirmed AND any unknown <code>kind</code> is coerced to
<code>other</code> with the original label preserved in notes. A confirmed
phantom is your fault; an unconfirmed one stays caged.</p>
<p>The vocabulary is precise:</p>
<ul>
<li><strong>Detected</strong> — the value Gemini emitted. Stored as-is in the
  *_approx fields (<code>width_mm_approx</code>, <code>ceiling_height_mm_approx</code>)
  AND in the corresponding non-approx fields on the appliance
  records. Detected is "the model said this," not "this is
  true."</li>
<li><strong>Confirmed</strong> — the designer has reviewed the value, possibly
  edited it to match a measured tape reading, and clicked the
  Confirm button. <code>confirmed_by_human=True</code> plus
  <code>confirmed_by_user_id</code> and <code>confirmed_at</code> stamps.</li>
</ul>
<p>Modules 7+ (the configuration engine, the cutlist generator)
read only confirmed values. Detected values exist on the record
for audit and for the designer's reference; they are NEVER
shipped to manufacturing-side computations.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p><strong>1. Decide whether to use AI assist at all.</strong></p>
<p>Use it when:</p>
<ul>
<li>The customer sent a single, well-lit, wide-angle photo of
  their existing kitchen.</li>
<li>You've never visited the site and have no measurements.</li>
<li>The photo shows at least two walls so the appliance positions
  along the run are inferable.</li>
<li>The customer hasn't already supplied a dimensioned
  hand-sketch.</li>
</ul>
<p>Do NOT use it when:</p>
<ul>
<li>The photo is low-light / heavily filtered / dim. Gemini's
  dimension confidence will be &lt; 0.4 across the board and every
  reading needs replacing — you've just made work for yourself.</li>
<li>The photo is a partial view (one wall, no context). Gemini
  will invent the other walls; you'll spend longer un-inventing
  them than measuring from scratch.</li>
<li>The customer already provided measurements. Trust the
  customer's tape reading over Gemini's pixel-counted estimate.
  Skip the analysis entirely and enter the measurements
  directly.</li>
<li>The kitchen is empty / under construction with no appliances
  installed. The appliance-position contract presumes installed
  appliances; an empty room produces an empty appliance list
  and you've gained nothing.</li>
<li>The photo shows people, pets, or significant clutter. The
  documented warning "Sink position partly occluded by a person
  — re-measure manually" is real — Gemini hits an occlusion and
  hedges.</li>
</ul>
<p><strong>2. Triggering the analysis (when you've decided yes).</strong></p>
<ul>
<li>Open the kitchen project.</li>
<li>Attach the photo as an <code>ir.attachment</code> on the project.</li>
<li>Call <code>analyze_photo(attachment_id)</code> — currently via a
  server-action button; the OWL UI button lands in Phase 2.</li>
<li>The call dispatches to <code>southbrook.gemini.client.analyze()</code>
  which either calls the real Gemini API (if
  <code>gemini.use_mock=False</code> and <code>gemini.api_key</code> is set) or
  returns a canned mock payload.</li>
<li><code>consume_gemini_analysis(payload)</code> lands one
  <code>sb.kitchen.ai.analysis</code> record and one
  <code>sb.kitchen.appliance</code> per detected appliance.</li>
<li>Every newly-created record has <code>confirmed_by_human = False</code>.</li>
<li>The call is <strong>idempotent</strong> by <code>(project_id, image_hash)</code> —
  uploading the same photo twice returns the existing analysis
  record without creating duplicates. Safe to retry on
  transient errors.</li>
</ul>
<p><strong>3. The confirmation walk (this is the work).</strong></p>
<p>For each detected appliance:</p>
<ul>
<li>Open the appliance record.</li>
<li>Cross-check with the photo (and with the customer on a call
  if the appliance is critical):</li>
<li>Is the <strong>type</strong> correct? (Gemini sometimes calls a
    farmhouse sink an "other" because it doesn't match the
    common-shape priors.)</li>
<li>Are the <strong>dimensions</strong> sane? 762 mm for a 30" range is
    correct; 800 mm is a 31.5" range, which doesn't exist —
    Gemini has rounded oddly. Replace with the standard.</li>
<li>Is the <strong>position</strong> correct? The <code>position_pct_along_wall</code>
    is "fraction from left edge" — 0.62 means 62% of the way
    along the wall from the left. Check by eye against the
    photo.</li>
<li>Edit any wrong value. Edits write directly to the field;
  there's no review queue.</li>
<li>Click <strong>Confirm</strong> to set <code>confirmed_by_human = True</code> for
  this appliance.</li>
</ul>
<p>For the room analysis:</p>
<ul>
<li>Walk through <code>sink_detected</code>, <code>window_count</code>,
  <code>room_door_count</code>, <code>floor_area_m2_approx</code>,
  <code>ceiling_height_mm_approx</code>.</li>
<li>Especially scrutinise <code>ceiling_height_mm_approx</code> — it's the
  field Gemini is worst at (the documented contract confidence
  range for this category is 0.40, the lowest). If you don't
  have a measured ceiling height, leave the field blank and
  flag the project for a site visit rather than confirming a
  Gemini guess.</li>
<li>Click <strong>Confirm</strong> at the analysis level.</li>
</ul>
<p><strong>4. Releasing to the configuration engine.</strong></p>
<ul>
<li>Once every dimensional field is confirmed
  (<code>is_ready_for_config_engine()</code> returns True), the
  configurator can proceed.</li>
<li>The release is automatic — no separate "release" button; the
  next time someone runs the config engine on the project,
  the gate passes.</li>
</ul>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I confirmed the analysis without reviewing each appliance
individually, and the configurator placed cabinets around a
phantom dishwasher Gemini invented."</strong></p>
<p>Each appliance has its own <code>confirmed_by_human</code>. Confirming the
analysis record does NOT cascade to the appliances —
<code>is_ready_for_config_engine()</code> checks both:</p>
<pre><code>if not self.ai_analysis_id or not self.ai_analysis_id.confirmed_by_human:
    return False
if any(not a.confirmed_by_human for a in self.appliance_ids):
    return False
</code></pre>
<p>If your project somehow released to the engine, it means every
appliance also got confirmed. Recovery: open each phantom
appliance, click <strong>Unconfirm</strong> (resets <code>confirmed_by_human</code> and
the stamp fields), then delete it. Re-run the configurator. The
audit trail (mail.thread on the analysis) shows the unconfirm
and the deletion; an auditor can reconstruct what happened.</p>
<p><strong>"Gemini gave me an appliance with <code>kind='blender'</code>. The
configurator doesn't know what to do with it."</strong></p>
<p>The Gemini client's validator coerces unknown kinds to <code>other</code>
and stores the original label in the <code>notes</code> field on the
appliance. So you don't see a "blender" record — you see an
<code>other</code> record with the label "blender" in notes. Decide
whether the appliance is something the configurator cares about
(an integrated coffee station might be modelled as a built-in
appliance; a counter-top blender shouldn't be on the appliance
list at all) and either delete the record or re-type it
(<code>appliance_type = "other"</code> is the right answer for genuinely
"other," but for an oven_wall mis-classified as other you'd
flip the type to <code>oven_wall</code>).</p>
<p><strong>"The photo had a window and a back door visible on the same
wall; Gemini reported <code>room_door_count: 2</code> because it counted
the back door AND a cabinet door."</strong></p>
<p><code>room_door_count</code> means doors INTO the room (room/hall/exterior
entries) — NOT cabinet doors. Gemini conflates them often
because the contract description in the prompt isn't unambiguous
on this. Fix the value by editing the field directly before
confirming. The next prompt-template revision is likely to
disambiguate this — file an internal ticket for the prompt-eng
team if you see it more than once a week.</p>
<p><strong>"The customer's photo was a low-resolution snapshot in poor
lighting. The Gemini analysis came back with
<code>dimensions_confidence.wall_lengths = 0.18</code>. I confirmed it
anyway and the configurator's room layout is way off."</strong></p>
<p>Low confidence values are a signal to NOT confirm. The
contract's role for <code>dimensions_confidence</code> is exactly this —
to flag readings the designer should reject rather than confirm.
There's no UI gate that prevents you confirming a 0.18-confidence
field; it's a discipline issue. Recovery: unconfirm the
analysis (re-opens the gate), edit the wrong values to either
known-measured numbers or blank, re-confirm. If the values are
unknown and unmeasurable from the photo, flag the project for a
site visit — that's the honest path. <strong>Never confirm a field
because you want to move on.</strong></p>
<p><strong>"I uploaded the same photo twice and expected a fresh
analysis. Got back the same record from the first upload."</strong></p>
<p>That's the idempotency contract working as designed. <code>analyze_
photo</code> hashes the image bytes (<code>sha256:</code>) and dedups against
<code>(project_id, image_hash)</code>. Two uploads of the <em>same bytes</em> are
the same analysis. If you genuinely want to reprocess — say,
because the prompt template has been revised since the first
analysis — delete the previous analysis record first, OR call
<code>analyze_photo</code> with <code>prompt_template_code</code> set to the new
revision (the idempotency key is <code>image_hash</code>, not the
template, so this will currently still dedup; the design fix is
to fold the template code into the dedup key, which is a known
punch-list item).</p>
<p><strong>"The Gemini call returned <code>gemini_unavailable</code> and the
analysis didn't land."</strong></p>
<p>Network or timeout. The client retries with exponential backoff
(250 ms / 1 s / 4 s) per the G3 §6 contract — three retries
total. If all three failed, the network's down or Gemini is
having a bad day. Wait a few minutes and try again. If it
persists, set <code>gemini.use_mock=True</code> on the staging instance
and develop with the canned response; in production, contact
ops to check the API status. <strong>Do not</strong> hand-author an analysis
record to work around the outage — the dedup key would then
clash with the real analysis when the network came back.</p>
<h2 id="when-not-to-use-ai-assist-expanded">When NOT to use AI assist (expanded)</h2>
<p>The default should NOT be "always run Gemini on every photo."
The default should be "use Gemini when it's the fastest path to
a good designer hand-off, otherwise measure or ask." Specific
no-fly zones:</p>
<ol>
<li><strong>Low light or heavy filter.</strong> The validator clamps
   out-of-range dimensions and adds warnings, but it can't fix
   confidence. Gemini's confidence on a dim photo is too low to
   be useful and your time to fix every wrong reading exceeds
   the time you'd have spent measuring.</li>
<li><strong>Partial view.</strong> The contract's <code>wall_segments</code> list assumes
   Gemini can see at least two walls. A close-up of a single
   cabinet produces an analysis with one wall, no appliances,
   and a model_warnings entry. Skip.</li>
<li><strong>Customer supplied dimensions already.</strong> Trust the tape
   reading. Enter the measurements directly into the
   confirmed-side fields, leaving the detected side blank, and
   set <code>confirmed_by_human=True</code> on the analysis with a note
   citing the customer.</li>
<li><strong>Empty / under-construction room.</strong> No appliances to detect,
   no spatial context for Gemini to anchor wall lengths against.</li>
<li><strong>Photo shows people or significant clutter.</strong> Occlusion
   trips Gemini's "re-measure manually" warning; the time to
   work around it exceeds the time to take a fresh photo.</li>
<li><strong>Adversarial or non-kitchen scene.</strong> The schema validator
   will reject an analysis that came back with no plausible
   kitchen room shape, but the request still costs you a Gemini
   API call against your quota.</li>
</ol>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>When you call <code>analyze_photo(attachment_id)</code>:</p>
<ol>
<li>The handler reads <code>ir.attachment.raw</code> (or <code>datas</code>, for
   legacy attachments) on the attachment, extracts the bytes.</li>
<li>It dispatches to <code>southbrook.gemini.client.analyze(image_
   bytes, prompt_template_code)</code>.</li>
<li>The client checks <code>ir.config_parameter.gemini.use_mock</code>:
   if True, returns a canned payload from <code>_mock_response</code>;
   if False, calls <code>_call_gemini_real</code> (which lazily imports
   <code>httpx</code>, requires <code>gemini.api_key</code>).</li>
<li><code>_call_gemini_real</code> POSTs the image + prompt to
   <code>https://generativelanguage.googleapis.com/v1beta/models/&lt;model&gt;:generateContent?key=&lt;key&gt;</code>,
   with <code>temperature: 0.1</code> and a structured <code>responseSchema</code>
   matching the G3 §4 schema. Retries per §6: three exponential
   backoffs on transient failures; one retry on 429 quota; no
   retries on 401/403 auth (those surface as <code>gemini_auth_failed</code>
   immediately).</li>
<li>The returned JSON is parsed and passed to <code>_validate</code>, which
   enforces every G3 §4.1 rule: schema version literal must
   match <code>southbrook.gemini.room_analysis.v1</code>; out-of-range
   dimensions are nulled and a warning added; unknown appliance
   kinds are coerced to <code>other</code> with the original kind stored
   in <code>notes</code>; orphan <code>wall_segment_id</code> references rejected
   with <code>UserError("orphan_wall_segment_id")</code>; confidence
   values clamped to [0,1].</li>
<li>The image hash (<code>sha256:</code>) is stamped onto the validated
   payload.</li>
<li>Control returns to <code>sb.kitchen.project.consume_gemini_
   analysis(payload)</code>. The lander searches for an existing
   analysis with the same <code>image_hash</code> in its <code>raw_response_json</code>
   (substring search); if found, returns it (idempotency). If
   not, creates a new <code>sb.kitchen.ai.analysis</code> record AND one
   <code>sb.kitchen.appliance</code> per detected appliance, all with
   <code>confirmed_by_human=False</code>. Writes the analysis link to
   <code>project.ai_analysis_id</code>.</li>
</ol>
<p><code>is_ready_for_config_engine()</code> checks both
<code>ai_analysis_id.confirmed_by_human</code> AND every appliance's
<code>confirmed_by_human</code>. False until ALL of them are True; downstream
Module 7 refuses to run while False, per GAP-02.</p>
<p><code>raw_response_json</code> is the full Gemini response stored on the
analysis record, so any future schema versioning (v2, v3) can
re-process the original data without re-calling Gemini and
re-spending quota.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> A customer sends a wide-angle photo of their kitchen.
You run <code>analyze_photo</code>, get back six appliances, confirm five
of them, but skip a phantom dishwasher Gemini invented because
you intend to delete it. You click Confirm on the analysis
record (the room facts look right). Will the configurator now
proceed?</p>
<blockquote>
<p>No. <code>is_ready_for_config_engine()</code> checks every appliance's
<code>confirmed_by_human</code>, not just the analysis record's. The
phantom dishwasher's <code>confirmed_by_human</code> is still False, so
the gate stays closed. Fix: delete the phantom appliance
record (it'll come off the <code>appliance_ids</code> list), or click
Unconfirm on the analysis and revisit. Once every remaining
appliance is confirmed AND the analysis itself is confirmed,
the gate opens.</p>
</blockquote>
<p><strong>2.</strong> The Gemini response came back with <code>wall_lengths</code>
confidence 0.22 and <code>ceiling_height</code> confidence 0.31. You don't
have a tape measure handy and the customer is on the phone
asking you to "just run it." What's the right answer?</p>
<blockquote>
<p>Tell the customer you need measurements before
manufacturing can proceed. Low-confidence values mean Gemini
doesn't trust its own reading; if you confirm them anyway,
you've signed your name to a number that's wrong. Per the
G3 contract, the entire confirmation surface exists to make
"I signed off on Gemini's guess" your responsibility — not
the model's. Concrete action: edit the offending values to
blank, set the analysis-level confirm only when YOU have
the measurements (from a follow-up site visit or customer
sketch), and leave the project state at "Awaiting
Dimensions" until then.</p>
</blockquote>
<p><strong>3.</strong> Gemini's response included <code>appliances[0].kind =
"espresso_machine"</code>. You don't see this in the appliance_type
selection. What did the system actually do?</p>
<blockquote>
<p>The Gemini client's <code>_validate</code> method intercepts unknown
kinds: it coerces <code>kind</code> to <code>"other"</code> and appends the
original kind to the <code>notes</code> field (<code>notes = … +
"original_kind=espresso_machine"</code>). Plus it adds a warning
to <code>model_warnings</code>. So you see an <code>other</code>-type appliance
record with the original "espresso_machine" preserved in
notes for your reference. Your decision is whether the
appliance is relevant to cabinet placement (a fixed
built-in espresso machine probably is; a counter-top one
isn't) and edit accordingly.</p>
</blockquote>
<p><strong>4.</strong> You uploaded a photo, analysed it, did the confirm walk,
and released the project to the configurator. Two days later
the customer sends a higher-resolution version of the same
photo. You upload and re-analyse expecting a fresh analysis.
What happens?</p>
<blockquote>
<p>A NEW analysis lands — the new image has a different SHA-256
hash, so the idempotency dedup
(<code>raw_response_json ilike image_hash</code>) finds no match and a
new <code>sb.kitchen.ai.analysis</code> record is created on the same
project. Your project now has TWO analyses linked via the
mail.thread; <code>project.ai_analysis_id</code> will be overwritten
to point at the new one. Decision: do you re-confirm the
new analysis (and disregard the old confirmations) or stay
with the original? If the new image yielded different
values, you need to repeat the confirm walk; the old
confirmations are stale. The old record stays as audit
history.</p>
</blockquote>
<p><strong>5.</strong> A junior designer confirmed every appliance and the
analysis record after a 5-second glance at the photo. Two weeks
later the cabinets arrive at the customer's home 80 mm too
wide. Where's the blame, and what's the systemic fix?</p>
<blockquote>
<p>The blame is the designer's confirm click — that's literally
what the G3 contract is for. The system worked: Gemini gave
approximations; the contract gated them behind
<code>confirmed_by_human</code>; the gate fired (configurator only ran
after the click). The designer's signature is on the
dimensions. The systemic fix is not technical — it's
training and audit. Concretely: surface the appliance-level
confidence in the UI more prominently (so a 0.22 reading is
visually impossible to confirm casually), add an audit
report that flags analyses confirmed within N seconds of
creation as suspicious, and use the chatter trail (the
analysis inherits <code>mail.thread</code>, so the confirm event is
recorded with timestamp + user) to review confirmations
against the photo at the post-mortem. The contract makes
the bad confirm visible; it can't prevent the bad confirm.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The downstream configuration engine that reads the confirmed
  surface to lay out cabinets — Course 5 (configurator).</li>
<li>The PLM ECO workflow that revises BoMs and cut specs
  independent of any AI analysis — lessons 4.1 and 4.2.</li>
<li>The FreeCAD bridge that renders the resulting cabinets —
  lesson 4.3.</li>
<li>Prompt-template engineering (adding a new
  <code>sb.gemini.prompt.template</code> record) — admin workflow, separate
  doc (<code>docs/ai_prompt_spec.md</code>).</li>
<li>Vertex AI / Studio key management — Course 7 (sysadmin).</li>
<li>Native Odoo <code>mail.thread</code> mechanics — Odoo's own training.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
    </data>
</odoo>
