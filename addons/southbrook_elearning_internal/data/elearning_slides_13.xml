<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="slide_13_fabio_architecture" model="slide.slide">
            <field name="name">Fabio Architecture — The Four Pillars and How They Fit</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">10</field>
            <field name="description">Every Fabio user (Production Manager, CS lead, Estimator, Sysadmin) and every developer touching the addon.</field>
            <field name="html_content"><![CDATA[<h1 id="fabio-architecture-the-four-pillars-and-how-they-fit">Fabio Architecture — The Four Pillars and How They Fit</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're anyone who interacts with Fabio — the trade-partner-facing AI assistant
surface that Southbrook embeds inside Odoo. Maybe you click <strong>Approve</strong> on
recommendations (lesson 3.2). Maybe you're a dealer asking "where is my
kitchen?" from the Order Builder. Maybe you're a developer adding a thirteenth
tool. Before any of that goes anywhere useful, you need the same mental model
of <em>what Fabio actually is</em>.</p>
<p>The internal addon is called <code>southbrook_hermes</code>. The user-facing brand
everywhere — menus, partner record, the chat panel header — is <strong>Fabio</strong>. The
two names are the same thing. The spec uses "Hermes" (the platform), the UI
uses "Fabio" (the persona); this lesson uses whichever the surface you're
reading uses.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>Fabio exposes itself through three concrete surfaces; you reach each one a
different way.</p>
<blockquote>
<p><strong>Fabio → Recommendations</strong> — the in-Odoo approval queue (lesson 3.2).
Requires the <code>southbrook_hermes.group_hermes_reviewer</code> group.</p>
<p><strong>Fabio → Ask</strong> — the in-Odoo conversation log (<code>southbrook.hermes.question</code>
list), also reviewer-gated. Useful for reading prior turns.</p>
<p><strong>Order Builder portal → chat panel</strong> — the OWL chat panel that auto-mounts
on the customer's Order Builder page (<code>southbrook_estimating_website.portal_order_builder</code>).
Trade partners hit this; sysadmins don't. The panel mount HTML is injected
by <code>views/order_builder_chat_inject.xml</code> and the auto-mount is wired up in
<code>static/src/components/hermes_chat/hermes_chat.esm.js</code>.</p>
</blockquote>
<p>System-parameter wiring lives at:</p>
<blockquote>
<p><strong>Settings → Technical → System Parameters</strong> —
<code>southbrook_hermes.sidecar_url</code>, <code>southbrook_hermes.sidecar_enabled</code>,
<code>southbrook_hermes.jwt_secret</code>. These three knobs decide whether Fabio's
chat is live, stubbed, or off. (Detail: lesson 13.3 + lesson 13.7.)</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>The "four pillars" framing makes Fabio understandable in one diagram. Each
pillar is a concrete chunk of code; each interacts with the others through a
single, documented contract.</p>
<h3 id="pillar-1-the-recommendation-queue-southbrookhermesrecommendation">Pillar 1 — The recommendation queue (<code>southbrook.hermes.recommendation</code>)</h3>
<p>The human-approval boundary. Every business mutation Fabio proposes — request
a revision, reschedule an install, raise a clarification — lands here as a
<em>draft</em> row that a human reviewer must approve and apply before the database
changes. Model lives in <code>models/hermes_recommendation.py</code>. State machine:
<code>draft → ready → approved → applied</code> with a sideways <code>rejected</code> exit. The
queue is what makes Fabio safe to deploy at all — a hallucinating agent
without an approval boundary would be a liability. Full depth: lesson 13.2.</p>
<h3 id="pillar-2-jwt-auth-persona-resolution-utilsjwt_helperpy">Pillar 2 — JWT auth + persona resolution (<code>utils/jwt_helper.py</code>)</h3>
<p>Every Fabio call lives inside a short-lived HS256 JWT. The token carries
four claims: <code>tenant</code>, <code>persona</code>, <code>partner_id</code>, and <code>tier</code>. The minting +
verification helpers + the <code>resolve_persona(user)</code> function are in
<code>utils/jwt_helper.py</code>. Persona is derived from <code>res.users.share</code> plus group
membership — <em>not</em> from anything the browser sends. The tier is derived from
the persona, again server-side. This is the boundary that means a malicious
prompt or a leaked token can't escalate. Full depth: lesson 13.3.</p>
<h3 id="pillar-3-the-tool-registry-hermes_tool-tool_registry">Pillar 3 — The tool registry (<code>@hermes_tool</code> + <code>TOOL_REGISTRY</code>)</h3>
<p>Thirteen tools at v1.0 — 9 read + 4 write. Each is a plain Python function
decorated with <code>@hermes_tool(slug, personas, tier, scope, description)</code>.
The decorator (<code>tools/decorator.py</code>) registers the function into a
process-level <code>TOOL_REGISTRY</code> list at import time. The sidecar fetches the
registry from <code>GET /api/hermes/tools</code> and dispatches individual calls via
<code>POST /api/hermes/tools/&lt;slug&gt;</code> — both routes in <code>controllers/hermes_tools_api.py</code>.
Persona × tier filtering happens at registry time AND at dispatch time;
record-rule scoping happens inside the tool, via
<code>request.env(user=portal_user)</code>. Full depth: lessons 13.4, 13.5, 13.6.</p>
<h3 id="pillar-4-the-chat-panel-vercel-sidecar-owl-nextjs">Pillar 4 — The chat panel + Vercel sidecar (OWL + Next.js)</h3>
<p>The OWL component (<code>HermesChat</code> in
<code>static/src/components/hermes_chat/hermes_chat.esm.js</code>) is what the trade
partner sees. It POSTs to <code>/hermes/v1/ask</code> (controller in
<code>controllers/hermes_proxy.py</code>), which mints a JWT and then either (a) calls a
configured Vercel sidecar over HTTP and streams the response back to the
browser, or (b) returns a JSON stub when <code>sidecar_enabled</code> is <code>false</code>. The
sidecar (in <code>sidecar/</code> at the repo root) does the LLM call + RAG retrieval +
tool dispatch + conversation logging. The Odoo side never talks to the LLM
directly. Full depth: lesson 13.7.</p>
<h3 id="what-southbrook_os-provides-the-foundation-fabio-grounds-in">What <code>southbrook_os</code> provides (the foundation Fabio grounds in)</h3>
<p><code>southbrook_os</code> is a separate addon (manifest in
<code>addons/southbrook_os/__manifest__.py</code>, depends: <code>base, mail, product, mrp,
southbrook_estimating</code>) that ships the Southbrook business knowledge as
markdown sections. Ten <strong>canonical</strong> files under <code>addons/southbrook_os/canonical/</code>
(<code>00_charter.md</code>, <code>01_company.md</code>, … <code>99_glossary.md</code>); four <strong>generated</strong>
files that the nightly cron rebuilds from live Odoo state (catalog,
attributes, cut spec, work centers). All sections surface through the
<code>southbrook.os.section</code> model and are exposed publicly as JSON at
<code>GET /southbrook/os.json</code>. The sidecar's RAG index is built from that JSON
at deploy time. Without an OS, Fabio has nothing to ground in; <code>southbrook_hermes</code>
hard-depends on <code>southbrook_os</code> for exactly this reason.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p>You don't run Fabio's architecture — you use one of its three surfaces. The
flow you follow depends on your role.</p>
<p><strong>1. Trade partner (the originating persona of a Fabio chat):</strong></p>
<ul>
<li>Open your order in the portal Order Builder.</li>
<li>Find the chat panel below the order header.</li>
<li>Type your question. The panel POSTs to <code>/hermes/v1/ask</code>, your JWT is
  minted in-place, your persona is resolved as <code>trade_partner</code> because
  <code>env.user.share == True</code>, the question goes to the sidecar (or stub),
  the answer streams back into the panel.</li>
<li>If the answer creates a draft recommendation
  (<code>propose_recommendation</code> was called by the model), the chat tells you
  so. The recommendation is now in Fabio's queue for a Southbrook
  reviewer.</li>
</ul>
<p><strong>2. Production manager / CS lead (the human approval side):</strong></p>
<ul>
<li>Open <strong>Fabio → Recommendations</strong>, default <code>Open</code> filter applied (state
  in draft/ready/approved).</li>
<li>Read the recommendation, decide, approve/reject/apply. Lesson 3.2
  walks the queue end-to-end. Lesson 13.2 goes one level deeper into the
  state machine, the audit fields, the <code>_create_project_task</code> mechanic,
  and the security group that gates the menu.</li>
</ul>
<p><strong>3. Developer (adding or changing a tool):</strong></p>
<ul>
<li>Read the spec (§ 6 catalog, § 4 ACL model).</li>
<li>Read lessons 13.4 (registry) + 13.5 (read tools) + 13.6 (write tools +
  tier model).</li>
<li>Add the function in <code>tools/read_tools.py</code> or <code>tools/write_tools.py</code>,
  decorate with <code>@hermes_tool</code>, decide personas + tier + scope honestly.</li>
<li>For T2 tools, route through <code>propose_recommendation</code> — do NOT write
  business state directly.</li>
<li>Update the OS section that documents the concept the tool exercises
  (the <code>test_os_coverage.py</code> CI gate enforces this).</li>
<li>Smoke-test with <code>scripts/smoke_hermes.sh</code>.</li>
</ul>
<p><strong>4. Sysadmin (the platform owner):</strong></p>
<ul>
<li>Settings → Technical → System Parameters. The three relevant keys
  decide Fabio's state: <code>sidecar_url</code> (where), <code>sidecar_enabled</code>
  (whether), <code>jwt_secret</code> (signed by what). Lesson 13.3 covers
  rotation policy.</li>
</ul>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I deployed <code>southbrook_hermes</code> but the Fabio menu doesn't show up."</strong></p>
<p>The menu is gated by <code>southbrook_hermes.group_hermes_reviewer</code>. Your user
needs to be in that group. Note from <code>security/hermes_security.xml</code>:
<code>base.group_system</code> (Settings → Users → Internal User → Administration:
Settings) inherits the reviewer group, so any sysadmin sees it for free.
Production managers need to be added explicitly. Trade partners are share
users and never see this menu — that's correct.</p>
<p><strong>"The chat panel says '(Stub answer — sidecar not yet wired)'."</strong></p>
<p>That's not broken. That's the design. The proxy returns a stub answer when
<code>southbrook_hermes.sidecar_enabled</code> is unset/false OR <code>sidecar_url</code> is empty
(see <code>controllers/hermes_proxy.py</code> lines around the <code>_is_truthy(... sidecar_enabled)</code>
check). The stub still exercises JWT mint + verify so you know the auth
path works. Flip <code>sidecar_enabled</code> to <code>true</code> after the Vercel deploy
lands. See lesson 13.7 for the deploy.</p>
<p><strong>"I added a new tool, it's in <code>tools/</code>, decorated with <code>@hermes_tool</code>, but
the sidecar's tool list doesn't have it."</strong></p>
<p>The sidecar caches the registry at cold start. <code>lib/tools.ts</code> keys the
cache by <code>tenant::persona::tier</code> (post-fix per spec § 14.8). A Vercel
redeploy invalidates it; an addon upgrade alone doesn't. If you want the
sidecar to pick up a new tool, redeploy: <code>vercel deploy --prod</code> (see
lesson 13.7).</p>
<p><strong>"The recommendation queue is empty but the chat says 'I've queued a
draft.'"</strong></p>
<p>Check what user the chat is authenticated as. The
<code>controllers/hermes_tools_api.py</code> dispatch creates the recommendation in
<code>sudo()</code> mode (explicit § 4.4 carve-out: a draft recommendation is owned
by Fabio, not the requesting partner). It's visible to reviewers via the
<code>group_hermes_reviewer</code> group, not to the partner who proposed it. The
trade-partner UI shows nothing; the reviewer UI shows the row in <code>draft</code>
state. That's working as designed.</p>
<p><strong>"I want to read what Fabio is grounded in but the OS sections are out of
date."</strong></p>
<p>Generators rebuild the four <code>*.generated.md</code> sections nightly (see
<code>addons/southbrook_os/data/ir_cron.xml</code>). For manual refresh, the spec
section § 5.4 prescribes a Settings button. Hit <code>/southbrook/os.json</code> to
see what's published <em>right now</em> — the response cache is 5 minutes
(<code>Cache-Control: public, max-age=300</code> in
<code>addons/southbrook_os/controllers/os_public.py</code>). The sidecar's RAG corpus
is a build-time snapshot of that JSON; a fresh corpus requires a sidecar
redeploy. Trade partners can't trigger this; sysadmins can.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>The four pillars wire together like this on a single trade-partner turn:</p>
<ol>
<li>The OWL chat panel POSTs <code>{q, order_id}</code> to <code>/hermes/v1/ask</code>.</li>
<li><code>HermesProxyController.ask</code> calls <code>jwt_helper.resolve_persona(request.env.user)</code>.
   For a share user this returns <code>"trade_partner"</code>. For a non-share user with
   the salesman group, <code>"sales_rep"</code>. For a non-share user with
   <code>southbrook_kitchen_workspace.group_kitchen_ops</code>, <code>"mfg_manager"</code>.
   Otherwise <code>AccessError</code>.</li>
<li>Tier is derived: <code>tier_for_persona(persona)</code> returns <code>"T0+T1+T2"</code> for
   all three personas (per the § 14.1 implementation delta — the tier mask
   was widened to T0+T1+T2 because <code>propose_recommendation</code> is the
   "universal T2 escape hatch" and the trade-partner safety boundary
   moved <em>into</em> the tool itself via <code>_TRADE_PARTNER_INTENTS</code>).</li>
<li>JWT is minted by <code>jwt_helper.mint_jwt</code> with <code>ttl=120s</code> (widened from the
   spec's 60s default per delta § 14.7 to cover multi-step agent loops).</li>
<li>If <code>sidecar_enabled</code> is true, the proxy POSTs to <code>&lt;sidecar_url&gt;/api/hermes/ask</code>
   with <code>Authorization: Bearer &lt;jwt&gt;</code>. The sidecar verifies the JWT
   (<code>lib/jwt.ts</code>), retrieves top-K RAG hits from <code>data/index/southbrook/</code>,
   fetches the tool registry from <code>GET /api/hermes/tools</code> (cached by
   tenant::persona::tier), and runs an OpenAI / Gemini call through the
   Vercel AI Gateway.</li>
<li>The LLM may call tools. Each tool call goes back to Odoo at
   <code>POST /api/hermes/tools/&lt;slug&gt;</code> with the same JWT. Dispatch
   (<code>hermes_tools_api.dispatch</code>) enforces persona membership + tier mask
   + record-rule scope (<code>env(user=portal_user)</code>). Claim-bound args
   (<code>persona</code>, <code>partner_id</code>, <code>tenant</code>) are overridden from JWT claims
   before the tool is called (delta § 14.2 — closes the prompt-injection
   self-attribution hole).</li>
<li>The streamed answer flows back through <code>/hermes/v1/ask</code> to the OWL
   panel.</li>
<li>After the turn, the sidecar fire-and-forgets a
   <code>POST /api/hermes/conversation/log</code> with <code>{question, answer, order_id, …}</code>,
   which <code>controllers/hermes_conversation_api.py</code> writes as a
   <code>southbrook.hermes.question</code> record. The <code>order_id</code> is stored on the
   <code>sale_order_id</code> field (delta § 14.6).</li>
</ol>
<p>If <code>sidecar_enabled</code> is false (the default after install), step 5–8
collapses to a JSON stub from the proxy. The OWL panel still rendered the
panel, the JWT was still minted + verified, you have proof the platform
is wired correctly before the sidecar is deployed.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> You SSH into the QNAP, install <code>southbrook_hermes</code> for the first
time, and a trade partner immediately opens their Order Builder. What
does the chat panel display when they ask "where is my kitchen?"</p>
<blockquote>
<p>A JSON stub answer of the form
<code>"(Stub answer — sidecar not yet wired.) Question received: 'where is my
kitchen?'."</code>. <code>southbrook_hermes.sidecar_enabled</code> defaults to <code>false</code>
at install (see <code>data/ir_config_parameter.xml</code>), so the proxy short-
circuits to a stub. JWT mint+verify still ran, so you have confirmation
the auth path works.</p>
</blockquote>
<p><strong>2.</strong> A developer adds a new tool <code>get_order_invoices</code> decorated
<code>@hermes_tool(personas=["trade_partner"], tier="T0", scope="own_order")</code>,
ships the addon, restarts the Odoo container — but a trade partner asking
about invoices gets "I don't have a tool for that." What's most likely
wrong?</p>
<blockquote>
<p>The sidecar caches the tool registry at cold start (<code>lib/tools.ts</code>), and
an addon upgrade alone doesn't invalidate that cache. The fix is a
Vercel redeploy of the sidecar: <code>vercel deploy --prod</code>. The new tool
will then be in the registry returned by <code>GET /api/hermes/tools</code> on the
next cold start.</p>
</blockquote>
<p><strong>3.</strong> Why does <code>southbrook_hermes</code> hard-depend on <code>southbrook_os</code> in
its manifest (<code>depends = [..., "southbrook_os"]</code>)?</p>
<blockquote>
<p>Because Fabio is grounded in the OS. Without canonical knowledge to
ground in — Southbrook's customer model, catalog, lifecycle, partner
FAQ — the LLM has nothing but its own pretraining, which is the
hallucination path the spec § 11 calls out as a v1.0 non-goal. The
dependency forces the OS to install first; the RAG corpus is built
from <code>/southbrook/os.json</code> which is <code>southbrook_os</code>'s endpoint.</p>
</blockquote>
<p><strong>4.</strong> Trade partner Fred chats Fabio: "actually reschedule my install
for next Tuesday." The LLM emits a <code>propose_recommendation</code> tool call.
Where does the change to Fred's install date happen?</p>
<blockquote>
<p>Nowhere — yet. <code>propose_recommendation</code> creates a <em>draft</em>
<code>southbrook.hermes.recommendation</code> row (in <code>sudo()</code>, owned by the Fabio
agent partner). A Southbrook reviewer in <code>group_hermes_reviewer</code> sees
the row at <strong>Fabio → Recommendations</strong>, reads it, and clicks <strong>Approve</strong>
→ <strong>Apply</strong> to actually act on the request. Trade partners cannot move
recommendation state. This is pillar 1's whole point.</p>
</blockquote>
<p><strong>5.</strong> A sysadmin asks "is Fabio's chat panel hitting the sidecar right
now?" You don't have shell access, only the Odoo backend. What do you
check?</p>
<blockquote>
<p>Settings → Technical → System Parameters →
<code>southbrook_hermes.sidecar_enabled</code>. If it's <code>false</code>, the proxy stubs;
live path is off. If it's <code>true</code>, also check <code>southbrook_hermes.sidecar_url</code>
is non-empty — both conditions must hold for the proxy to actually
call out. Cross-check by opening the chat panel and asking a question:
the stub answer begins <code>"(Stub answer — sidecar not yet wired.)"</code>, so
if you see that prefix you're in stub mode regardless of the parameter
state.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The recommendation queue's state machine in source-level detail —
  lesson 13.2.</li>
<li>JWT mint + verify internals and persona resolution — lesson 13.3.</li>
<li>The <code>@hermes_tool</code> decorator, the registry endpoint, and dispatch ACL
  layering — lesson 13.4.</li>
<li>The 9 read tools one by one — lesson 13.5.</li>
<li>The 4 write tools and the T0/T1/T2 tier model — lesson 13.6.</li>
<li>The OWL chat panel + Vercel sidecar streaming wire format — lesson 13.7.</li>
<li>Approving recommendations (the floor manager's daily flow) —
  lesson 3.2 (<code>03_hermes_fabio_approval.md</code>).</li>
<li>The CS-side view of the queue — lesson 6.3 (<code>06_hermes_cs_view.md</code>).</li>
<li>Sysadmin recommendations (separate operator surface at hermes.odooiq.com)
  — lesson 7.3 (<code>07_hermes_sysadmin.md</code>).</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_recommendation_queue_deep" model="slide.slide">
            <field name="name">Recommendation Queue — Source-Level Deep Dive</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">20</field>
            <field name="description">Production Manager, CS Lead, and any developer reviewing the approval-boundary code.</field>
            <field name="html_content"><![CDATA[<h1 id="recommendation-queue-source-level-deep-dive">Recommendation Queue — Source-Level Deep Dive</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're a production manager or CS lead who already runs the queue daily
(lesson 3.2) and now needs to understand the model in full so you can
diagnose unusual states, train a peer, or partner with a developer to add
a new recommendation type. Or you're a developer who's about to touch
<code>models/hermes_recommendation.py</code> and wants to know what every field is
for before you change anything.</p>
<p>This lesson is the queue, end to end, at the source level. Lesson 3.2
covers the daily flow; this lesson covers <em>why each field exists</em>, <em>what
each method does</em>, and <em>what side effects every state transition fires</em>.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>Same surface as lesson 3.2:</p>
<blockquote>
<p><strong>Fabio → Recommendations</strong> — the list view, default <code>Open</code> filter.</p>
</blockquote>
<p>But also, for source-level work:</p>
<blockquote>
<p><strong>Settings → Technical → Database Structure → Models</strong> — search for
<code>southbrook.hermes.recommendation</code>, open it to see the field list, the
attached rules, the inherited mixins. Useful when an auditor asks "is
there a record of this?" and you need to point at the schema.</p>
<p><strong>Settings → Technical → Security → Groups</strong> — search for
<code>Fabio Reviewer</code> (<code>southbrook_hermes.group_hermes_reviewer</code>). This is
the gate on the entire queue.</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<h3 id="the-full-field-list-southbrookhermesrecommendation">The full field list (<code>southbrook.hermes.recommendation</code>)</h3>
<p>The model lives at <code>addons/southbrook_hermes/models/hermes_recommendation.py</code>
and inherits <code>mail.thread</code> + <code>mail.activity.mixin</code> so it gets a chatter and
activity widget for free. Every field that ships, in the order they're
declared:</p>
<ul>
<li><strong><code>name</code></strong> (Char, required, tracking=True) — the title shown in the
  list and the form header.</li>
<li><strong><code>state</code></strong> (Selection, required, tracking=True) —
  <code>draft / ready / approved / rejected / applied</code>. Default <code>draft</code>. Tracked
  in chatter on every change.</li>
<li><strong><code>recommendation_type</code></strong> (Selection, required, tracking=True) —
  <code>task / risk / note / followup</code>. Default <code>task</code>. Tells <code>action_apply</code>
  which side effect to fire (only <code>task</code> triggers <code>_create_project_task</code>).</li>
<li><strong><code>priority</code></strong> (Selection, required, tracking=True) —
  <code>low / normal / high / blocker</code>. Default <code>normal</code>. Blocker rows decorate
  red in the list view.</li>
<li><strong><code>agent_partner_id</code></strong> (Many2one <code>res.partner</code>, readonly, copy=False) —
  the agent that proposed the recommendation. Defaulted via
  <code>_default_agent_partner_id</code> to the seeded Fabio partner XML id
  <code>southbrook_hermes.partner_fabio_agent</code> (data record in
  <code>data/fabio_partner.xml</code>).</li>
<li><strong><code>summary</code></strong> (Text, required) — what Fabio is recommending, in one
  paragraph.</li>
<li><strong><code>rationale</code></strong> (Text) — why.</li>
<li><strong><code>proposed_action</code></strong> (Text) — what specifically should happen.</li>
<li><strong><code>source_model</code></strong> (Char) — the model the recommendation is about (e.g.
  <code>sale.order</code>, <code>res.partner</code>, <code>mrp.production</code>).</li>
<li><strong><code>source_res_id</code></strong> (Integer) — the record id within that model.</li>
<li><strong><code>payload_json</code></strong> (Text, required, default <code>"{}"</code>) — the structured
  payload Fabio sent. Validated as a JSON object on every save by
  <code>_check_payload_json</code> (raises <code>ValidationError</code> if it doesn't parse
  to a dict).</li>
<li><strong><code>agent_run_id</code></strong> (Char, indexed) — the sidecar's trace id for the
  agent loop that produced the recommendation. Indexed because
  diagnostic queries ("which recs came out of run X?") are common.</li>
<li><strong><code>model_provider</code></strong> (Char) — e.g. <code>anthropic</code>, <code>openai</code>. Free-form.</li>
<li><strong><code>model_name</code></strong> (Char) — e.g. <code>claude-opus-4-7</code>. Free-form.</li>
<li><strong><code>reviewer_id</code></strong> (Many2one <code>res.users</code>, readonly, copy=False,
  tracking=True) — set on <code>action_approve</code> and <code>action_reject</code>.</li>
<li><strong><code>reviewed_date</code></strong> (Datetime, readonly, copy=False) — same.</li>
<li><strong><code>applied_date</code></strong> (Datetime, readonly, copy=False) — set on
  <code>action_apply</code>.</li>
<li><strong><code>created_task_id</code></strong> (Many2one <code>project.task</code>, readonly,
  copy=False) — the task that <code>_create_project_task</code> produced. Only
  populated when <code>recommendation_type == 'task'</code>.</li>
</ul>
<h3 id="the-state-machine-every-transition-every-guard">The state machine — every transition, every guard</h3>
<p>The state machine is implemented as four imperative methods. Each
enforces a guard and emits a chatter line.</p>
<p><strong><code>action_mark_ready</code> — <code>draft → ready</code>.</strong></p>
<pre><code class="language-python">def action_mark_ready(self):
    for rec in self:
        if rec.state != &quot;draft&quot;:
            raise UserError(_(&quot;Only draft recommendations can be marked ready.&quot;))
        rec.state = &quot;ready&quot;
    return True
</code></pre>
<p>No reviewer field is written. No chatter post (the tracking=True on
<code>state</code> already logs the transition). Multi-record-safe by the for-loop.</p>
<p><strong><code>action_approve</code> — <code>(draft | ready) → approved</code>.</strong></p>
<pre><code class="language-python">def action_approve(self):
    now = fields.Datetime.now()
    for rec in self:
        if rec.state not in (&quot;draft&quot;, &quot;ready&quot;):
            raise UserError(_(&quot;Only draft or ready recommendations can be approved.&quot;))
        rec.write({
            &quot;state&quot;: &quot;approved&quot;,
            &quot;reviewer_id&quot;: self.env.user.id,
            &quot;reviewed_date&quot;: now,
        })
        rec.message_post(body=_(&quot;Fabio recommendation approved.&quot;))
    return True
</code></pre>
<p>Writes <code>reviewer_id</code> + <code>reviewed_date</code>. Explicit <code>message_post</code> in
addition to the field tracking — the explicit post is what the auditor
reads as "approved by X at T."</p>
<p><strong><code>action_reject</code> — <code>(any except applied) → rejected</code>.</strong></p>
<pre><code class="language-python">def action_reject(self):
    now = fields.Datetime.now()
    for rec in self:
        if rec.state == &quot;applied&quot;:
            raise UserError(_(&quot;Applied recommendations cannot be rejected.&quot;))
        ...
</code></pre>
<p>Note the guard is "anything except applied," not "draft or ready." This is
intentional: you can reject an <em>approved</em> recommendation as long as you
haven't pressed Apply yet. Reviewer + date are written; chatter post is
explicit.</p>
<p><strong><code>action_apply</code> — <code>approved → applied</code>.</strong></p>
<pre><code class="language-python">def action_apply(self):
    now = fields.Datetime.now()
    for rec in self:
        if rec.state != &quot;approved&quot;:
            raise UserError(_(&quot;A recommendation must be approved before it can be applied.&quot;))
        values = {&quot;state&quot;: &quot;applied&quot;, &quot;applied_date&quot;: now}
        if rec.recommendation_type == &quot;task&quot;:
            task = rec._create_project_task()
            values[&quot;created_task_id&quot;] = task.id
        rec.write(values)
        rec.message_post(body=_(&quot;Fabio recommendation applied.&quot;))
    return True
</code></pre>
<p>If the type is <code>task</code>, <code>_create_project_task</code> runs <em>before</em> the state
write. If it raises, the write is rolled back inside the same Odoo
transaction — the state stays <code>approved</code> and <code>applied_date</code> /
<code>created_task_id</code> stay null. This is the deliberate guarantee: a partial
apply never leaves the queue in an inconsistent state.</p>
<h3 id="the-recommendation_type-taxonomy">The <code>recommendation_type</code> taxonomy</h3>
<p>Four values, each with a different meaning:</p>
<ul>
<li><strong><code>task</code></strong> — when applied, creates a <code>project.task</code>. This is the
  workhorse type; the trade-partner intent allow-list in <code>write_tools.py</code>
  (<code>request_revision</code>, <code>request_install_reschedule</code>,
  <code>request_clarification</code>) ships every draft as <code>task</code>. Reviewer applies
  → task lands on a Southbrook project with the original chatter line on
  the task pointing back to the recommendation ("Created from Fabio
  recommendation Hermes/request_revision/…").</li>
<li><strong><code>risk</code></strong> — purely informational. <code>action_apply</code> does NOT create a
  task. Use for "I noticed your install is 6 weeks out and the door
  vendor lead time is 8 weeks." The Apply button still fires (closes the
  record as "you saw it, you decided no action") but nothing downstream
  happens.</li>
<li><strong><code>note</code></strong> — also informational, lower priority than <code>risk</code>. Use for
  observations.</li>
<li><strong><code>followup</code></strong> — created by <code>action_create_recommendation</code> on the
  <code>southbrook.hermes.question</code> model when you want to turn a chat answer
  into a tracked follow-up. The created record carries
  <code>recommendation_type='followup'</code> and is wired up with <code>agent_run_id</code> =
  <code>fabio-ask-&lt;question.id&gt;</code>.</li>
</ul>
<h3 id="_create_project_task-the-side-effect-path"><code>_create_project_task</code> — the side-effect path</h3>
<pre><code class="language-python">def _create_project_task(self):
    self.ensure_one()
    payload = self._payload()
    project_id = payload.get(&quot;project_id&quot;)
    if not project_id:
        raise UserError(_(&quot;Task recommendations require payload.project_id.&quot;))
    project = self.env[&quot;project.project&quot;].browse(project_id).exists()
    if not project:
        raise UserError(_(&quot;Task recommendation project does not exist.&quot;))

    task_name = payload.get(&quot;task_name&quot;) or self.name
    task = self.env[&quot;project.task&quot;].create({
        &quot;project_id&quot;: project.id,
        &quot;name&quot;: task_name,
        &quot;description&quot;: self._task_description(payload),
    })
    task.message_post(
        body=_(&quot;Created from Fabio recommendation %s.&quot;) % self.display_name,
    )
    return task
</code></pre>
<p>The payload <strong>must</strong> contain <code>project_id</code> (integer id of a
<code>project.project</code>). May contain <code>task_name</code> (else uses <code>self.name</code>).
<code>_task_description(payload)</code> returns the description, which is either
<code>payload["description"]</code> verbatim or a synthesized
<code>summary + proposed_action + rationale</code> block. The task's chatter gets a
back-link to the recommendation so future-you can navigate one click
back.</p>
<p>This is what makes <code>task</code>-type recommendations feel "real" once applied:
the resulting task is what production sees in their project board, and
the back-link audits the why.</p>
<h3 id="the-audit-field-set">The audit field set</h3>
<p>When an auditor or a defensive review asks "who decided this and when?",
the answer is on five fields:</p>
<ul>
<li><code>reviewer_id</code> — who clicked Approve or Reject.</li>
<li><code>reviewed_date</code> — when.</li>
<li><code>applied_date</code> — when it actually hit the database.</li>
<li><code>agent_partner_id</code> — who proposed (the Fabio partner).</li>
<li><code>agent_run_id</code> — the sidecar trace id, so you can dig back into what
  the LLM saw.</li>
</ul>
<p>Plus the chatter, which has every state transition timestamped + every
explicit <code>message_post</code>. The combination is overdetermined on purpose —
state can be reconstructed even if one field is lost.</p>
<p>The trio (<code>agent_partner_id</code>, <code>agent_run_id</code>, <code>model_provider</code>,
<code>model_name</code>) is the <em>provenance</em> set: it tells you which Fabio agent
run, on which model, on which provider, produced this recommendation.
Useful when the prod team asks "which version of the model said this?"
during a quality-of-recommendations review.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p>This section deliberately complements lesson 3.2 — it covers what 3.2
doesn't.</p>
<p><strong>Reviewing in batch.</strong></p>
<p>The list view at <strong>Fabio → Recommendations</strong> supports the standard Odoo
batch-action pattern: tick multiple rows in the leftmost column → the
top <strong>Actions</strong> button reveals batch operations. The wired buttons in
the header are:</p>
<ul>
<li><strong>Mark Ready</strong> — works only on rows in <code>draft</code>. Skips any non-draft
  selection (raises <code>UserError</code>, no transaction commits).</li>
<li><strong>Approve</strong> — works on <code>draft</code> + <code>ready</code> rows.</li>
<li><strong>Reject</strong> — works on anything not yet <code>applied</code>.</li>
<li><strong>Apply</strong> — works only on <code>approved</code> rows.</li>
</ul>
<p>Because each transition method loops with <code>for rec in self</code>, batch
operations are all-or-nothing per the guard. One bad row aborts the
whole batch. If you've got 30 ready rows and one of them is somehow in
<code>applied</code>, the batch <code>Approve</code> will fail on iteration N — fix the odd
one out first.</p>
<p><strong>Filtering by source.</strong></p>
<p>The list view supports filtering by <code>source_model</code>. Useful filters:</p>
<ul>
<li><code>source_model = 'res.partner'</code> — recommendations attached to a
  partner (these are the trade-partner-originated ones, since
  <code>propose_recommendation</code> sets <code>source_model='res.partner'</code>,
  <code>source_res_id=partner_id</code>).</li>
<li><code>source_model = 'sale.order'</code> — order-level recommendations.</li>
<li><code>source_model = 'mrp.production'</code> — manufacturing-order
  recommendations.</li>
</ul>
<p>The form view shows source as a read-only <code>source_model</code> + <code>source_res_id</code>
pair; there's no Many2one widget that resolves the link automatically.
That's a deliberate v1 simplification — the model field is generic so
the queue can attach to anything; resolving the link is the reviewer's
job (open <code>Settings → Technical → Database Structure → Models</code>, search
the model name, jump to record by id).</p>
<p><strong>Cross-referencing the <code>agent_run_id</code>.</strong></p>
<p>Every recommendation carries a trace id. If the same run produced multiple
recommendations (common: a single trade-partner conversation that
proposed an install reschedule + a revision request), you can find them
all with the list filter <code>agent_run_id = &lt;id&gt;</code>. This is a debugging
workflow: "Fabio went sideways during run X, what did it propose?"</p>
<p><strong>Quarterly audit cadence.</strong></p>
<p>A standing list filter for the quarterly audit:</p>
<ul>
<li><code>Reviewed Date is set in [last quarter]</code> — what got decided last
  quarter.</li>
<li>Group by <code>Reviewer</code> — who decided what.</li>
<li>Group by <code>Recommendation Type</code> then by <code>State</code> — what got applied vs
  rejected.</li>
</ul>
<p>The data is overdetermined enough that this is a 10-minute report, not a
week.</p>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I want to reject a recommendation that's already <code>applied</code>."</strong></p>
<p>Can't. <code>action_reject</code> raises <code>UserError("Applied recommendations cannot
be rejected.")</code>. The change is in the database; rejecting the
recommendation now would be misleading. The right action: open the
<code>created_task_id</code> (if it's a task type), close or delete <em>that</em>, and
post a chatter note on the recommendation explaining the reversal. The
chatter is the audit log; the queue state stays <code>applied</code> because that
remains historically true.</p>
<p><strong>"<code>action_apply</code> raised <code>UserError("Task recommendation project does not
exist.")</code> — what does that mean?"</strong></p>
<p>The payload's <code>project_id</code> resolved to a project record that doesn't
exist (deleted or never existed). The state stays <code>approved</code> —
<code>_create_project_task</code> raised before the <code>rec.write({state: applied})</code>
landed. Edit <code>payload_json</code>, point at a real project id, save, click
Apply again. The save validates JSON-shape via <code>_check_payload_json</code>;
it does NOT validate that the project_id resolves — only Apply does.</p>
<p><strong>"I changed <code>payload_json</code> on an <code>applied</code> record. What happens?"</strong></p>
<p>The change saves (no state guard on writes). The <code>created_task_id</code> is
not regenerated. If you intended to "redo" the recommendation with a
new payload, the cleaner path is: leave this record applied, create a
new draft recommendation referencing the original, and apply that.
Mutating an applied record's payload pollutes the audit trail — past-you
applied with a different payload than what's now stored.</p>
<p><strong>"The queue is empty but I know the sidecar called <code>propose_recommendation</code>
30 seconds ago."</strong></p>
<p>Two suspects. (1) The sidecar got a 4xx/5xx from <code>POST /api/hermes/tools/propose_recommendation</code>
and the create never happened — check the sidecar's stderr / Vercel
log. (2) The reviewer group isn't on your user; the menu hides empty
states behind the group gate. Confirm via <strong>Settings → Users → [your
user] → Other</strong> that <code>Fabio Reviewer</code> is checked.</p>
<p><strong>"A draft recommendation has been sitting for 4 hours."</strong></p>
<p>Per lesson 3.2 — this is usually a sidecar error after the draft was
posted. You can mark it ready yourself (<code>action_mark_ready</code> is callable
from the button) and review it. The deeper question: who's monitoring
sidecar errors? In v1 that's a manual responsibility (no alerting
shipped); a stuck-drafts query in your morning standup catches it.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<p>The model is small (160-odd lines of Python) but every line is doing a
specific job.</p>
<p><strong>Inheritance.</strong> <code>_inherit = ["mail.thread", "mail.activity.mixin"]</code> —
that's what gets you chatter, the <code>message_post</code> API, the activity
widget, automatic following on <code>create()</code>, and the tracking diff on
fields marked <code>tracking=True</code>. Every state change writes a tracking row
because <code>state</code> has <code>tracking=True</code>. So does <code>recommendation_type</code>,
<code>priority</code>, <code>name</code>, <code>reviewer_id</code>. That's why the chatter is
auto-narrated even without the explicit <code>message_post</code> calls — those
calls are belt-and-braces.</p>
<p><strong>Default for agent partner.</strong></p>
<pre><code class="language-python">@api.model
def _default_agent_partner_id(self):
    return self.env.ref(
        &quot;southbrook_hermes.partner_fabio_agent&quot;,
        raise_if_not_found=False,
    )
</code></pre>
<p><code>raise_if_not_found=False</code> so the model still loads if the seed XML
record was somehow uninstalled. New recommendations from a fully-installed
addon always carry the Fabio partner.</p>
<p><strong>Payload validation.</strong></p>
<pre><code class="language-python">@api.constrains(&quot;payload_json&quot;)
def _check_payload_json(self):
    for rec in self:
        payload = rec._payload()
        if not isinstance(payload, dict):
            raise ValidationError(_(&quot;Payload JSON must be a JSON object.&quot;))

def _payload(self):
    self.ensure_one()
    try:
        return json.loads(self.payload_json or &quot;{}&quot;)
    except json.JSONDecodeError as exc:
        raise ValidationError(_(&quot;Payload JSON is invalid: %s&quot;) % exc) from exc
</code></pre>
<p>Constraints run on every write that touches <code>payload_json</code>. The shape
guarantee is "it's a JSON object." Type-specific keys (<code>project_id</code>,
<code>task_name</code>) are enforced only at apply time by <code>_create_project_task</code> —
that's the deliberate split: queue state can carry incomplete payloads
during review; only the apply step requires the full thing.</p>
<p><strong>Access control.</strong></p>
<p><code>security/ir.model.access.csv</code>:</p>
<pre><code>access_hermes_recommendation_reviewer,southbrook.hermes.recommendation reviewer,
  model_southbrook_hermes_recommendation,group_hermes_reviewer,1,1,1,0
</code></pre>
<p>Read, write, create — yes; unlink — no. Reviewers cannot delete
recommendations. The audit trail is permanent. (If you absolutely must
remove a record, sysadmins with <code>base.group_system</code> can do it through
the technical menu — but <code>group_system</code> is on the
<code>group_hermes_reviewer.implied_ids</code> graph anyway, so this is a
deliberately narrow loophole.)</p>
<p><code>security/hermes_security.xml</code> adds the <code>group_hermes_reviewer</code> group and
declares <code>base.group_system → group_hermes_reviewer</code> so sysadmins inherit
review rights. The menu items in <code>views/hermes_menus.xml</code> all carry
<code>groups="southbrook_hermes.group_hermes_reviewer"</code> — a non-reviewer user
sees no Fabio menu at all.</p>
<p><strong>Why the <code>sudo()</code> carve-out in <code>propose_recommendation</code>.</strong></p>
<p>The recommendation is created on behalf of the requesting trade partner
but must be visible to Southbrook reviewers, who are not the partner. If
the create ran as the partner's portal user, the resulting record would
fall under whatever record rules the partner has, and the reviewer's
search domain wouldn't pick it up. The spec § 4.4 calls this out as
the "explicitly tier-gated T2 sudo carve-out, with a documented reason."
The reason is in <code>write_tools.py</code>'s comment: a draft recommendation is
owned by the Fabio partner, not by the requesting user. See lesson 13.6.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> You approve a <code>task</code> recommendation. Before you click Apply, an
audit asks "what's the <code>created_task_id</code> on this record?" What does the
field show?</p>
<blockquote>
<p>Empty / <code>False</code>. <code>created_task_id</code> is written by <code>action_apply</code> →
<code>_create_project_task</code>, not by <code>action_approve</code>. Approval is the
review decision; apply is the database mutation. Between the two,
<code>state='approved'</code>, <code>reviewer_id</code> and <code>reviewed_date</code> are populated,
but <code>applied_date</code> and <code>created_task_id</code> are still null.</p>
</blockquote>
<p><strong>2.</strong> A <code>risk</code>-type recommendation needs to be closed because it's
obsolete. You click Approve, then look for an Apply button to dispose of
it. Apply runs cleanly but <code>created_task_id</code> is still null. Is that a
bug?</p>
<blockquote>
<p>No — that's the design. <code>action_apply</code> only calls
<code>_create_project_task</code> when <code>recommendation_type == 'task'</code>. For
<code>risk</code>, <code>note</code>, and <code>followup</code> types, applying just records "we've
decided no action" — sets <code>applied_date</code> + <code>state='applied'</code> and
posts the chatter "Fabio recommendation applied." <code>created_task_id</code>
stays null because there's no task to point at.</p>
</blockquote>
<p><strong>3.</strong> A new junior dev wants to add a fifth recommendation type
called <code>chase</code> (for chasing a partner for missing info). Where do
they edit it, and what side effect will <code>action_apply</code> fire for that
type by default?</p>
<blockquote>
<p>They add the new value to the <code>state</code> Selection on
<code>southbrook.hermes.recommendation.recommendation_type</code> in
<code>models/hermes_recommendation.py</code>. By default <code>action_apply</code> will
fire NO side effect for <code>chase</code>, because the only side effect
branch in <code>action_apply</code> is <code>if rec.recommendation_type == "task":</code>.
If <code>chase</code> should also create a task or send an email, they have to
add an explicit branch.</p>
</blockquote>
<p><strong>4.</strong> Auditor asks "show me every recommendation approved by user
'Morgan Production' in March 2026 that was attached to a
<code>mrp.production</code> source." How do you build that list?</p>
<blockquote>
<p>Open <strong>Fabio → Recommendations</strong>. Apply filters:
<code>reviewer_id = Morgan Production</code>, <code>reviewed_date &gt;= 2026-03-01</code>,
<code>reviewed_date &lt; 2026-04-01</code>, <code>source_model = 'mrp.production'</code>. Group
by <code>state</code> (so you can see how many were approved vs rejected vs
applied). The audit fields are denormalized for exactly this query.</p>
</blockquote>
<p><strong>5.</strong> A reviewer accidentally rejects an <code>approved</code> recommendation
that should have been applied. Can they un-reject and apply it?</p>
<blockquote>
<p>No, not directly — the state machine has no <code>rejected → approved</code>
transition. The path is: create a new recommendation with the same
<code>summary</code>/<code>payload_json</code>/<code>source_*</code> fields (the original
<code>payload_json</code> can be copy-pasted), mark it ready, approve, apply.
Post a chatter note on both records explaining the reroute (the
original record's chatter records the reversal; the new one carries
the actual action). The audit reads as "rejected by mistake;
superseded by REC-X." That's the conservative, defensible path; the
alternative (a reopen transition) was deliberately not shipped.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The trade-partner-facing UI of proposing a recommendation —
  lesson 13.7 (chat panel) + lesson 13.6 (<code>propose_recommendation</code>
  intent allow-list).</li>
<li>The CS-side filter conventions on the queue — lesson 6.3
  (<code>06_hermes_cs_view.md</code>).</li>
<li>The five-state daily-flow walkthrough (start, ready, approve, apply,
  reject) — lesson 3.2 (<code>03_hermes_fabio_approval.md</code>).</li>
<li>The sidecar trace that produced the recommendation — lesson 13.7.</li>
<li>The JWT + persona that authenticated the proposing call —
  lesson 13.3.</li>
<li>Sysadmin recommendations (separate operator surface) — lesson 7.3
  (<code>07_hermes_sysadmin.md</code>).</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_jwt_auth_personas" model="slide.slide">
            <field name="name">JWT Auth and Persona Resolution</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">30</field>
            <field name="description">Developer touching `utils/jwt_helper.py` or any Hermes controller; IT admin responsible for the JWT secret + system parameters.</field>
            <field name="html_content"><![CDATA[<h1 id="jwt-auth-and-persona-resolution">JWT Auth and Persona Resolution</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the developer about to touch <code>utils/jwt_helper.py</code>, an Odoo
controller that mints or verifies a JWT, or a new tool that needs to know
who's calling. Or you're the IT admin responsible for rotating the
secret, watching for misconfigurations, and making sure the right partners
land on the right persona.</p>
<p>The JWT layer is small (88 lines of Python) but load-bearing — every
Fabio call sits inside one. Get it wrong and the whole ACL model falls
through.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<blockquote>
<p><strong>Settings → Technical → System Parameters</strong> — search for
<code>southbrook_hermes.jwt_secret</code>. This is the HS256 signing secret. The
placeholder shipped by the addon is the literal string
<code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code> (see <code>data/ir_config_parameter.xml</code>);
the helper actively refuses to mint or verify against the placeholder
(raises <code>RuntimeError</code>).</p>
<p><strong>Settings → Users → [user] → Other</strong> — <code>Share User</code> (the
<code>res.users.share</code> field) decides whether <code>resolve_persona</code> returns
<code>trade_partner</code>. Trade partners are portal users; share=True.</p>
<p><strong>Settings → Users → [user] → Sales</strong> — <code>Salesperson</code> membership
(<code>sales_team.group_sale_salesman</code>) drives the <code>sales_rep</code> persona.</p>
<p><strong>Settings → Users → [user] → Kitchen Ops</strong> —
<code>southbrook_kitchen_workspace.group_kitchen_ops</code> drives the
<code>mfg_manager</code> persona.</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>You don't see a screen for the JWT layer most of the time — JWTs live
inside HTTP headers and a system parameter. What you see is the
consequences in three places.</p>
<h3 id="the-system-parameter-triad">The system parameter triad</h3>
<p>Three rows under <strong>Settings → Technical → System Parameters</strong> define the
auth surface:</p>
<table>
<thead>
<tr>
<th>Key</th>
<th>Default at install</th>
<th>What it controls</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>southbrook_hermes.jwt_secret</code></td>
<td><code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code></td>
<td>HS256 signing secret. Must be rotated before any live traffic.</td>
</tr>
<tr>
<td><code>southbrook_hermes.sidecar_url</code></td>
<td><code>https://hermes.southbrookcabinetry.space</code></td>
<td>Where <code>/hermes/v1/ask</code> sends the LLM request.</td>
</tr>
<tr>
<td><code>southbrook_hermes.sidecar_enabled</code></td>
<td><code>false</code></td>
<td>Whether the proxy actually calls the sidecar (vs. returning a JSON stub).</td>
</tr>
</tbody>
</table>
<p>The shipped placeholder value (<code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code>)
is recognized by <code>_get_secret(env)</code> in <code>utils/jwt_helper.py</code> as a
sentinel:</p>
<pre><code class="language-python">def _get_secret(env):
    secret = env[&quot;ir.config_parameter&quot;].sudo().get_param(
        &quot;southbrook_hermes.jwt_secret&quot;)
    if not secret or secret == &quot;PLACEHOLDER_ROTATE_BEFORE_PRODUCTION&quot;:
        raise RuntimeError(
            &quot;southbrook_hermes.jwt_secret is not set. Rotate the placeholder &quot;
            &quot;via Settings → Technical → System Parameters before going live.&quot;)
    return secret
</code></pre>
<p>Any controller that tries to mint or verify a JWT before the rotation
will get a <code>RuntimeError</code>, surface it as a <code>503 hermes_not_configured</code>
response (see <code>controllers/hermes_proxy.py</code> exception handling), and the
chat panel will render a clear admin-targeted error. The placeholder
exists so the addon installs cleanly without leaking a real secret in
version control; it stops short of letting you actually use it.</p>
<h3 id="the-persona-resolution-function">The persona resolution function</h3>
<p>In <code>utils/jwt_helper.py</code>:</p>
<pre><code class="language-python">def resolve_persona(user):
    if user.share:
        return &quot;trade_partner&quot;                       # v1.0
    if user.has_group(&quot;sales_team.group_sale_salesman&quot;):
        return &quot;sales_rep&quot;                           # v1.1
    if user.has_group(&quot;southbrook_kitchen_workspace.group_kitchen_ops&quot;):
        return &quot;mfg_manager&quot;                         # v1.2
    raise AccessError(&quot;Hermes is not available for this user role.&quot;)
</code></pre>
<p>Three buckets, evaluated in order. The first hit wins. If a user is
both a share user (portal) and in the salesman group (unusual but
possible), they resolve to <code>trade_partner</code> because <code>share</code> is checked
first. If a user matches none, the proxy returns 403 to the chat panel.</p>
<h3 id="the-tier-mask">The tier mask</h3>
<pre><code class="language-python">def tier_for_persona(persona):
    return {
        &quot;trade_partner&quot;: &quot;T0+T1+T2&quot;,
        &quot;sales_rep&quot;: &quot;T0+T1+T2&quot;,
        &quot;mfg_manager&quot;: &quot;T0+T1+T2&quot;,
    }.get(persona, &quot;T0&quot;)
</code></pre>
<p>All three personas get the full T0+T1+T2 mask. This is intentional, per
implementation delta § 14.1: the trade-partner safety boundary used to be
"trade partners cannot call T2 tools at all," which is a clean idea right
up until you ship <code>propose_recommendation</code> (which is T2 and which trade
partners absolutely must be able to call to file a revision request). The
fix was to leave the mask wide open and move the trade-partner intent
allow-list <em>inside</em> <code>propose_recommendation</code> itself
(<code>_TRADE_PARTNER_INTENTS = ("request_revision",
"request_install_reschedule", "request_clarification")</code> in
<code>tools/write_tools.py</code>). Net behavior matches the spec's intent; the
enforcement layer moved.</p>
<h3 id="the-jwt-payload-shape">The JWT payload shape</h3>
<p><code>mint_jwt</code> (lines 29–48 of <code>utils/jwt_helper.py</code>):</p>
<pre><code class="language-python">payload = {
    &quot;iat&quot;: int(now.timestamp()),
    &quot;exp&quot;: int((now + datetime.timedelta(seconds=ttl_seconds)).timestamp()),
    &quot;tenant&quot;: tenant,
    &quot;persona&quot;: persona,
    &quot;partner_id&quot;: partner_id,
    &quot;tier&quot;: tier,
}
if extra:
    payload.update(extra)
</code></pre>
<p>Four required claims:
- <strong><code>tenant</code></strong> — <code>"southbrook"</code> in v1.0. v1.x plans to scale to
  Porterly/Tribancs/etc. so the sidecar can route per tenant.
- <strong><code>persona</code></strong> — one of <code>trade_partner / sales_rep / mfg_manager</code>.
- <strong><code>partner_id</code></strong> — the requesting user's <code>partner_id.id</code>. Used by
  dispatch to look up the right portal user for record-rule scoping.
- <strong><code>tier</code></strong> — the tier mask. Always <code>T0+T1+T2</code> in v1.0.</p>
<p>Plus two standard JWT claims (<code>iat</code>, <code>exp</code>), and an optional <code>extra</code>
dict (the proxy passes <code>{"order_id": body.get("order_id")}</code> so tools can
optionally restrict to a specific order context).</p>
<h3 id="jwt-lifetime">JWT lifetime</h3>
<p><code>DEFAULT_TTL_SECONDS = 60</code> at the top of the module. But the proxy
overrides this to 120s for the <code>/hermes/v1/ask</code> call:
<code>_JWT_TTL_FOR_ASK = 120</code> in <code>controllers/hermes_proxy.py</code>. The reason is
in implementation delta § 14.7 — a 60s token expired mid-loop on a
multi-step agent run (4–5 tool roundtrips), tool dispatch returned 401,
and the LLM saw a confusing "tool failed" result rather than the actual
data. 120s gives headroom for the whole loop while staying well under
Vercel's 60s function cap × multi-loop budget.</p>
<p>The token is still per-request, not per-conversation — every new
<code>/hermes/v1/ask</code> call mints a fresh JWT. If the user's Odoo session
expires between turns, the next turn fails cleanly at issuance.</p>
<h3 id="claim-bound-args-the-dispatch-time-enforcement">Claim-bound args (the dispatch-time enforcement)</h3>
<p>In <code>controllers/hermes_tools_api.py</code>:</p>
<pre><code class="language-python">_CLAIM_BOUND_ARGS = (&quot;persona&quot;, &quot;partner_id&quot;, &quot;tenant&quot;)

# ... inside dispatch():
sig_params = inspect.signature(fn).parameters
for arg_name in _CLAIM_BOUND_ARGS:
    if arg_name in sig_params:
        if arg_name == &quot;persona&quot;:
            args[&quot;persona&quot;] = claims[&quot;persona&quot;]
        elif arg_name == &quot;partner_id&quot;:
            args[&quot;partner_id&quot;] = claims[&quot;partner_id&quot;]
        elif arg_name == &quot;tenant&quot;:
            args[&quot;tenant&quot;] = claims[&quot;tenant&quot;]
</code></pre>
<p>Three tool args (<code>persona</code>, <code>partner_id</code>, <code>tenant</code>) are
<strong>authoritatively set from JWT claims</strong> before the tool runs, overriding
anything in the request body. The LLM (or a prompt-injection attack)
<strong>cannot</strong> spoof its persona to lift the trade-partner intent guard, and
<strong>cannot</strong> attribute a recommendation to another partner. This was the
fix in implementation delta § 14.2 — without it, the first
implementation accepted <code>persona</code> and <code>partner_id</code> as ordinary tool args.</p>
<p>Tools that don't declare those args (most tools don't) are unaffected.
The dispatch inspects the function signature with <code>inspect.signature(fn).parameters</code>
and only injects when the arg is actually declared.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<p>This section is split by the role you're playing.</p>
<h3 id="as-the-developer-wiring-a-new-tool">As the developer wiring a new tool</h3>
<ol>
<li>Decide your persona allowlist. Trade-partner-callable? Sales-rep-only?
   Sysadmin-only?</li>
<li>Decide your tier honestly. T0 = read or low-stakes write (chatter
   note, log). T1 = communication (email, activity). T2 = business
   mutation (proposes via recommendation).</li>
<li>Declare your tool signature with <code>partner_id</code> if the tool's behavior
   is scoped to the calling partner. This will be auto-injected from
   the JWT and is the safe way to scope.</li>
<li>Inside the tool, read partner from <code>env.user.partner_id</code>, NOT from
   the function arg. <code>partner_id</code> arg is "informational only" (delta
   § 14.4 — <code>list_my_kitchen_projects</code> and <code>list_my_recommendations</code>
   were rewritten to ignore the arg specifically for this reason). The
   dispatch sets <code>env(user=portal_user)</code> from the JWT, so
   <code>env.user.partner_id</code> is always the verified caller.</li>
<li>Add <code>check_access_rights("read")</code> AND <code>check_access_rule("read")</code>
   inside the tool for record-rule scope. Delta § 14.5 added these to
   three read tools that missed them; the pattern is now uniform.</li>
<li>Test with <code>scripts/smoke_hermes.sh</code> — it mints a fresh JWT and
   exercises every route.</li>
</ol>
<h3 id="as-the-it-admin-operating-fabio">As the IT admin operating Fabio</h3>
<p><strong>Rotate the JWT secret.</strong></p>
<ol>
<li>Open <strong>Settings → Technical → System Parameters</strong>.</li>
<li>Search <code>southbrook_hermes.jwt_secret</code>. Edit the value to a fresh
   high-entropy string. The placeholder check is exact (<code>==</code> against
   <code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code>); any other non-empty value
   is accepted.</li>
<li>Update the sidecar's <code>HERMES_JWT_SECRET</code> env var to match (Vercel
   dashboard → Environment Variables, or <code>vercel env add</code> from CLI).</li>
<li>Redeploy the sidecar: <code>vercel deploy --prod</code>.</li>
</ol>
<p>Rotation is currently atomic — there's no JWKS endpoint and no overlap
period. If you rotate in Odoo before the sidecar picks it up, every
in-flight chat will 401 with <code>invalid_token</code> for the gap window
(typically seconds). The spec § 12 open question 3 calls this out: "v1.0
ships with a static <code>HERMES_JWT_SECRET</code> env var; if you want rotation
from day one, we add JWKS endpoint complexity." That complexity wasn't
shipped in v1.0.</p>
<p><strong>Suggested rotation policy.</strong></p>
<p>Quarterly, or immediately on suspicion of leak. The secret never leaves
two places: the Odoo system parameter (sudo-only via the
<code>ir.config_parameter</code> ACL) and the Vercel env var (env-scoped). It is
NOT in version control — <code>data/ir_config_parameter.xml</code> ships the
placeholder, not a real secret.</p>
<p><strong>Audit who has access to the secret.</strong></p>
<p>The system parameter is readable only by users in the
<code>base.group_system</code> group (Odoo's standard ACL on <code>ir.config_parameter</code>).
Trade partners cannot read it. Reviewers (group_hermes_reviewer) do not
inherit <code>group_system</code> — only the other direction (<code>group_system</code>
inherits <code>group_hermes_reviewer</code>).</p>
<h3 id="as-the-developer-reading-a-request-log">As the developer reading a request log</h3>
<p>A typical good-state log line for <code>/api/hermes/tools/get_order_status</code>:</p>
<pre><code>... POST /api/hermes/tools/get_order_status HTTP/1.1 200 ...
... [DEBUG] JWT verified: tenant=southbrook persona=trade_partner
                          partner_id=42 tier=T0+T1+T2
... [DEBUG] Tool get_order_status called for partner 42, order 235
</code></pre>
<p>A bad-state log line means one of the error envelopes from delta § 14.11:</p>
<table>
<thead>
<tr>
<th>Code</th>
<th>When</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>missing_bearer_token</code> (401)</td>
<td>Authorization header missing</td>
</tr>
<tr>
<td><code>invalid_token</code> (401)</td>
<td>JWT signature wrong or expired</td>
</tr>
<tr>
<td><code>hermes_not_configured</code> (503)</td>
<td>Secret not set or PyJWT missing</td>
</tr>
<tr>
<td><code>unknown_partner</code> (403)</td>
<td><code>partner_id</code> claim points at a deleted partner</td>
</tr>
<tr>
<td><code>partner_has_no_user</code> (403)</td>
<td>Partner has no linked <code>res.users</code></td>
</tr>
<tr>
<td><code>not_allowed_for_persona</code> (403)</td>
<td>Tool's persona allowlist doesn't include claim</td>
</tr>
<tr>
<td><code>tier_required</code> (403)</td>
<td>Tool's tier not in claim's tier mask</td>
</tr>
</tbody>
</table>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"The chat panel is returning 503 with <code>hermes_not_configured</code>."</strong></p>
<p>You're hitting one of two RuntimeErrors:
- <code>southbrook_hermes.jwt_secret</code> is still the placeholder. Fix: rotate
  it (see admin flow above).
- <code>PyJWT</code> (<code>import jwt</code>) is not installed in the container. Fix:
  <code>docker exec southbrook-odoo pip install PyJWT --break-system-packages</code>
  (the temporary install, per CLAUDE.md amendment), OR bake into the
  Docker image. The dependency is documented in the manifest comments
  but deliberately not gated on install — the helper raises a clear
  RuntimeError when missing, which surfaces as the 503.</p>
<p><strong>"A trade partner is getting 403 <code>forbidden</code> on every chat turn."</strong></p>
<p><code>resolve_persona</code> returned <code>AccessError</code>. Three causes:
- The user is internal but matches none of the persona buckets. They're
  not a share user, not a salesman, not in <code>group_kitchen_ops</code>. Fix:
  add them to one of those groups, OR (if they're an admin who just
  wants to test) add them to the kitchen ops group.
- The user IS a share user but somehow got into the chat panel without
  share=True (very rare). Fix: verify the user record.</p>
<p><strong>"The sidecar is rejecting tokens with <code>invalid_token</code>."</strong></p>
<p>Three causes:
- Secret mismatch between Odoo's system parameter and Vercel's
  <code>HERMES_JWT_SECRET</code> env var. Fix: re-sync, redeploy sidecar.
- Clock skew. The <code>exp</code> claim is in seconds; if the sidecar's clock
  drifts past <code>exp</code>, it'll reject as expired. Fix: NTP on both
  hosts.
- The sidecar's <code>HERMES_JWT_SECRET</code> env var is empty / missing —
  delta § 14.9. Sidecar should respond 503 <code>jwt_config</code> (not 401
  <code>invalid_token</code>) in this case — if you see 401 instead, your
  sidecar is on a pre-delta build, redeploy.</p>
<p><strong>"A new tool I added is accepting a <code>persona</code> arg from the LLM and
running with it."</strong></p>
<p>The dispatch's claim-binding (delta § 14.2) inspects the function
signature: it only overrides <code>persona</code> / <code>partner_id</code> / <code>tenant</code> if those
args are <em>declared on the tool function</em>. If you declared a <code>persona</code> arg,
dispatch overrides it. If you didn't, no override happens and the LLM
can pass anything — but the tool can't <em>use</em> it anyway because no claim
was bound. The takeaway: declare those args explicitly when you need
them; never read persona/partner_id from anywhere else.</p>
<p><strong>"<code>schedule_followup_activity</code> raised <code>AccessError</code> even though the
partner can see the order."</strong></p>
<p>Almost certainly because the partner has no linked <code>res.users</code> record.
Dispatch returns 403 <code>partner_has_no_user</code> in that case (delta § 14.3).
Fix: open the partner record, invite as portal user from the partner
form. The auto-generated portal user gets linked to <code>res.partner.user_ids</code>
and dispatch resolves cleanly.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<h3 id="why-hs256-symmetric-not-rs256-asymmetric">Why HS256 (symmetric), not RS256 (asymmetric)</h3>
<p>v1.0 has one signer (the Odoo proxy) and one verifier (the sidecar). A
shared secret is sufficient. RS256 would let <em>multiple</em> signers verify
against a public key, which is the right pattern for a JWKS endpoint with
rotation overlap — and that's deferred to v1.x (spec § 12 q3). HS256 is
the simpler thing that works for v1.</p>
<p>The algorithm is locked in two places in <code>jwt_helper.py</code>:
<code>JWT_ALGORITHM = "HS256"</code> at module scope, and <code>algorithms=[JWT_ALGORITHM]</code>
in the <code>verify_jwt</code> call. A token signed with any other algorithm
(including the dangerous <code>none</code>) is rejected by PyJWT.</p>
<h3 id="why-persona-is-server-side-not-client-side">Why persona is server-side, not client-side</h3>
<p><code>resolve_persona(user)</code> reads <code>user.share</code> and <code>user.has_group(...)</code> —
both of which are sourced from the Odoo database via the authenticated
session. The client (browser) cannot influence these. The persona is
locked into the JWT at mint time, and the sidecar trusts the JWT, not
anything else from the wire. This is the spec § 3 invariant: "the sidecar
never holds business state; the JWT carries the identity."</p>
<h3 id="why-dispatch-re-resolves-the-user-from-partner_id">Why dispatch re-resolves the user from <code>partner_id</code></h3>
<p><code>POST /api/hermes/tools/&lt;slug&gt;</code> runs as <code>auth='public'</code> — it has no
session. Dispatch verifies the JWT, then:</p>
<pre><code class="language-python">partner = request.env[&quot;res.partner&quot;].sudo().browse(claims[&quot;partner_id&quot;])
if not partner.exists():
    return 403 unknown_partner
user = partner.user_ids[:1]
if not user:
    return 403 partner_has_no_user
env_with_user = request.env(user=user.id)
</code></pre>
<p>This is what makes record rules apply. If dispatch ran the tool as the
public user (the default for <code>auth='public'</code>), every tool would run with
the least-privileged identity, returning empty results and silently
hiding everything the partner owns. Delta § 14.3 was added because the
original implementation fell back to <code>request.env.user</code> (public) when
the partner had no user — a misconfiguration that should be loud became
silent.</p>
<h3 id="why-the-extra-dict-pattern">Why the <code>extra</code> dict pattern</h3>
<p><code>extra</code> is the open-ended JWT extension hook. The proxy uses it for
<code>{"order_id": body.get("order_id")}</code> so tools can optionally see what
order the chat panel was open against. The claim isn't required (the
trade-partner chat panel might be open without an order context — e.g.
from a general portal page), but when present it lets the LLM scope its
RAG queries and tool calls more tightly. Tools that want this read
<code>claims.get("order_id")</code> — but in v1.0 no tool actually reads it
from claims; the order id comes through the request body. Reserved for
future use.</p>
<h3 id="why-the-placeholder-is-a-literal-string">Why the placeholder is a literal string</h3>
<p><code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code> is the <em>exact</em> string the helper
compares against. Two reasons:
- The addon installs cleanly without a manual env-var step. Trade
  partners can't access <code>Settings → Technical</code>, so a missing secret
  doesn't break their UI before it's wired up.
- The string is loud and impossible to mistake for a real secret. A
  developer who greps for it in the codebase finds the rotation TODO
  immediately. A leaked logs grep for the value matches the literal
  string, not a hashed/encoded artifact.</p>
<p>The cost is one extra branch (<code>if not secret or secret ==
"PLACEHOLDER_…"</code>). The benefit is a foolproof default.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> Trade-partner Fred chats Fabio. The LLM emits a tool call with
<code>persona="mfg_manager"</code> and <code>partner_id=99</code> in the args. What happens at
dispatch?</p>
<blockquote>
<p>Both args are overridden from the JWT claims (delta § 14.2). Dispatch
inspects the tool function signature — if <code>persona</code> and <code>partner_id</code>
are declared, they get set to <code>"trade_partner"</code> and Fred's partner id
from the verified JWT, regardless of what the LLM sent. If they're
not declared, no override happens but the tool can't use the LLM's
values anyway because the args wouldn't be bound. The persona
escalation attempt is silently neutralized.</p>
</blockquote>
<p><strong>2.</strong> You rotate the JWT secret in Odoo but haven't redeployed the
sidecar yet. What's the user-facing symptom?</p>
<blockquote>
<p>Every in-flight chat returns a sidecar error to the panel. From
Odoo's perspective, mint succeeds. The sidecar's <code>verifyJwt</code> fails
because its <code>HERMES_JWT_SECRET</code> env var still has the old value. The
sidecar responds 401 <code>invalid_token</code>; the proxy passes that through
to the panel which renders "(Hermes is unavailable: invalid_token)".
Fix: update the Vercel env var, <code>vercel deploy --prod</code>. Time gap is
typically seconds during which chat is down.</p>
</blockquote>
<p><strong>3.</strong> A new internal user is created with <code>share=False</code>, no salesman
group, no kitchen_ops group. They open the Order Builder. What
happens when they try to chat?</p>
<blockquote>
<p><code>resolve_persona</code> raises <code>AccessError("Hermes is not available for
this user role.")</code>. The proxy returns 403 with <code>{error: "forbidden",
detail: "Hermes is not available…"}</code>. The chat panel renders
<code>(Hermes is unavailable: Hermes is not available for this user role.)</code>.
Fix: add the user to one of the three persona-deciding groups
(probably <code>southbrook_kitchen_workspace.group_kitchen_ops</code> for an
internal user).</p>
</blockquote>
<p><strong>4.</strong> The JWT TTL is 60s in <code>DEFAULT_TTL_SECONDS</code> but 120s in
<code>_JWT_TTL_FOR_ASK</code>. Which one is actually used by a chat turn, and
why the discrepancy?</p>
<blockquote>
<p>120s. <code>controllers/hermes_proxy.py</code> passes <code>ttl_seconds=_JWT_TTL_FOR_ASK</code>
to <code>mint_jwt</code>, overriding the helper's default. The discrepancy is
the implementation delta § 14.7 — a 60s token expired mid-loop on a
multi-step agent run. 60s remains the helper default for callers
that don't need the wider window; the proxy explicitly bumps to 120
because the LLM may do 4–5 tool roundtrips per turn.</p>
</blockquote>
<p><strong>5.</strong> A developer wants to add a new persona <code>installer</code> for the
field-install team. Where do they edit it, and where does the addition
fail open?</p>
<blockquote>
<p>Edit <code>utils/jwt_helper.py</code> — add a fourth branch in
<code>resolve_persona(user)</code> (decide what group implies installer), and
a key in <code>tier_for_persona</code> (decide what tier mask). They must ALSO
update every <code>@hermes_tool</code> decorator's <code>personas</code> list to include
<code>"installer"</code> everywhere appropriate — otherwise existing tools will
reject installers as <code>not_allowed_for_persona</code>. The mask change
alone doesn't enroll the persona; the tool registry filtering is
the second check.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The recommendation queue itself — lesson 13.2.</li>
<li>The tool registry mechanics, <code>@hermes_tool</code>, and dispatch internals
  beyond the JWT layer — lesson 13.4.</li>
<li>The 9 read tools' record-rule scoping in detail — lesson 13.5.</li>
<li>The T0/T1/T2 tier model and the <code>propose_recommendation</code> intent
  allow-list — lesson 13.6.</li>
<li>The chat panel + sidecar (the consumers of these JWTs) — lesson 13.7.</li>
<li>The sidecar's JWT verification path (TypeScript in <code>sidecar/lib/jwt.ts</code>,
  not Python) — lesson 13.7 references it.</li>
<li>General Odoo session / cookie auth, which is separate from the
  Hermes JWT layer.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_tool_registry" model="slide.slide">
            <field name="name">The Tool Registry — Decorator, Dispatch, and ACL Layering</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">40</field>
            <field name="description">Developer adding or modifying tools; admin / sysadmin debugging tool-call traces.</field>
            <field name="html_content"><![CDATA[<h1 id="the-tool-registry-decorator-dispatch-and-acl-layering">The Tool Registry — Decorator, Dispatch, and ACL Layering</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the developer about to add the fourteenth tool. Or you're the
admin investigating why a tool the spec says should be available isn't
showing up in the LLM's function list. Or you're the IT person being
asked "is this tool call safe?" before flipping the sidecar live.</p>
<p>The tool registry is the single most-load-bearing piece of Fabio's
internals. It's small (~65 lines for the decorator, ~130 lines for the
controller) but it's where every ACL decision gets made.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>You don't reach the registry through a menu — it's an HTTP endpoint:</p>
<blockquote>
<p><strong><code>GET /api/hermes/tools</code></strong> — public route, JWT-bearer authenticated.
Returns the tool list filtered by the caller's <code>(persona, tier)</code>
claims. The sidecar calls this once at cold start.</p>
<p><strong><code>POST /api/hermes/tools/&lt;slug&gt;</code></strong> — public route, JWT-bearer
authenticated. Dispatches the named tool. The sidecar calls this every
time the LLM emits a function call.</p>
</blockquote>
<p>Both controllers are in <code>addons/southbrook_hermes/controllers/hermes_tools_api.py</code>.</p>
<p>You can also inspect the registry from a <code>odoo-bin shell</code> Python REPL:</p>
<pre><code class="language-python">&gt;&gt;&gt; from odoo.addons.southbrook_hermes.tools.decorator import TOOL_REGISTRY
&gt;&gt;&gt; [t[&quot;slug&quot;] for t in TOOL_REGISTRY]
['list_my_orders', 'get_order_status', 'get_order_line',
 'list_my_kitchen_projects', 'get_kitchen_project',
 'get_install_schedule', 'get_quote_pdf_url',
 'list_my_recommendations', 'get_os_section',
 'post_internal_note', 'send_spec_pdf_email',
 'schedule_followup_activity', 'propose_recommendation']
</code></pre>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>Five concrete things make up the registry layer.</p>
<h3 id="1-the-hermes_tool-decorator">1. The <code>@hermes_tool</code> decorator</h3>
<p><code>addons/southbrook_hermes/tools/decorator.py</code>:</p>
<pre><code class="language-python">TOOL_REGISTRY = []

def hermes_tool(*, personas, tier, scope, description, parameters=None):
    def wrap(fn):
        slug = fn.__name__
        params = parameters or _extract_params_from_signature(fn)
        TOOL_REGISTRY.append({
            &quot;slug&quot;: slug,
            &quot;qualified_slug&quot;: f&quot;southbrook__{slug}&quot;,
            &quot;personas&quot;: list(personas),
            &quot;tier&quot;: tier,
            &quot;scope&quot;: scope,
            &quot;description&quot;: description,
            &quot;parameters&quot;: params,
            &quot;fn&quot;: fn,
        })
        @functools.wraps(fn)
        def call(env, **kwargs):
            return fn(env, **kwargs)
        return call
    return wrap
</code></pre>
<p>Four required keyword args:</p>
<ul>
<li><strong><code>personas</code></strong> — list of persona strings allowed to call the tool.
  Drawn from <code>{trade_partner, sales_rep, mfg_manager}</code>. If a tool is
  trade-partner-only, list only <code>["trade_partner"]</code>. Most v1.0 tools
  list <code>["trade_partner", "sales_rep"]</code> because sales reps inherit
  trade-partner capabilities.</li>
<li><strong><code>tier</code></strong> — one of <code>T0 / T1 / T2</code>. See lesson 13.6 for the tier
  definitions. Dispatch enforces this against the JWT's tier mask
  claim.</li>
<li><strong><code>scope</code></strong> — a string describing the tool's data-scoping discipline.
  Free-form but conventionally one of: <code>own</code>, <code>own_order</code>, <code>global</code>.
  This is documentation, not enforcement — actual scope is enforced
  by record rules inside the tool. But the registry lists it so the
  sidecar and reviewers can see the intent.</li>
<li><strong><code>description</code></strong> — one-paragraph natural-language description shown
  to the LLM. This is the LLM's only narrative source for "when to use
  this tool"; write it carefully.</li>
<li><strong><code>parameters</code></strong> (optional) — JSON Schema for the tool args. If
  omitted, <code>_extract_params_from_signature(fn)</code> introspects the Python
  signature and generates a minimal schema from type hints.</li>
</ul>
<p>The decorator runs at import time. The <code>TOOL_REGISTRY</code> list is
process-level — populated when Odoo starts and the
<code>southbrook_hermes.tools</code> package is imported. There's no database
backing; restart Odoo and the registry rebuilds itself from source.</p>
<h3 id="2-the-tool_registry-data-structure">2. The <code>TOOL_REGISTRY</code> data structure</h3>
<p>After all decorators have run, <code>TOOL_REGISTRY</code> is a list of dicts:</p>
<pre><code class="language-python">{
    &quot;slug&quot;: &quot;get_order_status&quot;,
    &quot;qualified_slug&quot;: &quot;southbrook__get_order_status&quot;,
    &quot;personas&quot;: [&quot;trade_partner&quot;, &quot;sales_rep&quot;],
    &quot;tier&quot;: &quot;T0&quot;,
    &quot;scope&quot;: &quot;own_order&quot;,
    &quot;description&quot;: &quot;Return the production status of a specific order, …&quot;,
    &quot;parameters&quot;: {
        &quot;type&quot;: &quot;object&quot;,
        &quot;properties&quot;: {&quot;order_id&quot;: {&quot;type&quot;: &quot;integer&quot;}},
        &quot;required&quot;: [&quot;order_id&quot;],
    },
    &quot;fn&quot;: &lt;function get_order_status at 0x…&gt;,
}
</code></pre>
<p>The <code>qualified_slug</code> (<code>southbrook__get_order_status</code>) is the per-tenant
prefix from spec § 4.4 rule 3 — prevents misrouted cross-tenant calls
when v1.x adds Porterly. The sidecar uses the qualified slug; Odoo
dispatch uses the bare slug because the controller is already
tenant-scoped (an Odoo instance is one tenant).</p>
<h3 id="3-_extract_params_from_signature-the-schema-introspection">3. <code>_extract_params_from_signature</code> — the schema introspection</h3>
<pre><code class="language-python">def _extract_params_from_signature(fn):
    sig = inspect.signature(fn)
    props = {}
    required = []
    for name, param in sig.parameters.items():
        if name == &quot;env&quot;:
            continue
        json_type = &quot;string&quot;
        if param.annotation is int:
            json_type = &quot;integer&quot;
        elif param.annotation is bool:
            json_type = &quot;boolean&quot;
        elif param.annotation is float:
            json_type = &quot;number&quot;
        props[name] = {&quot;type&quot;: json_type}
        if param.default is inspect.Parameter.empty:
            required.append(name)
    return {&quot;type&quot;: &quot;object&quot;, &quot;properties&quot;: props, &quot;required&quot;: required}
</code></pre>
<p><code>env</code> is dropped (it's the Odoo environment, not a tool arg). Each remaining
arg becomes a JSON Schema property. Type hints map to JSON types:
<code>int → integer</code>, <code>bool → boolean</code>, <code>float → number</code>, anything else
(including <code>str</code> and <code>dict</code>) defaults to <code>string</code>. Args without a default
are marked required.</p>
<p>The minimalism is deliberate. The LLM is given just enough type info to
generate a valid call; it doesn't need to know about Python's <code>Optional</code>
or <code>Union</code>. If a tool's signature needs richer JSON Schema (enums, nested
objects), pass an explicit <code>parameters=</code> to the decorator.</p>
<h3 id="4-registry_for_personapersona-tier_mask">4. <code>registry_for_persona(persona, tier_mask)</code></h3>
<p>The filter the registry endpoint applies:</p>
<pre><code class="language-python">def registry_for_persona(persona, tier_mask):
    allowed_tiers = set(tier_mask.split(&quot;+&quot;))
    return [
        {k: v for k, v in t.items() if k != &quot;fn&quot;}
        for t in TOOL_REGISTRY
        if persona in t[&quot;personas&quot;] and t[&quot;tier&quot;] in allowed_tiers
    ]
</code></pre>
<p>Two filters: persona must be in the tool's allowlist; tool's tier must
be in the JWT's tier mask. Strips the <code>fn</code> key (don't send the Python
function object across HTTP). Returns the filtered list to the sidecar.</p>
<p>Per spec § 4.3: <strong>forbidden tools don't exist</strong> in the LLM's function
definitions. The LLM is shown only the tools the caller can use. There's
no "you tried to call a forbidden tool" path; the LLM literally doesn't
know forbidden tools exist for this caller. This is by design — it
removes a class of prompt-injection attacks (you can't trick the model
into calling a tool it can't see).</p>
<h3 id="5-the-dispatch-controller-post-apihermestoolsslug">5. The dispatch controller (<code>POST /api/hermes/tools/&lt;slug&gt;</code>)</h3>
<p>Full flow in <code>controllers/hermes_tools_api.py:HermesToolsApiController.dispatch</code>:</p>
<pre><code class="language-python">@http.route(&quot;/api/hermes/tools/&lt;slug&gt;&quot;, type=&quot;http&quot;, auth=&quot;public&quot;,
            methods=[&quot;POST&quot;], csrf=False)
def dispatch(self, slug, **kw):
    claims = self._verify()                    # 1. JWT verify
    if isinstance(claims, http.Response):
        return claims                          # 401 / 503 short-circuit
    fn = decorator.get_tool_function(slug)
    if fn is None:
        return self._json({&quot;error&quot;: &quot;unknown_tool&quot;, ...}, status=404)
    meta = next(
        (t for t in decorator.TOOL_REGISTRY if t[&quot;slug&quot;] == slug), None)
    if claims[&quot;persona&quot;] not in meta[&quot;personas&quot;]:
        return self._json({&quot;error&quot;: &quot;not_allowed_for_persona&quot;, ...},
                          status=403)          # 2. persona check
    allowed_tiers = set(claims[&quot;tier&quot;].split(&quot;+&quot;))
    if meta[&quot;tier&quot;] not in allowed_tiers:
        return self._json({&quot;error&quot;: &quot;tier_required&quot;, ...}, status=403)
                                               # 3. tier check
    try:
        args = json.loads(request.httprequest.data or b&quot;{}&quot;)
    except json.JSONDecodeError:
        return self._json({&quot;error&quot;: &quot;invalid_json&quot;}, status=400)
    partner = request.env[&quot;res.partner&quot;].sudo().browse(claims[&quot;partner_id&quot;])
    if not partner.exists():
        return self._json({&quot;error&quot;: &quot;unknown_partner&quot;, ...}, status=403)
                                               # 4. partner exists
    user = partner.user_ids[:1]
    if not user:
        return self._json({&quot;error&quot;: &quot;partner_has_no_user&quot;, ...},
                          status=403)          # 5. partner has user
    env_with_user = request.env(user=user.id)
    # 6. Claim-bound args
    sig_params = inspect.signature(fn).parameters
    for arg_name in _CLAIM_BOUND_ARGS:
        if arg_name in sig_params:
            args[arg_name] = claims[arg_name]
    try:
        result = fn(env_with_user, **args)     # 7. Tool runs
    except Exception as e:
        return self._json({&quot;error&quot;: &quot;tool_exception&quot;, ...}, status=500)
    return self._json(result)
</code></pre>
<p>Six layered checks before the tool runs:</p>
<ol>
<li><strong>JWT verification</strong> — <code>_verify()</code> returns 401 if no bearer, 503 if
   not configured, 401 if invalid.</li>
<li><strong>Persona allowlist</strong> — tool's <code>personas</code> list must include the
   JWT's <code>persona</code> claim.</li>
<li><strong>Tier mask</strong> — tool's <code>tier</code> must be in the JWT's <code>tier</code> claim
   (<code>T0+T1+T2</code>).</li>
<li><strong>Partner exists</strong> — <code>claims["partner_id"]</code> must resolve to a real
   <code>res.partner</code> (delta § 14.3).</li>
<li><strong>Partner has user</strong> — the partner must have a linked <code>res.users</code>
   (delta § 14.3).</li>
<li><strong>Claim-bound args</strong> — <code>persona</code>, <code>partner_id</code>, <code>tenant</code> are
   overridden from claims (delta § 14.2).</li>
</ol>
<p>THEN the tool runs in <code>env(user=portal_user)</code> — so record rules in the
ORM see the right user identity, and Odoo's standard ACL applies inside
every <code>search()</code> / <code>browse()</code> / <code>write()</code> the tool performs.</p>
<h3 id="6-the-signature-binding-mechanic">6. The signature-binding mechanic</h3>
<p><code>inspect.signature(fn).parameters</code> walks the function's declared args.
The dispatch checks each of the three claim-bound names against the
signature:</p>
<pre><code class="language-python">sig_params = inspect.signature(fn).parameters
for arg_name in _CLAIM_BOUND_ARGS:
    if arg_name in sig_params:
        if arg_name == &quot;persona&quot;:
            args[&quot;persona&quot;] = claims[&quot;persona&quot;]
        elif arg_name == &quot;partner_id&quot;:
            args[&quot;partner_id&quot;] = claims[&quot;partner_id&quot;]
        elif arg_name == &quot;tenant&quot;:
            args[&quot;tenant&quot;] = claims[&quot;tenant&quot;]
</code></pre>
<p>So:
- A tool that declares <code>persona: str</code> in its signature gets <code>persona</code>
  overridden from the JWT, no matter what the LLM sent.
- A tool that doesn't declare <code>persona</code> doesn't get it injected — and
  if the LLM sent <code>persona="foo"</code> in the body, that ends up in
  <code>**kwargs</code> if the tool accepts arbitrary kwargs, or raises a
  <code>TypeError</code> if not.</p>
<p>Tools that don't need persona scoping just don't declare the arg. Tools
that do (like <code>propose_recommendation</code>) declare it and use it for guard
logic.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<h3 id="adding-a-new-tool">Adding a new tool</h3>
<ol>
<li>Pick a clear, kebab-case Python function name (becomes the slug).
   It will be visible to the LLM as <code>southbrook__&lt;slug&gt;</code>.</li>
<li>Pick the file. Read tools live in <code>tools/read_tools.py</code>, write tools
   in <code>tools/write_tools.py</code>. There's no enforcement; convention only.</li>
<li>Write the function:</li>
</ol>
<pre><code class="language-python">@hermes_tool(
    personas=[&quot;trade_partner&quot;, &quot;sales_rep&quot;],
    tier=&quot;T0&quot;,
    scope=&quot;own_order&quot;,
    description=&quot;Return the latest CAD drawings URL for an order.&quot;,
)
def get_drawings_url(env, order_id: int):
    order = env[&quot;sale.order&quot;].browse(order_id)
    order.check_access_rights(&quot;read&quot;)
    order.check_access_rule(&quot;read&quot;)
    return {&quot;drawings_url&quot;: order.drawings_url, &quot;as_of&quot;: str(fields.Datetime.now())}
</code></pre>
<ol start="4">
<li>Always include both <code>check_access_rights</code> and <code>check_access_rule</code> —
   the latter applies record rules (delta § 14.5 added this where it
   was missing). Don't use <code>sudo()</code> unless you have a documented
   reason (spec § 4.4 rule 2).</li>
<li>Add to <code>tools/__init__.py</code> so the decorator runs at import.</li>
<li>Restart Odoo. Verify with <code>python3 -c "from odoo.addons.southbrook_hermes.tools.decorator import TOOL_REGISTRY; print([t['slug'] for t in TOOL_REGISTRY])"</code> in a shell, or by hitting <code>GET /api/hermes/tools</code> with a fresh JWT.</li>
<li>Redeploy the sidecar (<code>vercel deploy --prod</code>) so its cached registry
   refreshes.</li>
</ol>
<h3 id="debugging-the-llm-didnt-call-my-tool">Debugging "the LLM didn't call my tool"</h3>
<p>Three likeliest causes:</p>
<ul>
<li><strong>Registry cache.</strong> The sidecar caches the registry per tenant ×
  persona × tier. Until the next sidecar cold start (or you redeploy),
  the LLM sees the old list. Force a cold start: <code>vercel deploy --prod</code>.</li>
<li><strong>Persona / tier filter.</strong> Verify your tool's <code>personas</code> includes the
  caller's persona, and <code>tier</code> is in the caller's mask. Hit
  <code>GET /api/hermes/tools</code> with the actual caller's JWT and check if
  your tool is in the response.</li>
<li><strong>Description.</strong> The LLM decides which tool to call based on the
  <code>description</code>. A vague description (<code>"do stuff"</code>) won't trigger calls
  even if the tool is in the registry. Rewrite to be specific about
  inputs, outputs, and the situation that warrants calling.</li>
</ul>
<h3 id="debugging-the-tool-got-called-but-the-result-is-empty">Debugging "the tool got called but the result is empty"</h3>
<p>This is almost always record-rule scope. Tools run in
<code>env(user=portal_user)</code>; if the partner's record rules don't grant
read access to the queried records, the search returns an empty
recordset. The classic case: a tool reads <code>mrp.production</code> records, and
trade partners have no record rule giving them access to
<code>mrp.production</code>. The tool returns <code>[]</code> — no error, no warning, just
silence. Fix: either widen the record rule, or scope the tool through
a partner-visible model (e.g. via <code>sale.order</code> → <code>order_line.move_ids</code> →
production).</p>
<h3 id="debugging-503-hermes_not_configured-on-apihermestools">Debugging "503 hermes_not_configured" on /api/hermes/tools</h3>
<p>The JWT secret is unset (or still the placeholder). See lesson 13.3.</p>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I added a tool but <code>GET /api/hermes/tools</code> doesn't show it."</strong></p>
<p>The <code>tools/__init__.py</code> import didn't pick up your file. The decorator
runs when the module is imported, which happens at Odoo startup. If
your file is <code>tools/my_tool.py</code> and <code>tools/__init__.py</code> doesn't import
it, the decorator never runs. Add an import statement (or <code>from . import
*</code>).</p>
<p><strong>"The dispatch returns 403 <code>tier_required</code> for my T0 tool."</strong></p>
<p>The JWT's <code>tier</code> claim isn't <code>T0+T1+T2</code>. Either persona resolution is
wrong (lesson 13.3) or someone has overridden the default tier mask.
Decode the JWT (jwt.io with the secret) and inspect.</p>
<p><strong>"My tool function uses <code>partner_id</code> from the args, but the LLM keeps
sending the wrong one."</strong></p>
<p>You're hitting the right pattern but trusting the wrong source. Even
though dispatch overrides <code>partner_id</code> from the JWT (delta § 14.2 +
§ 14.4), it's safer to ignore the arg and read
<code>env.user.partner_id.id</code> directly — that's <em>always</em> the verified
caller. <code>list_my_kitchen_projects</code> and <code>list_my_recommendations</code> show
this pattern (delta § 14.4 rewrote them to ignore the arg).</p>
<p><strong>"I want to return rich data structures (datetimes, recordsets) from a
tool."</strong></p>
<p>The dispatch JSON-encodes the return value with <code>json.dumps(result,
default=str)</code>. Datetimes serialize to ISO strings via <code>default=str</code>.
Recordsets serialize to their <code>__repr__</code> (not what you want). For
recordsets, convert to a list of dicts inside the tool. For complex
types, build a plain dict.</p>
<p><strong>"My tool raises <code>UserError</code> because the partner can't access the
record. The LLM gets a 500."</strong></p>
<p>Yes — dispatch's outer <code>try/except</code> catches all exceptions and returns
500 <code>tool_exception</code> with the exception message. That's the spec's
"graceful surrender" path — the LLM sees the error result and either
recovers (calls a different tool) or surrenders ("I can't reach that
data right now"). If you want a cleaner result, catch the exception
inside the tool and return <code>{"error": "...", "code": "..."}</code> instead
of raising. Compare <code>list_my_kitchen_projects</code> (returns <code>[]</code> if the
model doesn't exist) vs <code>get_kitchen_project</code> (raises <code>MissingError</code>).</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<h3 id="why-a-process-level-list-not-a-database-table">Why a process-level list, not a database table</h3>
<p>The registry is rebuilt from source on every Odoo restart. There's no
state to lose. Putting it in a database table would require migrations
on every tool change and would add a query for every registry lookup.
A Python list is simpler, faster, and the source of truth is the
decorated function — exactly where a developer expects.</p>
<h3 id="why-the-function-arg-parametersnone-exists">Why the function arg <code>parameters=None</code> exists</h3>
<p>For most tools, the auto-extracted schema from <code>inspect.signature</code> is
sufficient. For tools with enums (e.g. <code>recommendation_type</code> ∈
{task/risk/note/followup}) or nested objects (e.g. <code>payload: dict</code>
with required keys), you want richer JSON Schema. The decorator
accepts an explicit override:</p>
<pre><code class="language-python">@hermes_tool(
    personas=[&quot;trade_partner&quot;],
    tier=&quot;T2&quot;,
    scope=&quot;own&quot;,
    description=&quot;...&quot;,
    parameters={
        &quot;type&quot;: &quot;object&quot;,
        &quot;properties&quot;: {
            &quot;intent&quot;: {
                &quot;type&quot;: &quot;string&quot;,
                &quot;enum&quot;: [&quot;request_revision&quot;, &quot;request_install_reschedule&quot;,
                         &quot;request_clarification&quot;]
            },
            &quot;payload&quot;: {&quot;type&quot;: &quot;object&quot;},
            &quot;summary&quot;: {&quot;type&quot;: &quot;string&quot;},
        },
        &quot;required&quot;: [&quot;intent&quot;, &quot;payload&quot;, &quot;summary&quot;],
    },
)
def propose_recommendation(env, intent, payload, summary, ...):
    ...
</code></pre>
<p>Currently <code>propose_recommendation</code> ships with auto-extracted schema —
the explicit override would be a future polish (would help the LLM
emit a valid <code>intent</code> without trial-and-error).</p>
<h3 id="why-the-dispatch-returns-the-result-raw-not-wrapped">Why the dispatch returns the result raw, not wrapped</h3>
<pre><code class="language-python">return self._json(result)
</code></pre>
<p>Where <code>result</code> is whatever the tool returned. The sidecar gets the raw
dict (or list) as the tool result and passes it to the LLM as the
function-call result. There's no <code>{"ok": true, "result": ...}</code> envelope.
Errors ARE wrapped (<code>{"error": ..., "code": ..., "detail": ...}</code>) so the
sidecar can distinguish — but successful results are raw, matching the
sidecar's <code>dispatch(slug, args, jwt)</code> contract.</p>
<h3 id="the-qualified-slug-why-prefix-with-tenant">The qualified slug — why prefix with tenant</h3>
<p><code>qualified_slug = f"southbrook__{slug}"</code> is the per-tenant prefix from
spec § 4.4 rule 3. When v1.x adds a second tenant (Porterly), each
tenant's tools register as <code>porterly__list_my_orders</code>,
<code>southbrook__list_my_orders</code>. The sidecar uses the qualified name in
the LLM's function definitions so a tool call routed to the wrong
tenant would mismatch and 404. Today, with one tenant, the prefix is
inert — but it's wired in so v1.x doesn't need a migration.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> You add a tool decorated <code>@hermes_tool(personas=["sales_rep"],
tier="T1", scope="own_order", description="…")</code>. A trade partner
chats with Fabio and asks something only this new tool can answer.
Why doesn't the LLM call it?</p>
<blockquote>
<p>Because the LLM doesn't know it exists. Trade partners hitting
<code>GET /api/hermes/tools</code> get a filtered list that includes only tools
whose <code>personas</code> includes <code>"trade_partner"</code>. Your tool's <code>personas</code>
is <code>["sales_rep"]</code> only, so the trade partner sees no tool with this
capability and the LLM doesn't generate a call for it. To fix: add
<code>"trade_partner"</code> to the personas list — assuming the tool's scope
actually permits trade-partner use.</p>
</blockquote>
<p><strong>2.</strong> You add a tool and verify it shows up in <code>TOOL_REGISTRY</code> via
the Odoo shell, but <code>GET /api/hermes/tools</code> returns the OLD list. The
JWT is fresh. What's the most likely cause?</p>
<blockquote>
<p>The Odoo container has the new code; the sidecar has cached the
registry from an earlier call. The sidecar caches per
<code>tenant::persona::tier</code> (delta § 14.8); cache survives cold-start
warmups but not actual cold starts. Force a sidecar redeploy:
<code>vercel deploy --prod</code>. Alternatively, the Odoo container hasn't
actually reloaded the addon — <code>docker restart southbrook-odoo</code> or
<code>-u southbrook_hermes</code> would also be required.</p>
</blockquote>
<p><strong>3.</strong> A tool function is declared <code>def my_tool(env, order_id: int,
persona: str)</code>. Dispatch is called with body <code>{"order_id": 42,
"persona": "mfg_manager"}</code>. What value does the function see for
<code>persona</code>?</p>
<blockquote>
<p>The JWT's verified persona claim, NOT <code>"mfg_manager"</code>. Dispatch
inspects the function signature, sees that <code>persona</code> is declared,
and overrides the request-body value with <code>claims["persona"]</code> before
calling. This is delta § 14.2 — closes the prompt-injection
self-attribution hole.</p>
</blockquote>
<p><strong>4.</strong> A new developer writes a read tool that uses <code>.sudo()</code> to fetch
records "because the test wasn't working otherwise." Code review
flags this. Why is <code>.sudo()</code> banned in tools by default?</p>
<blockquote>
<p>Spec § 4.4 rule 2. Record rules are how Odoo enforces who can read
what; <code>.sudo()</code> bypasses them. If a tool uses <code>.sudo()</code>, a trade
partner could read data belonging to other partners, violating the
persona ACL boundary. The only allowed exception is "explicitly
tier-gated T2 tools with a documented reason" — for example,
<code>propose_recommendation</code> uses <code>.sudo()</code> to create the recommendation
because draft recs are owned by the Fabio partner, not the
requester, and reviewers need to see them regardless of who proposed.</p>
</blockquote>
<p><strong>5.</strong> You're debugging a 403 <code>partner_has_no_user</code> response from
dispatch. What does this mean, and how do you fix it?</p>
<blockquote>
<p>The JWT's <code>partner_id</code> claim resolved to a real <code>res.partner</code>, but
that partner has no linked <code>res.users</code> record. Dispatch refuses to
fall back to <code>request.env.user</code> (the public user on this
<code>auth='public'</code> route) because that would run every tool with the
least-privileged identity (delta § 14.3). Fix: open the partner
record in Odoo → click "Grant portal access" → confirm. The newly
created portal user gets linked via <code>res.partner.user_ids</code> and the
next tool call dispatches cleanly.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The 9 read tools individually, with their scoping detail —
  lesson 13.5.</li>
<li>The 4 write tools and the T0/T1/T2 tier model — lesson 13.6.</li>
<li>JWT mint / verify / persona resolution internals — lesson 13.3.</li>
<li>The OWL chat panel and sidecar streaming wire — lesson 13.7.</li>
<li>The recommendation queue side effects (what happens after
  <code>propose_recommendation</code> succeeds) — lesson 13.2.</li>
<li>The Fabio recommendation approval flow — lesson 3.2.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_read_tools" model="slide.slide">
            <field name="name">The Nine Read Tools — Source, Scope, and ACL</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">50</field>
            <field name="description">Developer working in `tools/read_tools.py`; power-user / admin who needs to know what data Fabio can see for each persona.</field>
            <field name="html_content"><![CDATA[<h1 id="the-nine-read-tools-source-scope-and-acl">The Nine Read Tools — Source, Scope, and ACL</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the developer about to modify one of the nine read tools, or
add a tenth. You're the admin trying to answer "what can a trade
partner see through Fabio?" with precision. You're the auditor reading
the source and wanting an inline guide to each function.</p>
<p>Every read tool ships in <code>addons/southbrook_hermes/tools/read_tools.py</code>.
This lesson walks through each one in registration order, with the
persona allowlist, the data returned, the ACL boundary, an example
invocation, and the common confusions.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>You don't see the read tools on a screen — they're invoked by the LLM
through <code>POST /api/hermes/tools/&lt;slug&gt;</code>. But the registry that
declares them is the canonical inventory, browsable two ways:</p>
<blockquote>
<p><strong><code>GET /api/hermes/tools</code></strong> — with a fresh JWT (from <code>scripts/smoke_hermes.sh</code>),
returns the list filtered to your persona. The read tools live in
the first nine entries.</p>
<p><strong><code>odoo-bin shell</code> → <code>from odoo.addons.southbrook_hermes.tools.decorator import TOOL_REGISTRY; [t['slug'] for t in TOOL_REGISTRY if t['tier'] == 'T0' and t['slug'].startswith(('list_', 'get_'))]</code></strong></p>
</blockquote>
<p>The source file is the authoritative spec; the registry is the runtime
reflection. They never drift because the runtime is built from the
source at import time.</p>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<p>Nine read tools, all <code>tier="T0"</code>. By personas + scope:</p>
<table>
<thead>
<tr>
<th>#</th>
<th>Slug</th>
<th>Personas</th>
<th>Scope</th>
<th>Returns</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>list_my_orders</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own</code></td>
<td>List of orders visible to caller's partner</td>
</tr>
<tr>
<td>2</td>
<td><code>get_order_status</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own_order</code></td>
<td>Production status of one order</td>
</tr>
<tr>
<td>3</td>
<td><code>get_order_line</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own_order</code></td>
<td>Configured detail of one order line</td>
</tr>
<tr>
<td>4</td>
<td><code>list_my_kitchen_projects</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own</code></td>
<td>List of kitchen projects</td>
</tr>
<tr>
<td>5</td>
<td><code>get_kitchen_project</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own_order</code></td>
<td>Options + approval status of one project</td>
</tr>
<tr>
<td>6</td>
<td><code>get_install_schedule</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own_order</code></td>
<td>Install date + risk flag</td>
</tr>
<tr>
<td>7</td>
<td><code>get_quote_pdf_url</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own_order</code></td>
<td>Quote PDF URL + expiry</td>
</tr>
<tr>
<td>8</td>
<td><code>list_my_recommendations</code></td>
<td>trade_partner, sales_rep</td>
<td><code>own</code></td>
<td>Open recommendations attached to caller's partner</td>
</tr>
<tr>
<td>9</td>
<td><code>get_os_section</code></td>
<td>trade_partner, sales_rep, mfg_manager</td>
<td><code>global</code></td>
<td>One named OS section's body + version</td>
</tr>
</tbody>
</table>
<p>Read tools 1–8 are partner-scoped (own or own_order). Read tool 9
(<code>get_os_section</code>) is global — reading the Southbrook OS doesn't depend
on persona. Sales reps and trade partners both get the same eight
partner-scoped tools; sales reps in v1.0 don't have any extra read tools
yet (v1.1 will add their backend-specific surface).</p>
<h3 id="tool-1-list_my_orderspartner_id-int">Tool 1 — <code>list_my_orders(partner_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own</code>.</p>
<pre><code class="language-python">def list_my_orders(env, partner_id: int):
    orders = env[&quot;sale.order&quot;].search(
        [(&quot;partner_id&quot;, &quot;=&quot;, partner_id)], order=&quot;date_order desc&quot;, limit=50)
    return [
        {
            &quot;ref&quot;: o.name,
            &quot;stage&quot;: o.state,
            &quot;partner_name&quot;: o.partner_id.name,
            &quot;install_due&quot;: (o.commitment_date.isoformat()
                            if o.commitment_date else None),
        }
        for o in orders
    ]
</code></pre>
<p>Note: <code>partner_id</code> is passed in but the actual ACL is the record-rule
scope on <code>sale.order</code> running as <code>env.user</code> (the portal user resolved by
dispatch). A trade partner whose <code>partner_id.id == 42</code> calling with
<code>partner_id=42</code> gets their own orders. Calling with <code>partner_id=99</code> gets
<strong>nothing</strong> because the record rule denies access — even though the
search domain says <code>("partner_id", "=", 99)</code>, the underlying query is
ANDed with the record rule. This is the spec § 4.3 pattern: ACL is the
tool registry + record rules. Note <code>list_my_kitchen_projects</code> and
<code>list_my_recommendations</code> go one step further (delta § 14.4) and ignore
the <code>partner_id</code> arg entirely, keying off <code>env.user.partner_id.id</code>. For
<code>list_my_orders</code>, the record-rule defense is sufficient.</p>
<p>Example invocation (sidecar perspective):</p>
<pre><code class="language-http">POST /api/hermes/tools/list_my_orders HTTP/1.1
Authorization: Bearer &lt;JWT with partner_id=42, persona=trade_partner&gt;
Content-Type: application/json

{&quot;partner_id&quot;: 42}
</code></pre>
<p>Returns:</p>
<pre><code class="language-json">[
  {&quot;ref&quot;: &quot;S00235&quot;, &quot;stage&quot;: &quot;sale&quot;, &quot;partner_name&quot;: &quot;Smith Cabinetry&quot;,
   &quot;install_due&quot;: &quot;2026-07-15&quot;},
  ...
]
</code></pre>
<p>Common confusions:
- "Why does the tool need <code>partner_id</code> if the ACL doesn't use it?"
  Because the dispatch's claim-binding (delta § 14.2) writes
  <code>partner_id</code> from the JWT, so the tool is <em>guaranteed</em> to see the
  verified caller's partner. The record rule then filters. Belt and
  braces.
- "Why a <code>limit=50</code>?" Soft cap so the result fits in the LLM's context.
  An LLM seeing 200 orders is going to summarize unhelpfully. 50 is
  the v1.0 ceiling.</p>
<h3 id="tool-2-get_order_statusorder_id-int">Tool 2 — <code>get_order_status(order_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own_order</code>.</p>
<p>The richest of the read tools. Returns:
- <code>stage</code> (the order's <code>state</code>)
- <code>mos</code> (count of <code>mrp.production</code> records linked via order_line)
- <code>bottleneck</code> (current bottleneck work center name, if a
  <code>sb.kitchen.project</code> task exists for the order)
- <code>blocker</code> (top blocker text from the kitchen project task)
- <code>next_action</code> (next-best-action text)
- <code>install_due</code> (the kitchen project's <code>date_deadline</code>)
- <code>readiness_score</code> (the readiness score from the project task)
- <code>version</code> (the order's <code>southbrook_version</code>, defaulting to 1)</p>
<p>Full implementation:</p>
<pre><code class="language-python">def get_order_status(env, order_id: int):
    order = env[&quot;sale.order&quot;].browse(order_id)
    try:
        order.check_access_rights(&quot;read&quot;)
        order.check_access_rule(&quot;read&quot;)
    except Exception:
        raise MissingError(&quot;Order not visible to this user.&quot;)
    kj = env[&quot;project.task&quot;].search(
        [(&quot;sale_order_id&quot;, &quot;=&quot;, order.id)], limit=1)
    return {
        &quot;stage&quot;: order.state,
        &quot;mos&quot;: _count_mos_for_order(env, order),
        &quot;bottleneck&quot;: (kj.southbrook_current_bottleneck_wc.name
                       if kj and getattr(kj, &quot;southbrook_current_bottleneck_wc&quot;, False)
                       else None),
        &quot;blocker&quot;: (kj.southbrook_top_blocker if kj else None),
        &quot;next_action&quot;: (kj.southbrook_next_best_action if kj else None),
        &quot;install_due&quot;: (kj.date_deadline.isoformat()
                        if kj and kj.date_deadline else None),
        &quot;readiness_score&quot;: (kj.southbrook_readiness_score if kj else None),
        &quot;version&quot;: getattr(order, &quot;southbrook_version&quot;, 1),
    }
</code></pre>
<p>ACL boundary:
- <code>order.check_access_rights("read")</code> + <code>order.check_access_rule("read")</code>
  raise if the partner can't see this order.
- <code>project.task</code> + <code>mrp.production</code> are searched <strong>without sudo</strong>, so
  the partner's record rules apply. If a record rule hides them (e.g.
  MOs are internal-only by default), the calls degrade to empty results
  and the return dict surfaces <code>None</code> — exactly what spec § 4.4
  ACL rule wants.</p>
<p>Example invocation:</p>
<pre><code class="language-json">{&quot;order_id&quot;: 235}
</code></pre>
<p>Returns:</p>
<pre><code class="language-json">{
  &quot;stage&quot;: &quot;sale&quot;,
  &quot;mos&quot;: 3,
  &quot;bottleneck&quot;: &quot;SB-EDGE&quot;,
  &quot;blocker&quot;: &quot;Door vendor lead time slipped 2 weeks.&quot;,
  &quot;next_action&quot;: &quot;Confirm replacement door SKU with planner.&quot;,
  &quot;install_due&quot;: &quot;2026-07-15&quot;,
  &quot;readiness_score&quot;: 0.62,
  &quot;version&quot;: 1
}
</code></pre>
<p>Common confusions:
- "Why does <code>mos</code> use <code>sale_order_line_id</code>, not <code>sale_id</code>?" Because
  Odoo's <code>mrp.production.sale_order_line_id</code> is the canonical link to a
  sale line (one MO per line); <code>sale_id</code> is a denormalized convenience.
- "Why <code>getattr(kj, 'southbrook_current_bottleneck_wc', False)</code>?"
  Defensive — the kitchen-ops fields are added by
  <code>southbrook_kitchen_workspace</code>. If that addon is uninstalled, the
  tool degrades to None rather than crashing.</p>
<h3 id="tool-3-get_order_lineorder_id-int-line_id-int">Tool 3 — <code>get_order_line(order_id: int, line_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own_order</code>.</p>
<p>Returns the configured detail of a single sale order line: SKU,
variant name, qty, attribute key-value dict, retail price, channel
price, flags list.</p>
<pre><code class="language-python">def get_order_line(env, order_id: int, line_id: int):
    order = env[&quot;sale.order&quot;].browse(order_id)
    try:
        order.check_access_rights(&quot;read&quot;)
        order.check_access_rule(&quot;read&quot;)
    except Exception:
        raise MissingError(&quot;Order not visible to this user.&quot;)
    line = order.order_line.filtered(lambda l: l.id == line_id)
    if not line:
        raise UserError(
            f&quot;Line {line_id} does not belong to order {order.name} &quot;
            f&quot;or is not visible to this user.&quot;)
    return {
        &quot;sku&quot;: line.product_id.default_code or &quot;&quot;,
        &quot;variant_name&quot;: line.product_id.name,
        &quot;qty&quot;: line.product_uom_qty,
        &quot;attributes&quot;: {
            v.attribute_id.name: v.name
            for v in line.product_id.product_template_attribute_value_ids
        },
        &quot;retail&quot;: line.price_unit,
        &quot;channel&quot;: line.price_subtotal,
        &quot;flags&quot;: [],
    }

</code></pre>
<p>ACL boundary: order-level access check, then the <code>.filtered()</code> on
<code>order.order_line</code> is implicit record-rule (you can only see lines on
orders you can see). If <code>line_id</code> doesn't belong to the order or isn't
visible, raises <code>UserError</code> — distinguishable from "order not visible"
which raises <code>MissingError</code>.</p>
<p>Example invocation:</p>
<pre><code class="language-json">{&quot;order_id&quot;: 235, &quot;line_id&quot;: 5891}
</code></pre>
<p>Returns:</p>
<pre><code class="language-json">{
  &quot;sku&quot;: &quot;WALL-2DR-600-WHT&quot;,
  &quot;variant_name&quot;: &quot;Wall Cabinet 2-Door 600mm White Shaker&quot;,
  &quot;qty&quot;: 4.0,
  &quot;attributes&quot;: {
    &quot;Family&quot;: &quot;Wall Cabinet&quot;,
    &quot;Width&quot;: &quot;600mm&quot;,
    &quot;Series&quot;: &quot;Contemporary&quot;,
    &quot;Door Style&quot;: &quot;Shaker&quot;,
    ...
  },
  &quot;retail&quot;: 487.50,
  &quot;channel&quot;: 1267.50
}
</code></pre>
<p>Common confusions:
- The <code>attributes</code> dict is built from <code>product_template_attribute_value_ids</code>
  — the values that distinguish this <em>variant</em>. Not from
  <code>product.config.session</code> (the configurator's transient state).
- The <code>flags</code> list is always empty in v1.0. Reserved for "out of stock,"
  "discontinued," "stale price" warnings; never populated yet.
- "Retail" is <code>price_unit</code> (per-unit list price); "channel" is
  <code>price_subtotal</code> (qty × unit price with channel discount applied).
  The names are confusing on purpose — they mirror the Estimating brief's
  "retail vs channel" framing.</p>
<h3 id="tool-4-list_my_kitchen_projectspartner_id-int-none">Tool 4 — <code>list_my_kitchen_projects(partner_id: int = None)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own</code>.</p>
<pre><code class="language-python">def list_my_kitchen_projects(env, partner_id: int = None):
    # partner_id is informational only — the authoritative scope comes from
    # the caller's env.user (set by the dispatch controller from JWT claims),
    # so an LLM arg can NOT widen the result set to another partner.
    Project = env[&quot;sb.kitchen.project&quot;] if &quot;sb.kitchen.project&quot; in env else None
    if Project is None:
        return []
    projects = Project.search(
        [(&quot;partner_id&quot;, &quot;=&quot;, env.user.partner_id.id)], order=&quot;create_date desc&quot;)
    return [
        {
            &quot;ref&quot;: p.name,
            &quot;stage&quot;: getattr(p, &quot;state&quot;, None),
            &quot;option_count&quot;: len(p.option_ids) if hasattr(p, &quot;option_ids&quot;) else 0,
            &quot;selected&quot;: next(
                (o.name for o in getattr(p, &quot;option_ids&quot;, [])
                 if getattr(o, &quot;is_selected&quot;, False)), None),
        }
        for p in projects
    ]
</code></pre>
<p>This is one of the two tools rewritten in delta § 14.4 to ignore the
<code>partner_id</code> arg and key off <code>env.user.partner_id.id</code> directly. The arg
is preserved in the signature for backward compatibility and so the
JSON Schema declares it (and so claim-binding still hits it — see lesson
13.4), but the body of the function never reads it.</p>
<p>Returns a list of kitchen projects with stage, option count, and the
name of the currently selected option (if any).</p>
<p>Common confusions:
- "Why <code>if 'sb.kitchen.project' in env</code>?" Defensive — if
  <code>southbrook_kitchen_workspace</code> isn't installed, the model doesn't
  exist. The tool degrades to <code>[]</code> instead of crashing.
- "Why is <code>state</code> accessed with <code>getattr</code>?" Same — the kitchen project
  model might not have the field in every install state.
- "Why don't we use <code>check_access_rights</code> here?" Because the search is
  already scoped to <code>env.user.partner_id</code> — a partner querying their
  own projects. Record rules then filter further if needed. The
  pattern is "explicit scope + record rules" not "explicit access
  check."</p>
<h3 id="tool-5-get_kitchen_projectproject_id-int">Tool 5 — <code>get_kitchen_project(project_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own_order</code>.</p>
<p>Returns one project's options list (each with <code>name</code> + <code>is_selected</code>),
the project's <code>approval_status</code>, and a <code>drawings_url</code>. Full access
check pattern:</p>
<pre><code class="language-python">project.check_access_rights(&quot;read&quot;)
project.check_access_rule(&quot;read&quot;)
</code></pre>
<p>Both checks present; raises <code>MissingError("Project not visible to this
user.")</code> on either failure. The two failure modes are model-level ACL
(can the user read the <em>model</em>?) and record-rule (can this user read
<em>this record</em>?). Production-side wisdom: prod users almost always have
model-level read; the record-rule check is the one that actually fires
when a trade partner asks about a kitchen project belonging to a
different partner.</p>
<p>Common confusions:
- The returned <code>options</code> list comes from <code>getattr(project,
  "option_ids", [])</code> — defensive against the field not existing.
- <code>approval_status</code> and <code>drawings_url</code> are read with <code>getattr</code> and
  default to None — these may not exist in every install.</p>
<h3 id="tool-6-get_install_scheduleorder_id-int">Tool 6 — <code>get_install_schedule(order_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own_order</code>.</p>
<p>Returns the order's <code>commitment_date</code>, <code>delivery_status</code> (Odoo native
field), and risk flag + reason. As of v1.0, <code>risk_flag</code> is always
<code>"unknown"</code> and <code>risk_reason</code> is <code>None</code>:</p>
<pre><code class="language-python">return {
    &quot;date&quot;: (order.commitment_date.isoformat()
             if order.commitment_date else None),
    &quot;dispatch&quot;: getattr(order, &quot;delivery_status&quot;, None),
    &quot;risk_flag&quot;: &quot;unknown&quot;,
    &quot;risk_reason&quot;: None,
}
</code></pre>
<p>The risk fields are reserved for v1.1 — the manufacturing intelligence
side hasn't been wired into the read path yet. The tool answers the
schedule question; "is it at risk?" gets a placeholder.</p>
<p>Delta § 14.5 added <code>check_access_rule</code> here. Originally only
<code>check_access_rights</code> was called.</p>
<p>Common confusions:
- "Why doesn't this tool read the kitchen project task's
  <code>date_deadline</code> like <code>get_order_status</code> does?" Because the spec
  groups install date with sale order's <code>commitment_date</code>. If the
  customer-facing date differs from the planned production date, we
  show the commitment date (what the partner was promised).</p>
<h3 id="tool-7-get_quote_pdf_urlorder_id-int">Tool 7 — <code>get_quote_pdf_url(order_id: int)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own_order</code>.</p>
<pre><code class="language-python">def get_quote_pdf_url(env, order_id: int):
    order = env[&quot;sale.order&quot;].browse(order_id)
    try:
        order.check_access_rights(&quot;read&quot;)
        order.check_access_rule(&quot;read&quot;)
    except Exception:
        raise MissingError(&quot;Order not visible to this user.&quot;)
    base_url = env[&quot;ir.config_parameter&quot;].sudo().get_param(&quot;web.base.url&quot;, &quot;&quot;)
    return {
        &quot;pdf_url&quot;: (f&quot;{base_url}/my/orders/{order.id}?report_type=pdf&quot;
                    if base_url else None),
        &quot;valid_until&quot;: (order.validity_date.isoformat()
                        if order.validity_date else None),
    }
</code></pre>
<p>Builds the URL from the Odoo native portal report path. Returns
<code>valid_until</code> from <code>order.validity_date</code>. Note the <code>.sudo()</code> on
<code>ir.config_parameter</code> — this is reading a global system parameter
(<code>web.base.url</code>), not partner data, so it's allowed under spec § 4.4
rule 2.</p>
<p>Common confusions:
- The URL is the <em>portal</em> report path, not the backend report path —
  so a trade partner clicking it gets the customer-facing PDF, not the
  internal "Order Builder" PDF.
- <code>valid_until</code> may be None for confirmed orders (<code>validity_date</code>
  applies to quotations, not sales orders).</p>
<h3 id="tool-8-list_my_recommendationspartner_id-int-none">Tool 8 — <code>list_my_recommendations(partner_id: int = None)</code></h3>
<p>Personas: <code>trade_partner, sales_rep</code>. Tier: T0. Scope: <code>own</code>.</p>
<pre><code class="language-python">def list_my_recommendations(env, partner_id: int = None):
    Rec = env[&quot;southbrook.hermes.recommendation&quot;] if (
        &quot;southbrook.hermes.recommendation&quot; in env) else None
    if Rec is None:
        return []
    recs = Rec.search([
        (&quot;source_model&quot;, &quot;=&quot;, &quot;res.partner&quot;),
        (&quot;source_res_id&quot;, &quot;=&quot;, env.user.partner_id.id),
        (&quot;state&quot;, &quot;in&quot;, (&quot;draft&quot;, &quot;ready&quot;)),
    ], order=&quot;create_date desc&quot;)
    return [
        {
            &quot;rec_id&quot;: r.id,
            &quot;type&quot;: r.recommendation_type,
            &quot;summary&quot;: r.summary or &quot;&quot;,
            &quot;state&quot;: r.state,
        }
        for r in recs
    ]
</code></pre>
<p>The other delta § 14.4 tool. Like <code>list_my_kitchen_projects</code>, ignores
<code>partner_id</code> arg, keys off <code>env.user.partner_id.id</code>. Filters to
<code>source_model='res.partner'</code> (i.e. partner-scoped recommendations) AND
<code>source_res_id=&lt;caller&gt;</code> AND <code>state in ('draft', 'ready')</code> (only what's
still in the open queue).</p>
<p>Note this doesn't include <code>approved</code> or <code>applied</code> recommendations.
Trade-partner-facing view is "what's pending review on my behalf" —
once approved or rejected, it falls off the partner's radar (but
remains visible to reviewers in the Fabio menu — different surface,
same database).</p>
<h3 id="tool-9-get_os_sectionslug-str">Tool 9 — <code>get_os_section(slug: str)</code></h3>
<p>Personas: <code>trade_partner, sales_rep, mfg_manager</code>. Tier: T0. Scope:
<code>global</code>.</p>
<pre><code class="language-python">def get_os_section(env, slug: str):
    Section = env[&quot;southbrook.os.section&quot;].sudo()
    section = Section.search([(&quot;slug&quot;, &quot;=&quot;, slug)], limit=1)
    if not section:
        raise MissingError(f&quot;OS section '{slug}' not found.&quot;)
    return {
        &quot;slug&quot;: section.slug,
        &quot;name&quot;: section.name,
        &quot;version&quot;: section.version,
        &quot;source&quot;: section.source,
        &quot;body&quot;: section.body,
    }
</code></pre>
<p>The only read tool available to all three personas — every persona can
read the Southbrook OS. The only read tool that uses <code>.sudo()</code> — this
is the explicit § 4.4 carve-out for "global, partner-independent
reads." There's no record rule on <code>southbrook.os.section</code> that varies
by user; the OS is the same for everyone.</p>
<p>Example invocation:</p>
<pre><code class="language-json">{&quot;slug&quot;: &quot;07_partner_faq&quot;}
</code></pre>
<p>Returns:</p>
<pre><code class="language-json">{
  &quot;slug&quot;: &quot;07_partner_faq&quot;,
  &quot;name&quot;: &quot;Partner FAQ&quot;,
  &quot;version&quot;: 3,
  &quot;source&quot;: &quot;canonical&quot;,
  &quot;body&quot;: &quot;# Partner FAQ\n\n## 1. Where is my kitchen?\n...&quot;
}
</code></pre>
<p>Common confusions:
- "Why is <code>slug</code> a Char rather than a Selection?" Because the LLM
  picks the slug from RAG context — having a fixed list constrains
  the LLM but adds rigidity. The trade-off was: free-form slug,
  raise <code>MissingError</code> if not found, and the LLM recovers.
- "Why <code>.sudo()</code>?" The section model has no partner scope; the OS is
  global. The sudo is a deliberate § 4.4 exemption with a documented
  reason. Don't replicate this pattern in partner-scoped tools.
- "Why doesn't the OS section model have a <code>published</code> flag?" It
  effectively does via <code>southbrook.os.publication</code> (lesson 13.1) — but
  <code>get_os_section</code> returns the current (latest) section, not a
  publication snapshot. v1.0 trade-off: the partner sees what's true
  now, not what was true at order time. v1.1 may add a publication-
  scoped read.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<h3 id="as-a-developer-adding-a-tenth-read-tool">As a developer adding a tenth read tool</h3>
<ol>
<li>Decide what data your tool returns. Make sure it's something a
   trade partner is allowed to see (spec § 6 "Explicit non-tools" lists
   v1.0 exclusions — no billing, no payments, no cross-partner data).</li>
<li>Decide the scope honestly. <code>own</code> if the returned data is
   partner-wide (a list); <code>own_order</code> if it's order-specific; <code>global</code>
   if it's Southbrook-wide.</li>
<li>Write the function with <code>partner_id: int</code> (or <code>order_id: int</code>,
   etc.) as the leading arg. Type hints become the JSON Schema.</li>
<li>Add <strong>both</strong> <code>check_access_rights</code> and <code>check_access_rule</code> for the
   record-bounded reads. Delta § 14.5 fixed three tools that missed
   <code>check_access_rule</code>; new tools start with both.</li>
<li>Don't use <code>.sudo()</code> unless you're reading global config. Document
   any <code>.sudo()</code> use case in the comment.</li>
<li>Return a JSON-serializable dict (or list of dicts). Datetimes:
   call <code>.isoformat()</code>. Recordsets: convert to id lists or name
   dicts.</li>
<li>Add an OS section that documents the concept the tool exercises
   (the spec § 5.6 coverage test enforces this in CI).</li>
</ol>
<h3 id="as-an-admin-tracing-what-can-fabio-see">As an admin tracing "what can Fabio see?"</h3>
<p>For a given trade partner X:
1. Find their <code>res.partner</code> id.
2. Run a smoke test as that partner (or impersonate via a fresh JWT
   from the Odoo shell).
3. Hit <code>GET /api/hermes/tools</code> — you'll see 9 tools.
4. For each tool, invoke with a representative payload. The empty
   responses are the answer to "what can't they see."</p>
<p>This is the answer to compliance questions: "show me what data this
trade partner can access through chat" — it's exactly the 9 tools'
outputs.</p>
<h3 id="as-a-power-user-understanding-why-an-answer-is-incomplete">As a power user understanding why an answer is incomplete</h3>
<p>If Fabio gives you a thin answer to "where is my kitchen?", it's
almost always because one of the read tools returned <code>None</code> for an
expected field. Common causes:</p>
<ul>
<li>The kitchen project doesn't have a task yet (no <code>sb.kitchen.project</code>
  → <code>project.task</code> mapping). <code>get_order_status</code> returns
  <code>bottleneck=None</code>, <code>blocker=None</code>, etc.</li>
<li>The record rule on <code>mrp.production</code> denies trade-partner reads.
  <code>mos=0</code> even though the order has MOs.</li>
<li>The order's <code>commitment_date</code> isn't set. <code>install_due=None</code>.</li>
</ul>
<p>The tools are reading honestly. The answer reflects the data.</p>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"<code>list_my_orders</code> returns an empty list but the partner clearly has
orders."</strong></p>
<p>Most likely: the partner's user doesn't have a record rule granting
access to <code>sale.order</code> records where <code>partner_id == self.partner_id</code>.
Check <strong>Settings → Technical → Security → Record Rules</strong> → filter by
<code>Model = sale.order</code>. The portal user (<code>base.group_portal</code>) should
have a rule like <code>("partner_id", "=", user.partner_id.id)</code>. If it's
missing, the search returns nothing.</p>
<p><strong>"<code>get_order_status</code> returns <code>bottleneck=null</code> but I know the order
has a bottleneck."</strong></p>
<p><code>bottleneck</code> comes from <code>project.task.southbrook_current_bottleneck_wc</code>,
which is set by the kitchen-workspace addon. Two failure modes:
- No <code>project.task</code> exists for the order (no <code>sale_order_id</code> link).
- The task exists but <code>southbrook_current_bottleneck_wc</code> isn't computed
  yet (the orchestration cron hasn't run).</p>
<p>Check the actual project.task record manually.</p>
<p><strong>"<code>get_quote_pdf_url</code> returns a URL but the partner gets 404."</strong></p>
<p>The URL is <code>&lt;base_url&gt;/my/orders/&lt;id&gt;?report_type=pdf</code> — the partner
needs to be logged in to the portal. The URL works for the logged-in
partner only. If you're sharing the URL out-of-band (email, chat), the
recipient must already have a portal session.</p>
<p><strong>"<code>list_my_recommendations</code> returns <code>[]</code> but I see drafts in </strong>Fabio →
Recommendations<strong>."</strong></p>
<p>The drafts in the menu are filtered by
<code>source_model='res.partner'</code> AND <code>source_res_id=&lt;caller's partner id&gt;</code>
AND <code>state in ('draft', 'ready')</code>. If the drafts visible in the menu
have a different <code>source_model</code> (e.g. <code>sale.order</code>), or the partner
ID doesn't match, the tool won't return them. Also: applied and
rejected recommendations are NOT in the tool's filter — that's
intentional.</p>
<p><strong>"I added a <code>Optional[int]</code> arg with a default and the JSON Schema
doesn't reflect it."</strong></p>
<p><code>_extract_params_from_signature</code> only checks for <code>int</code>, <code>bool</code>,
<code>float</code>, defaults to <code>string</code>. <code>Optional[int]</code> becomes <code>"string"</code> in
the auto-extracted schema. If you want richer schema, pass
<code>parameters=</code> explicitly to the decorator.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<h3 id="why-read-tools-are-uniformly-t0">Why read tools are uniformly T0</h3>
<p>The spec § 4.2 tier matrix puts all read operations at T0. The
distinction T0 vs T1 vs T2 is about <em>write impact</em>, not read
sensitivity. Reading data is universally T0; only writes split by
tier.</p>
<h3 id="why-so-many-getattr-default-calls">Why so many <code>getattr(..., default)</code> calls</h3>
<p>Two reasons:
- <strong>Defensive against partial installs.</strong> If <code>southbrook_kitchen_workspace</code>
  or <code>southbrook_premium_orchestration</code> is installed without all its
  fields, the tools should degrade gracefully rather than crash.
- <strong>Defensive against future field renames.</strong> If a downstream addon
  renames <code>southbrook_top_blocker</code> to <code>southbrook_blocker_text</code>, the
  <code>getattr</code> fallback returns None instead of crashing. The tools then
  show "no blocker info" — degraded but functional.</p>
<p>The pattern is repeated extensively across <code>get_order_status</code> and
<code>get_kitchen_project</code> because those tools read fields from addons
that ship separately.</p>
<h3 id="why-_count_mos_for_order-is-a-separate-function">Why <code>_count_mos_for_order</code> is a separate function</h3>
<p>Trivially reusable. Could be inlined. Kept separate because the count
might evolve (e.g. "count only MOs in 'progress' state for this order")
and a separate function is easier to test and modify.</p>
<h3 id="the-shape-of-what-a-tool-returns">The shape of "what a tool returns"</h3>
<p>Every read tool returns either:
- A dict (one record) with stringy keys and JSON-serializable values.
- A list of dicts (collection).
- An empty list/empty dict (no data visible) — never None.</p>
<p>Errors are raised as <code>MissingError</code> (caught by dispatch as 500) or
<code>UserError</code> (also caught as 500). Both result in
<code>{"error": "tool_exception", "detail": "...", "tool": &lt;slug&gt;}</code> to the
sidecar, which surfaces it as a tool-error result to the LLM, which
recovers gracefully ("I can't reach that data right now.").</p>
<p>The reason for never returning None: the LLM treats None as "I don't
know" rather than "the data isn't here." Returning <code>[]</code> or
<code>{}</code> or a dict with explicit None values is clearer.</p>
<h3 id="why-get_os_section-is-the-only-tool-available-to-mfg_manager">Why <code>get_os_section</code> is the only tool available to mfg_manager</h3>
<p>Sales reps and mfg managers will get their own read tools in v1.1 and
v1.2 respectively. v1.0 only ships the trade-partner / sales-rep
overlapping eight, plus the universal OS-section reader. A mfg manager
asking "where is order S00235?" today gets nothing back from any of
the 8 tools, because none include <code>mfg_manager</code> in their
personas list. The OS section is the only useful answer they can get
through the chat panel.</p>
<h3 id="why-dispatch-catches-all-tool-exceptions">Why dispatch catches all tool exceptions</h3>
<p><code>hermes_tools_api.dispatch</code>:</p>
<pre><code class="language-python">try:
    result = fn(env_with_user, **args)
except Exception as e:
    _logger.exception(&quot;Tool %s raised&quot;, slug)
    return self._json(
        {&quot;error&quot;: &quot;tool_exception&quot;, &quot;detail&quot;: str(e), &quot;tool&quot;: slug},
        status=500)
</code></pre>
<p>Bare <code>Exception</code> catch is deliberate. The dispatch is the boundary
between the LLM (which expects a JSON result, success or error) and the
Odoo ORM (which raises a zoo of exception types). Catching <code>Exception</code>
+ logging + returning a 500 with the exception's <code>str()</code> lets the LLM
recover and the developer debug. Without the catch, an unhandled
<code>AttributeError</code> deep inside a tool would propagate to the sidecar as
HTML, breaking the JSON parsing on the sidecar side.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> A trade partner asks Fabio "where is my kitchen?" and the LLM
calls <code>get_order_status(order_id=235)</code>. The order belongs to a
<em>different</em> partner. What does the tool return?</p>
<blockquote>
<p><code>MissingError("Order not visible to this user.")</code> from
<code>check_access_rule</code>. Dispatch catches the exception and returns
500 <code>tool_exception</code> to the sidecar. The LLM sees the error result
and surrenders gracefully ("I don't have access to that order.").
The data is protected at the record-rule layer; the tool just
surfaces the denial as a clean error.</p>
</blockquote>
<p><strong>2.</strong> A sales rep tries calling <code>get_kitchen_project(project_id=42)</code>
where project 42 belongs to a partner they don't manage. What
happens?</p>
<blockquote>
<p>Depends on the sales rep's record rules. Native Odoo gives
salesmen access to records they're assigned to (<code>user_id ==
self.id</code>), but kitchen projects' record-rule scope was set by
<code>southbrook_kitchen_workspace</code>. If the sales rep can't see project
42, <code>check_access_rule</code> raises and dispatch returns
<code>tool_exception</code>. The data is protected the same way for sales rep
as for trade partner — record rules are the single source of truth.</p>
</blockquote>
<p><strong>3.</strong> You add a tool <code>get_order_invoices(order_id: int)</code> that returns
the order's invoices. A trade partner asks about an invoice. The
tool returns <code>[]</code>. Why?</p>
<blockquote>
<p>Almost certainly because the partner has no read access to
<code>account.move</code> records. Trade partner record rules typically scope
<code>account.move</code> by <code>partner_id == user.partner_id</code>, but the join from
sale.order to its invoices may fail under that rule. Test the
record rule explicitly. The empty list isn't a tool bug; it's a
deliberate ACL outcome. To verify, run the search as the partner
user in the Odoo shell.</p>
</blockquote>
<p><strong>4.</strong> A trade partner sees a recommendation in the <strong>Fabio → My
Recommendations</strong> view of the portal but <code>list_my_recommendations</code>
returns <code>[]</code>. What's the mismatch?</p>
<blockquote>
<p>The tool filters <code>state in ('draft', 'ready')</code>. The portal view
probably also includes <code>approved</code> or <code>applied</code> recommendations.
Once a draft is approved (by a reviewer in the backend), it falls
out of the tool's filter but remains in the portal view. The
mismatch is intentional — the tool surfaces "what still needs your
input" rather than "your full history."</p>
</blockquote>
<p><strong>5.</strong> A developer adds a <code>risk_flag</code> computation to
<code>get_install_schedule</code> that reads <code>order.commitment_date - today</code> and
compares to the order's MO lead times. The computation calls
<code>mrp.production.search([...])</code>. A trade partner gets a thin answer
where <code>risk_flag</code> is always "unknown." Why?</p>
<blockquote>
<p>The trade partner can't read <code>mrp.production</code> records via record
rule. The search returns empty; the computation degrades to
"unknown." Two paths: (a) add a record rule granting trade
partners read access to MOs linked to their orders (data exposure
decision), or (b) route the computation through a partner-visible
model (e.g. a denormalized <code>sale.order.x_lead_time_max</code> field
computed nightly).</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The 4 write tools and the T0/T1/T2 tier model — lesson 13.6.</li>
<li>The tool registry mechanics (decorator, dispatch, ACL layering) —
  lesson 13.4.</li>
<li>JWT mint + verify + persona resolution — lesson 13.3.</li>
<li>The recommendation queue side — lesson 13.2.</li>
<li>The chat panel + sidecar streaming — lesson 13.7.</li>
<li>Adding new record rules to widen trade-partner data access — that's
  a separate Odoo skill, not Fabio-specific.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_write_tools" model="slide.slide">
            <field name="name">The Four Write Tools and the T0 / T1 / T2 Tier Model</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">60</field>
            <field name="description">Developer working in `tools/write_tools.py`; production manager who needs to understand the human-approval boundary for trade-partner-proposed writes.</field>
            <field name="html_content"><![CDATA[<h1 id="the-four-write-tools-and-the-t0-t1-t2-tier-model">The Four Write Tools and the T0 / T1 / T2 Tier Model</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're the developer adding a fifth write tool and need to know which
tier it should be. You're the production manager who's been asked "can
Fabio actually book installs?" and you need a precise answer. You're
the auditor reading the source and checking that the safety boundary
holds.</p>
<p>The four write tools are the ones with consequences — they change
state, send email, create activities, or queue recommendations. Each
sits at a tier that defines what kind of consequence: T0 is reversible
internal logging, T1 is communication-with-side-effects, T2 is business
mutation that always flows through the human approval queue.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<p>You don't see the write tools on a screen directly. You see their
<em>output</em> in multiple places:</p>
<blockquote>
<p><strong>(Internal note)</strong> — <code>post_internal_note</code> writes via
<code>message_post(subtype_xmlid='mail.mt_note')</code>. Appears on the chatter
of any record the tool can write to, with the author as the
calling user.</p>
<p><strong>(Email)</strong> — <code>send_spec_pdf_email</code> triggers
<code>sale.email_template_edi_sale.send_mail(order.id)</code>. Appears in
<strong>Settings → Technical → Email → Mass Mailing</strong> outgoing logs.</p>
<p><strong>(Activity)</strong> — <code>schedule_followup_activity</code> creates a
<code>mail.activity</code> row. Appears in the user's <strong>Activity</strong> widget
(top-right bell icon).</p>
<p><strong>(Recommendation)</strong> — <code>propose_recommendation</code> creates a
<code>southbrook.hermes.recommendation</code> draft. Appears in
<strong>Fabio → Recommendations</strong> with default <code>Open</code> filter, awaiting
reviewer action (lesson 3.2 + lesson 13.2).</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<h3 id="the-t0-t1-t2-tier-model-from-the-spec">The T0 / T1 / T2 tier model from the spec</h3>
<p>Spec § 4.2 defines three tiers. The implementation in <code>utils/jwt_helper.py</code>
gives all three personas the full mask <code>"T0+T1+T2"</code> — but per spec § 14.1,
the safety boundary moved into individual tool guards rather than the
mask. So the practical model is:</p>
<p><strong>T0 — internal logging / record-rule-bounded writes.</strong></p>
<p>Reversible, low-stakes, no out-of-system effects. A note posted via
<code>post_internal_note</code> is visible in the chatter; deleting it requires no
external coordination. Per spec § 4.2 row: "own-order chatter,
internal notes, conversation log."</p>
<p><strong>T1 — communication, side effects you can't reach into and undo.</strong></p>
<p>An email sent to a partner; an activity created on a user. These
trigger external behavior (the partner reads the email; the user
sees the activity in their queue). You can't unsend the email. Per
spec § 4.2 row: "resend spec PDF, schedule own follow-up activity"
for trade partners; "partner emails, internal calls" for sales reps.</p>
<p><strong>T2 — business mutation.</strong></p>
<p>The actual database change to business state (book an install,
revise a quote, confirm an MO). For trade partners, T2 always goes
through the human-approval queue via <code>propose_recommendation</code> (spec
§ 6 — the "universal T2 escape hatch"). For sales reps in v1.1,
some T2 paths will autonomously write under threshold; for mfg
managers in v1.2, T2 paths will go through a 2-eyes review modal.
Trade partners never write business state directly through Fabio,
period.</p>
<h3 id="the-4-write-tools">The 4 write tools</h3>
<p>In <code>addons/southbrook_hermes/tools/write_tools.py</code>, registered in
this order:</p>
<table>
<thead>
<tr>
<th>#</th>
<th>Slug</th>
<th>Tier</th>
<th>Personas</th>
<th>Scope</th>
<th>Side effect</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>post_internal_note</code></td>
<td>T0</td>
<td>trade, sales, mfg</td>
<td>own</td>
<td>Posts <code>mail.message</code> of type <code>mt_note</code></td>
</tr>
<tr>
<td>2</td>
<td><code>send_spec_pdf_email</code></td>
<td>T1</td>
<td>trade, sales</td>
<td>own_order</td>
<td>Sends <code>sale.email_template_edi_sale</code></td>
</tr>
<tr>
<td>3</td>
<td><code>schedule_followup_activity</code></td>
<td>T1</td>
<td>trade, sales, mfg</td>
<td>own</td>
<td>Creates <code>mail.activity</code> (todo)</td>
</tr>
<tr>
<td>4</td>
<td><code>propose_recommendation</code></td>
<td>T2</td>
<td>trade, sales, mfg</td>
<td>own</td>
<td>Creates draft <code>southbrook.hermes.recommendation</code></td>
</tr>
</tbody>
</table>
<h3 id="tool-1-post_internal_noterecord_model-record_id-content">Tool 1 — <code>post_internal_note(record_model, record_id, content)</code></h3>
<p>Tier: T0. Personas: all three. Scope: own.</p>
<pre><code class="language-python">def post_internal_note(env, record_model: str, record_id: int, content: str):
    allowed_models = (&quot;sale.order&quot;, &quot;project.task&quot;, &quot;southbrook.hermes.question&quot;)
    if record_model not in allowed_models:
        raise UserError(
            f&quot;Hermes can't post notes on '{record_model}'. &quot;
            f&quot;Allowed: {', '.join(allowed_models)}.&quot;)
    record = env[record_model].browse(record_id)
    record.check_access_rights(&quot;write&quot;)
    record.check_access_rule(&quot;write&quot;)
    msg = record.message_post(
        body=content, message_type=&quot;comment&quot;,
        subtype_xmlid=&quot;mail.mt_note&quot;)
    return {&quot;ok&quot;: True, &quot;message_id&quot;: msg.id}
</code></pre>
<p>Three explicit guards:
- <strong>Model allowlist.</strong> Three models are postable: <code>sale.order</code>,
  <code>project.task</code>, <code>southbrook.hermes.question</code>. Anything else raises
  <code>UserError</code>. This is the model-level allowlist — even with broader
  write access on other models, Fabio can't note them.
- <strong>Record-rule check.</strong> <code>check_access_rights("write")</code> +
  <code>check_access_rule("write")</code> — the partner must be able to write to
  this specific record. Internal notes count as writes, so the
  partner needs write access to the record.
- <strong>Internal note subtype.</strong> <code>subtype_xmlid="mail.mt_note"</code> makes the
  message an internal note (not a public message). Trade partners
  can see internal notes on records they have write access to, but
  they're not sent as emails. Posting "comment" without the note
  subtype would be a public message that could fire email
  notifications.</p>
<p>The result: trade partners can log "remind me later" notes on their
own orders. Sales reps can log notes on opportunities. Mfg managers
can log notes on tasks.</p>
<p>Common confusions:
- "Why is this a T0 tool if it writes to the database?" Because
  reversible, scoped to records the caller already controls, and
  doesn't trigger external behavior. The chatter note can be deleted
  from the record. The spec's tier model is about <em>consequence</em>, not
  <em>write</em>.
- "Why doesn't trade-partner have a model allowlist different from
  mfg manager?" Because the record-rule check already filters —
  trade partners can't write to a <code>project.task</code> they don't own. The
  model allowlist exists to prevent a wider class of mistakes (e.g.
  posting a note on a <code>res.users</code> record) regardless of persona.</p>
<h3 id="tool-2-send_spec_pdf_emailorder_id">Tool 2 — <code>send_spec_pdf_email(order_id)</code></h3>
<p>Tier: T1. Personas: trade, sales. Scope: own_order.</p>
<pre><code class="language-python">def send_spec_pdf_email(env, order_id: int):
    order = env[&quot;sale.order&quot;].browse(order_id)
    order.check_access_rights(&quot;read&quot;)
    order.check_access_rule(&quot;read&quot;)
    template = env.ref(
        &quot;sale.email_template_edi_sale&quot;, raise_if_not_found=False)
    if not template:
        raise UserError(&quot;Standard sale email template not available.&quot;)
    target_email = env.user.email or order.partner_id.email
    template.send_mail(order.id, force_send=False, email_values={
        &quot;email_to&quot;: target_email,
    })
    return {&quot;ok&quot;: True, &quot;sent_to&quot;: target_email}
</code></pre>
<p>The "resend me the spec sheet" tool. Constraints:
- <strong>Read-only check on the order</strong> — <code>check_access_rights("read")</code> +
  <code>check_access_rule("read")</code>. Sending an email doesn't require write
  access; only reading the order to build the PDF.
- <strong>Send to caller's email, not arbitrary recipient.</strong> <code>target_email
  = env.user.email or order.partner_id.email</code>. The function does NOT
  accept a recipient argument. This is deliberate: per spec § 4.2,
  T1 trade-partner communication is "resend spec PDF to <em>partner's
  own email</em>." A trade partner can't ask Fabio to send the PDF to a
  competitor's email.
- <strong><code>force_send=False</code></strong> — queues the mail through the standard
  outgoing-mail queue rather than blocking the HTTP response.</p>
<p>Common confusions:
- "Why is this T1 if it doesn't change order state?" Because of the
  out-of-system side effect (email sent). T0 is reversible
  internal-only writes; T1 is communication that can't be undone.
- "Could a malicious user spoof <code>env.user.email</code> to redirect?" No
  — <code>env.user</code> is the dispatched portal user resolved from the JWT.
  The user record's email is set at admin time, not by the chat.</p>
<h3 id="tool-3-schedule_followup_activityorder_id-summary-due_date">Tool 3 — <code>schedule_followup_activity(order_id, summary, due_date)</code></h3>
<p>Tier: T1. Personas: all three. Scope: own.</p>
<pre><code class="language-python">def schedule_followup_activity(env, order_id: int, summary: str, due_date: str):
    parsed_date = datetime.date.fromisoformat(due_date)
    if parsed_date &lt; datetime.date.today():
        raise UserError(&quot;Activity due_date must be today or in the future.&quot;)
    order = env[&quot;sale.order&quot;].browse(order_id)
    order.check_access_rights(&quot;read&quot;)
    order.check_access_rule(&quot;read&quot;)  # record-rule scope per spec § 4.4
    activity_type = env.ref(&quot;mail.mail_activity_data_todo&quot;)
    activity = env[&quot;mail.activity&quot;].create({
        &quot;res_id&quot;: order.id,
        &quot;res_model_id&quot;: env[&quot;ir.model&quot;]._get(&quot;sale.order&quot;).id,
        &quot;activity_type_id&quot;: activity_type.id,
        &quot;summary&quot;: summary,
        &quot;date_deadline&quot;: due_date,
        &quot;user_id&quot;: env.user.id,
    })
    return {&quot;ok&quot;: True, &quot;activity_id&quot;: activity.id}
</code></pre>
<p>Creates a <code>mail.activity</code> on the <em>order</em> with the caller as <code>user_id</code>
— i.e. an activity for the caller themselves, attached to the order.
Constraints:
- <strong>Date validation.</strong> <code>datetime.date.fromisoformat</code> raises
  ValueError on a malformed string (caught by dispatch as
  <code>tool_exception</code>); past dates raise <code>UserError</code>.
- <strong>Read-only on the order</strong>, not write. Creating an activity on a
  record doesn't require write to the record (Odoo's
  <code>mail.activity.mixin</code> ACL).
- <strong><code>user_id=env.user.id</code></strong> — the activity goes to the caller's
  user. The trade partner can't assign activities to other users.
  Sales reps and mfg managers also can't escalate to others through
  this tool.</p>
<p>Delta § 14.5 added <code>check_access_rule</code> here. Originally only
<code>check_access_rights</code>.</p>
<p>Common confusions:
- "Why is this T1?" Same reason as <code>send_spec_pdf_email</code> — it adds
  to someone's activity queue (visible to them, expected to be
  acted on), which is a communication side effect even though no
  email is sent.
- "Can the partner schedule an activity on someone else's record?"
  Only if they have read access to that record. Record rule denies
  → dispatch returns <code>tool_exception</code>.</p>
<h3 id="tool-4-propose_recommendationintent-payload-summary">Tool 4 — <code>propose_recommendation(intent, payload, summary, ...)</code></h3>
<p>Tier: T2. Personas: all three. Scope: own.</p>
<p><strong>This is the universal T2 escape hatch — the ONLY way trade partners
get business state to change.</strong></p>
<pre><code class="language-python">_TRADE_PARTNER_INTENTS = (
    &quot;request_revision&quot;,
    &quot;request_install_reschedule&quot;,
    &quot;request_clarification&quot;,
)

@hermes_tool(
    personas=[&quot;trade_partner&quot;, &quot;sales_rep&quot;, &quot;mfg_manager&quot;],
    tier=&quot;T2&quot;, scope=&quot;own&quot;,
    description=(
        &quot;Create a draft southbrook.hermes.recommendation for human review. &quot;
        &quot;The actual business mutation only happens when an approver clicks &quot;
        &quot;Approve. For trade-partner persona, only request_revision, &quot;
        &quot;request_install_reschedule, and request_clarification intents &quot;
        &quot;are allowed.&quot;),
)
def propose_recommendation(env, intent: str, payload: dict, summary: str,
                            partner_id: int = None, persona: str = None,
                            name: str = None):
    import json
    if persona == &quot;trade_partner&quot; and intent not in _TRADE_PARTNER_INTENTS:
        raise UserError(
            f&quot;Trade partners cannot propose '{intent}' recommendations. &quot;
            f&quot;Allowed intents: {', '.join(_TRADE_PARTNER_INTENTS)}.&quot;)
    if not partner_id:
        partner_id = env.user.partner_id.id
    Rec = env[&quot;southbrook.hermes.recommendation&quot;]
    full_payload = {&quot;intent&quot;: intent, &quot;data&quot;: payload or {}}
    rec = Rec.sudo().create({
        &quot;name&quot;: name or f&quot;Hermes/{intent}/{summary[:48]}&quot;,
        &quot;summary&quot;: summary,
        &quot;recommendation_type&quot;: &quot;task&quot;,
        &quot;payload_json&quot;: json.dumps(full_payload, default=str),
        &quot;source_model&quot;: &quot;res.partner&quot;,
        &quot;source_res_id&quot;: partner_id,
        &quot;state&quot;: &quot;draft&quot;,
    })
    return {&quot;ok&quot;: True, &quot;rec_id&quot;: rec.id, &quot;summary&quot;: summary, &quot;intent&quot;: intent}
</code></pre>
<p>The function declares <code>persona: str = None</code> and <code>partner_id: int = None</code>
— both <strong>claim-bound</strong>. Dispatch overrides them from JWT (delta § 14.2)
before the function runs. The function trusts those values absolutely.</p>
<p><strong>The trade-partner intent allow-list.</strong> Three intents only:</p>
<ul>
<li><code>request_revision</code> — "please revise my order to swap door colour
  to walnut" → reviewer files a project task to do the revision.</li>
<li><code>request_install_reschedule</code> — "can we push install to next month?"
  → reviewer files a task to coordinate with the install team.</li>
<li><code>request_clarification</code> — "what does 'soft-close' include?" → reviewer
  files a task to respond to the partner.</li>
</ul>
<p>Any other intent raises <code>UserError</code>. This is the safety boundary —
trade partners cannot propose recommendations for arbitrary intents.
The list is enforced <em>inside the tool</em>, not at the dispatch tier check.</p>
<p><strong>Why <code>sudo()</code> here.</strong> The created recommendation is owned by the
Fabio partner (<code>agent_partner_id</code> defaults to the Fabio seed partner),
not by the requesting trade partner. Reviewers in
<code>group_hermes_reviewer</code> see the queue; the requesting partner does
not. If the create ran as <code>env.user</code> (the partner's portal user), the
resulting record would be invisible to reviewers under their record
rules. The <code>.sudo()</code> is spec § 4.4 rule 2's documented exception.</p>
<p><strong>The recommendation always starts in <code>draft</code>.</strong> Reviewer must mark
ready → approve → apply. See lesson 13.2 for the full state machine.</p>
<p><strong><code>recommendation_type = 'task'</code> regardless of intent.</strong> All three
trade-partner intents map to <code>task</code> because applying needs to create
a <code>project.task</code> for someone to action. If a future intent wants to
fire a different side effect, the <code>recommendation_type</code> selection
in <code>models/hermes_recommendation.py</code> would need to expand and the
<code>action_apply</code> method would need a new branch.</p>
<p><strong>The <code>payload_json</code> shape.</strong></p>
<pre><code class="language-json">{
  &quot;intent&quot;: &quot;request_install_reschedule&quot;,
  &quot;data&quot;: {
    &quot;preferred_date&quot;: &quot;2026-08-15&quot;,
    &quot;reason&quot;: &quot;kitchen renovation delayed by drywall contractor&quot;
  }
}
</code></pre>
<p>The <code>intent</code> key tells the reviewer what the partner wanted. <code>data</code> is
arbitrary JSON the LLM filled in based on the conversation. The
reviewer reads both fields when deciding whether to approve.</p>
<p><strong><code>recommendation_type='task'</code> means <code>_create_project_task</code> runs on
apply.</strong> Which means the payload also needs <code>project_id</code> to be set
before apply. The LLM's <code>propose_recommendation</code> call doesn't set
<code>project_id</code> — the reviewer adds it to <code>payload_json</code> before clicking
Apply. This is the "human bridges" step: the partner's intent is
captured by the LLM; the project assignment is captured by the
reviewer. (Or: the apply raises <code>UserError("Task recommendations
require payload.project_id.")</code> and the reviewer fills it in. See
lesson 13.2.)</p>
<p>Common confusions:
- "Why is the tier T2 if it can't actually write business state?"
  Because <em>applying</em> the recommendation writes business state. The
  <em>creating</em> of the draft is the request side; the approval is the
  authorize side. The combined path is T2 because the eventual effect
  is a database mutation. The split makes the boundary auditable.
- "Why can sales reps and mfg managers also propose recommendations?"
  Because the queue is a useful pattern for them too — e.g. a sales
  rep using the chat panel to propose a re-quote that needs manager
  approval. v1.1/v1.2 will extend their tool set with more
  autonomous writes, but <code>propose_recommendation</code> remains a universal
  escape hatch.
- "What stops a malicious prompt from elevating the intent to
  <code>request_arbitrary_change</code>?" Two guards: (1) dispatch
  claim-binding sets <code>persona='trade_partner'</code> from the JWT,
  overriding any LLM-supplied value (delta § 14.2); (2) the function
  body's <code>if persona == 'trade_partner' and intent not in
  _TRADE_PARTNER_INTENTS</code> raise. Both must be defeated for an
  elevation to succeed — and the first is enforced by the dispatch
  controller, which the LLM cannot touch.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<h3 id="as-a-developer-adding-a-write-tool">As a developer adding a write tool</h3>
<ol>
<li>Decide the tier honestly.
   - Is the change reversible and internal-only? → T0.
   - Does it trigger out-of-system communication (email, activity,
     external API)? → T1.
   - Does it mutate business state (book, schedule, confirm,
     cancel)? → T2 — and route through <code>propose_recommendation</code>,
     don't write directly.</li>
<li>Decide the persona allowlist. For T2 tools written for
   trade-partner use, route everything through
   <code>propose_recommendation</code> rather than adding a new T2 tool.</li>
<li>For T0 tools: include both <code>check_access_rights("write")</code> and
   <code>check_access_rule("write")</code>. Use <code>mail.mt_note</code> subtype for
   chatter writes.</li>
<li>For T1 tools: think about who receives the side effect. Scope
   recipients tightly (caller's own email, caller's own user). Never
   accept arbitrary recipient args.</li>
<li>Avoid <code>.sudo()</code>. The only allowed use is the
   <code>propose_recommendation</code> carve-out where the recommendation is
   owned by Fabio, not the caller.</li>
<li>Smoke test with <code>scripts/smoke_hermes.sh</code>. If the tool changes
   state, verify rollback if the test fails mid-flight (Odoo's
   per-request transaction usually handles this).</li>
</ol>
<h3 id="as-a-production-manager-reviewing-the-trade-partner-t2-path">As a production manager reviewing the trade-partner T2 path</h3>
<p>Per spec § 6, the trade-partner T2 path is:</p>
<ol>
<li>Trade partner asks Fabio a question that implies a state change.</li>
<li>LLM emits <code>propose_recommendation(intent=..., payload=..., summary=...)</code>.</li>
<li>Tool creates a draft <code>southbrook.hermes.recommendation</code> with
   <code>source_model='res.partner'</code>, <code>source_res_id=&lt;partner.id&gt;</code>,
   <code>state='draft'</code>.</li>
<li>Chat answer to the partner: "I've queued a draft (#REC-1234)."</li>
<li>Reviewer (you) opens <strong>Fabio → Recommendations</strong>, finds the draft,
   reads, decides.</li>
<li>If approve: <code>state='approved'</code>, you fill in <code>project_id</code> in the
   payload, click Apply → <code>_create_project_task</code> creates the task,
   <code>state='applied'</code>.</li>
<li>The task is now in the project board for someone to action.</li>
</ol>
<p>Failure modes to watch for:
- <strong>Drafts stuck for hours.</strong> The sidecar errored after the draft
  was posted but before the LLM told the partner. Mark ready
  yourself, review.
- <strong>Drafts with malformed payloads.</strong> The LLM filled in
  <code>payload.data</code> with wrong types (datetime instead of date string,
  user id instead of partner id). Fix the payload, then approve.
- <strong>Drafts for the wrong partner.</strong> Shouldn't happen — dispatch's
  claim-binding sets <code>source_res_id</code> from JWT. If it does happen,
  there's an auth bug; escalate.</p>
<h3 id="as-an-auditor-checking-the-safety-boundary">As an auditor checking the safety boundary</h3>
<p>Three queries:
- Show every recommendation created by <code>propose_recommendation</code> (find
  <code>source_model = 'res.partner'</code>).
- Show every recommendation applied with <code>recommendation_type='task'</code>
  (state=applied AND created_task_id IS NOT NULL).
- Show every internal note posted by Fabio (search <code>mail.message</code>
  where <code>author_id</code> resolves through the chat panel — typically via
  the calling partner's user).</p>
<p>The combination tells you "what business state did Fabio touch this
quarter?" — recommendation applications are the only T2 writes;
internal notes are T0; emails are T1 logged in mail queue.</p>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"I want to add a T2 tool that writes directly without going through
recommendations."</strong></p>
<p>For trade partners: NO. The recommendation queue is the only T2 path
in v1.0. The spec is unambiguous: "No write actions for trade
partners that don't pass through a recommendation" (§ 11
non-goals).</p>
<p>For sales reps and mfg managers: spec § 4.2 says future tiers may
auto-apply under a threshold or require 2-eyes review. v1.1 and v1.2
will add those paths. Until then, route through
<code>propose_recommendation</code> and approve in the queue.</p>
<p><strong>"<code>post_internal_note</code> raised <code>UserError("Hermes can't post notes on
'res.partner'")</code>."</strong></p>
<p>The allowlist is <code>("sale.order", "project.task",
"southbrook.hermes.question")</code>. To post a note on a partner, file
a <code>propose_recommendation</code> with a <code>request_clarification</code> intent and
have the reviewer post the partner-level note manually. Or, if this
is genuinely a new use case, add <code>res.partner</code> to the allowlist —
but pause first and ask whether the use case is really "log on
partner" or "queue for reviewer to log."</p>
<p><strong>"<code>send_spec_pdf_email</code> sent the PDF to my email, not the
customer's."</strong></p>
<p>Working as designed. The recipient is <code>env.user.email or
order.partner_id.email</code> — the caller's email takes precedence. Trade
partners using Fabio get the PDF themselves; they re-share manually
if the customer wants a copy. This is the spec § 4.2 boundary:
T1 trade-partner communication is "to partner's own email," not
arbitrary recipients.</p>
<p><strong>"A <code>schedule_followup_activity</code> call returned 500 with
<code>activity_type_data_todo not found</code>."</strong></p>
<p>The standard mail activity types are seeded by the <code>mail</code> addon. If
they're missing, the <code>mail</code> addon needs reinstalling. Verify:
<strong>Settings → Technical → Activity Types</strong>. Should see "To-Do" and
"Email" at minimum.</p>
<p><strong>"A trade partner's <code>propose_recommendation</code> call returned 400
`Trade partners cannot propose 'request_install_cancel'
recommendations.'"</strong></p>
<p>Intent not in the allowlist. The fix is NOT to widen the allowlist;
it's to map "cancel install" to a <code>request_install_reschedule</code> with
a payload of "reschedule to indefinite hold." The allowlist is small
on purpose — every addition is a new risk surface that gets
audited.</p>
<p><strong>"A reviewer asked 'who authored this recommendation?' and the
answer was 'Fabio' but they want the trade partner's name."</strong></p>
<p>The <code>agent_partner_id</code> is Fabio; the <em>source</em> is the partner. Read
<code>source_model + source_res_id</code> — for trade-partner-proposed recs,
that's <code>('res.partner', &lt;partner.id&gt;)</code>. The reviewer can browse the
partner to see who actually triggered the proposal. This is the
intentional split: provenance (agent) vs subject (partner).</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<h3 id="why-the-tier-matrix-mask-is-t0t1t2-for-every-persona">Why the tier matrix mask is "T0+T1+T2" for every persona</h3>
<p>Spec § 14.1's implementation delta is the canonical explanation. The
short version: strict mask enforcement (trade partners get only
"T0+T1") would prohibit them from calling <code>propose_recommendation</code>,
which is a T2 tool they MUST be able to call. The fix was to widen the
mask for everyone, and move the trade-partner safety boundary INTO the
function: <code>_TRADE_PARTNER_INTENTS</code> is the actual gate.</p>
<p>This is a "the tool itself is the ACL" pattern. The dispatch's tier
check is still there (defense in depth), but the trade-partner safety
isn't dependent on it.</p>
<h3 id="why-the-recommendation-flow-is-always-draft-never-autonomous">Why the recommendation flow is "always draft, never autonomous"</h3>
<p>For trade partners, the answer is in the spec § 11 non-goals: "No write
actions for trade partners that don't pass through a recommendation."
The trade-partner persona is the lowest-trust persona in the system;
every business-state change must have a human authorize it.</p>
<p>For sales reps and mfg managers in v1.0, same thing — they get the
same <code>propose_recommendation</code> path. v1.1 / v1.2 will add tier-specific
auto-approve paths, but those have their own approval-after-the-fact
audit gates.</p>
<h3 id="why-payload_json-mixes-intent-llm-provided-and-data-llm-provided">Why <code>payload_json</code> mixes intent (LLM-provided) and data (LLM-provided)</h3>
<p>The split between <code>intent</code> (one of three) and <code>data</code> (arbitrary JSON)
lets the reviewer make fast filtering decisions ("show me all
<code>request_install_reschedule</code> proposals") while preserving the
unstructured details ("preferred date 2026-08-15, reason kitchen
delayed"). The reviewer reads the summary, eyeballs the data, decides
go / no-go quickly.</p>
<h3 id="why-recommendation_typetask-even-when-the-intent-is">Why <code>recommendation_type='task'</code> even when the intent is</h3>
<p><code>request_clarification</code></p>
<p>Because every approved recommendation should result in a tracked unit
of work for a Southbrook user. "Clarification request" → "task to
respond to partner" is the same pipeline as "install reschedule" →
"task to coordinate with install team." Treating all three intents as
tasks keeps the apply mechanic simple. If a future intent really has
no associated task (e.g. "FYI only"), the recommendation_type would
be <code>note</code> or <code>risk</code> and <code>_create_project_task</code> would not fire.</p>
<h3 id="why-name-is-auto-generated-as-hermesintentsummary48">Why <code>name</code> is auto-generated as <code>"Hermes/&lt;intent&gt;/&lt;summary[:48]&gt;"</code></h3>
<p>So the queue list view has scannable titles. A trade-partner-originated
draft might be:</p>
<pre><code>Hermes/request_install_reschedule/Push install to August 15 due to drywal...
</code></pre>
<p>The truncation at 48 chars keeps the list view tidy. The full summary
is in the <code>summary</code> field. The <code>name</code> is meant to be a glanceable
title, not the full text.</p>
<h3 id="why-force_sendfalse-on-send_spec_pdf_email">Why <code>force_send=False</code> on <code>send_spec_pdf_email</code></h3>
<p>Avoids blocking the HTTP response on SMTP. The mail queue runs every
60 seconds (Odoo native cron), so the email goes out within a minute.
Trade partner expectations are "I asked, I'll get an email"; a 60-second
wait is fine. If we used <code>force_send=True</code>, an SMTP timeout would
stall the chat panel.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> A trade partner asks Fabio "can you mark my order as urgent in
the planner?" The LLM tries to call a tool that writes
<code>order.priority='urgent'</code> directly. What stops this?</p>
<blockquote>
<p>Nothing exists to call. The 4 write tools don't include a "set
order priority" tool. The LLM may try <code>propose_recommendation</code>
with <code>intent='request_urgent_priority'</code>, which raises <code>UserError</code>
("Trade partners cannot propose 'request_urgent_priority'.").
The LLM recovers by surrendering ("I can't mark orders urgent; ask
your salesperson to flag it.") The closed allowlist of trade-
partner intents is the safety boundary.</p>
</blockquote>
<p><strong>2.</strong> A sales rep uses <code>propose_recommendation</code> with
<code>intent='reprice_order'</code>. Does the tool reject this?</p>
<blockquote>
<p>No — the <code>_TRADE_PARTNER_INTENTS</code> allow-list guard is gated by
<code>if persona == 'trade_partner'</code>. For sales rep or mfg manager
persona, any intent is accepted at the tool layer. The downstream
approval gate still applies (a reviewer must approve), and v1.1
may add a sales-rep auto-approve under threshold. The tool's
safety boundary is persona-specific.</p>
</blockquote>
<p><strong>3.</strong> <code>propose_recommendation</code> is decorated <code>tier="T2"</code>, but the JWT
tier mask is <code>"T0+T1+T2"</code> for trade partners. Why not strict mask
"T0+T1" with a special exemption?</p>
<blockquote>
<p>Because the strict mask would block every trade-partner call to
<code>propose_recommendation</code> at the dispatch's tier check, before
the function ever runs and could enforce its intent allow-list.
Spec § 14.1 documents the decision: "the trade-partner safety
boundary moves from the tier mask (always going to leak the
moment one T2 tool needed to be addressable by trade partners) to
the tool's own intent allow-list." The mask is permissive; the
tool function is restrictive.</p>
</blockquote>
<p><strong>4.</strong> You approve a trade-partner <code>propose_recommendation</code> draft.
Apply raises <code>UserError("Task recommendations require
payload.project_id.")</code>. What does this mean and how do you fix it?</p>
<blockquote>
<p>The <code>propose_recommendation</code> tool doesn't set <code>project_id</code> in the
payload — it only sets <code>intent</code> and <code>data</code>. Apply tries to create
a <code>project.task</code>, which needs <code>project_id</code>. The reviewer's job is
to add the project: edit <code>payload_json</code> to inject <code>"project_id":
&lt;id&gt;</code>, save (the JSON-shape validator passes; the field isn't
validated until apply), then click Apply again. The split is
intentional: the LLM captures partner intent; the reviewer
captures project assignment.</p>
</blockquote>
<p><strong>5.</strong> A junior dev wants to add an <code>email_partner</code> write tool that
sends arbitrary text to the calling trade partner's email. What
tier should it be, and what guards must it have?</p>
<blockquote>
<p>Tier T1 (communication side effect). Guards:
(a) personas restricted (probably trade_partner, sales_rep only —
mfg managers shouldn't be emailing trade partners through chat);
(b) <code>check_access_rights("read")</code> + <code>check_access_rule("read")</code> on
any record-context arg (e.g. an order_id);
(c) recipient is always <code>env.user.email</code>, NEVER an arbitrary
recipient arg — mirrors <code>send_spec_pdf_email</code>'s pattern;
(d) body is the text param, no template substitution that could
exfiltrate other data;
(e) <code>force_send=False</code> so SMTP queues asynchronously.
Document the design and code-review heavily — anything that sends
email is a security-sensitive surface.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The recommendation queue's state machine and audit fields — that's
  lesson 13.2 (<code>13_recommendation_queue_deep.md</code>).</li>
<li>The 9 read tools — lesson 13.5 (<code>13_read_tools.md</code>).</li>
<li>The tool registry mechanics — lesson 13.4 (<code>13_tool_registry.md</code>).</li>
<li>JWT + persona resolution — lesson 13.3 (<code>13_jwt_auth_personas.md</code>).</li>
<li>The chat panel + sidecar (consumers of these tools) — lesson 13.7.</li>
<li>The reviewer's daily flow (approve / reject / apply) — lesson 3.2
  (<code>03_hermes_fabio_approval.md</code>).</li>
<li>Sales-rep and mfg-manager tool sets (v1.1 + v1.2 — not shipped in
  v1.0) — see spec § 8 phasing.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
        <record id="slide_13_chat_panel_sidecar" model="slide.slide">
            <field name="name">The OWL Chat Panel and the Vercel Sidecar</field>
            <field name="channel_id" ref="course_fabio_module"/>
            <field name="slide_category">article</field>
            <field name="sequence">70</field>
            <field name="description">Estimator / trade-partner-facing dealer (the chat panel user), IT admin / sysadmin (the sidecar operator), developer (the panel + proxy + sidecar code).</field>
            <field name="html_content"><![CDATA[<h1 id="the-owl-chat-panel-and-the-vercel-sidecar">The OWL Chat Panel and the Vercel Sidecar</h1>
<h2 id="who-this-lesson-is-for">Who this lesson is for</h2>
<p>You're a dealer or estimator about to use the chat panel for the first
time and want to understand what's happening when you type a question.
You're an admin flipping <code>sidecar_enabled</code> to <code>true</code> for the first
time. You're a developer wiring a sidecar redeploy on a Vercel preview
URL.</p>
<p>The chat panel is the user-facing surface; the Vercel sidecar is the
LLM brain that powers it. The Odoo proxy controller is the bridge. This
lesson walks the round trip end-to-end.</p>
<h2 id="where-this-lives-on-the-site">Where this lives on the site</h2>
<h3 id="as-the-trade-partner-using-the-chat-panel">As the trade partner using the chat panel</h3>
<blockquote>
<p>Sign in at <strong>southbrookcabinetry.space/odoo</strong>, navigate to your portal:
<strong>My</strong> → <strong>Orders</strong> → click one of your orders → opens the <strong>Order
Builder</strong> portal page.
A chat panel appears below the order header card, labeled <strong>Ask
Hermes</strong> with a subtitle "Order #235" (or whatever your order ref is).</p>
</blockquote>
<p>The mount is injected by <code>views/order_builder_chat_inject.xml</code>:</p>
<pre><code class="language-xml">&lt;template id=&quot;hermes_chat_inject_order_builder&quot;
          inherit_id=&quot;southbrook_estimating_website.portal_order_builder&quot;
          name=&quot;Hermes Chat — Order Builder mount&quot;&gt;
  &lt;xpath expr=&quot;//div[hasclass('o_southbrook_portal_header')]&quot; position=&quot;after&quot;&gt;
    &lt;div class=&quot;o_hermes_chat_wrap mt-3 mb-3&quot;
         data-hermes-chat-mount=&quot;1&quot;
         t-att-data-order-id=&quot;order_id or ''&quot;/&gt;
  &lt;/xpath&gt;
&lt;/template&gt;
</code></pre>
<p>This adds a <code>&lt;div data-hermes-chat-mount="1"&gt;</code> to the Order Builder
portal page. The OWL bundle's <code>autoMount()</code> finds that div and mounts
a <code>HermesChat</code> component into it.</p>
<h3 id="as-the-it-admin-operating-the-chat-surface">As the IT admin operating the chat surface</h3>
<blockquote>
<p><strong>Settings → Technical → System Parameters</strong> — three keys:
<code>southbrook_hermes.jwt_secret</code>, <code>southbrook_hermes.sidecar_url</code>,
<code>southbrook_hermes.sidecar_enabled</code>. Flip <code>sidecar_enabled</code> to
<code>true</code> to wire chat to the live sidecar; leave at <code>false</code> for stub
mode.</p>
</blockquote>
<h3 id="as-the-developer-working-on-the-sidecar">As the developer working on the sidecar</h3>
<blockquote>
<p>Sidecar code lives at <code>/Users/naadmin/southbrook-v19cr/sidecar/</code>. Routes
in <code>sidecar/app/api/hermes/ask/route.ts</code> (TBD — agent didn't read
file). Vercel project linked via <code>vercel link</code> at repo root. Deploy:
<code>vercel deploy --prod</code> from <code>sidecar/</code>.</p>
</blockquote>
<h2 id="what-your-screen-shows">What your screen shows</h2>
<h3 id="the-chat-panel-ui">The chat panel UI</h3>
<p>The OWL template at <code>static/src/components/hermes_chat/hermes_chat.xml</code>
renders four sections:</p>
<ul>
<li><strong>Header.</strong> "Ask Hermes" title, with "· Order #235" subtitle when
  the panel knows the order context.</li>
<li><strong>Message scroll area.</strong> Empty state placeholder ("Ask about your
  order, the configurator, or anything in the Southbrook OS. Try:
  'where is my kitchen?' or 'what attributes can I configure?'") until
  the first message. Each subsequent message renders as
  <code>o_hermes_chat__msg o_hermes_chat__msg--{user|assistant}</code>. The
  in-progress assistant message shows a <code>▍</code> block cursor while
  streaming.</li>
<li><strong>Error line.</strong> Renders <code>state.error</code> if set (e.g. "Hermes is
  unavailable").</li>
<li><strong>Input row.</strong> A two-row textarea + an Ask button. Cmd-Enter (or
  Ctrl-Enter) submits.</li>
</ul>
<h3 id="what-clicking-ask-does">What clicking "Ask" does</h3>
<p><code>HermesChat.submit()</code> in <code>hermes_chat.esm.js</code>:</p>
<pre><code class="language-javascript">async submit() {
    const q = (this.state.inputValue || &quot;&quot;).trim();
    if (!q || this.state.sending) return;
    this.state.error = null;
    this.state.sending = true;
    this.state.messages.push({ role: &quot;user&quot;, content: q, streaming: false });
    const assistant = { role: &quot;assistant&quot;, content: &quot;&quot;, streaming: true };
    this.state.messages.push(assistant);
    this.state.inputValue = &quot;&quot;;
    this._scrollToBottom();
    try {
        const resp = await fetch(ENDPOINT, {  // ENDPOINT = &quot;/hermes/v1/ask&quot;
            method: &quot;POST&quot;,
            headers: { &quot;Content-Type&quot;: &quot;application/json&quot; },
            body: JSON.stringify({ q, order_id: this.orderId }),
        });
        ...
    }
}
</code></pre>
<p>Two things happen in parallel as the click lands:
1. The user's message is appended to <code>state.messages</code> and the input
   clears.
2. A placeholder assistant message is appended (with
   <code>streaming: true</code>), so the cursor shows immediately.</p>
<p>Then <code>fetch('/hermes/v1/ask', ...)</code>. The Odoo proxy mints a JWT (lesson
13.3), calls the sidecar (if <code>sidecar_enabled</code>), and streams the
response back. The OWL component handles two response modes:</p>
<p><strong>JSON mode.</strong> When <code>content-type</code> includes <code>application/json</code>. This
is the stub mode or an error from the sidecar. The component reads
<code>j.answer</code> (or <code>j.stub</code>-flagged stub answer) and sets it as the
assistant message content.</p>
<p><strong>Stream mode.</strong> When <code>content-type</code> is <code>text/plain</code> (or similar). The
component reads <code>resp.body.getReader()</code> and decodes chunks with a
streaming <code>TextDecoder</code>:</p>
<pre><code class="language-javascript">const reader = resp.body.getReader();
const decoder = new TextDecoder();
while (true) {
    const { value, done } = await reader.read();
    if (done) break;
    assistant.content += decoder.decode(value, { stream: true });
    this._scrollToBottom();
}
assistant.content += decoder.decode();  // flush trailing buffered bytes
assistant.streaming = false;
</code></pre>
<p>The <code>decoder.decode()</code> after the loop is the delta § 14.10 fix: flushes
the TextDecoder's internal buffer for any UTF-8 multi-byte sequence
split across the stream boundary. Without this, accented characters,
em-dashes, and currency symbols at the end of an answer get silently
dropped.</p>
<h3 id="the-three-system-parameters">The three system parameters</h3>
<table>
<thead>
<tr>
<th>Key</th>
<th>Effect when set</th>
<th>Default</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>southbrook_hermes.sidecar_enabled</code></td>
<td>When <code>true</code>, proxy calls sidecar. When <code>false</code>, returns JSON stub.</td>
<td><code>false</code></td>
</tr>
<tr>
<td><code>southbrook_hermes.sidecar_url</code></td>
<td>The base URL the proxy POSTs to (appending <code>/api/hermes/ask</code>).</td>
<td><code>https://hermes.southbrookcabinetry.space</code></td>
</tr>
<tr>
<td><code>southbrook_hermes.jwt_secret</code></td>
<td>HS256 signing key. Must match sidecar's <code>HERMES_JWT_SECRET</code> env var.</td>
<td><code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code></td>
</tr>
</tbody>
</table>
<p>The two-key gate is intentional: an admin can set the URL without
flipping enabled (preparing for deploy), or flip enabled without a
sane URL (which the proxy then catches as "URL empty" and falls back
to stub). Both must be set + truthy for the live path to actually run.</p>
<h3 id="the-proxy-controller-post-hermesv1ask">The proxy controller (<code>POST /hermes/v1/ask</code>)</h3>
<p>In <code>controllers/hermes_proxy.py</code>. End-to-end:</p>
<ol>
<li>Parse the JSON body (<code>{q, order_id}</code>).</li>
<li>Resolve persona from <code>request.env.user</code> (lesson 13.3). 403 if
   <code>AccessError</code>; 503 if <code>RuntimeError</code>.</li>
<li>Mint JWT with <code>ttl=120s</code> (the <code>_JWT_TTL_FOR_ASK</code> constant, widened
   from the helper default per delta § 14.7).</li>
<li>Read <code>sidecar_enabled</code> + <code>sidecar_url</code> system parameters.</li>
<li>If either is unset/falsy, return a JSON stub:
   <code>python
   stub_answer = (
       f"(Stub answer — sidecar not yet wired.) Question received: "
       f"'{body.get('q', '')}'.")
   return self._json({
       "stub": True, "answer": stub_answer,
       "persona": persona, "tier": tier, "jwt_iat_seen": claims["iat"],
   })</code>
   The stub still exercises the JWT mint+verify path (the
   <code>jwt_iat_seen</code> field surfaces the verified <code>iat</code> claim so a
   developer can confirm the loop ran).</li>
<li>If both set, POST to <code>&lt;sidecar_url&gt;/api/hermes/ask</code> with
   <code>Authorization: Bearer &lt;jwt&gt;</code> and a 65s timeout (one second over
   Vercel's 60s function cap, so the proxy errors out cleanly when
   the upstream hits its limit). <code>stream=True</code> so the requests library
   doesn't buffer.</li>
<li>If the upstream HTTP call raises (<code>requests.RequestException</code>),
   return 502 <code>sidecar_unreachable</code> with the exception detail.</li>
<li>If the upstream returns non-2xx, try to JSON-decode and pass
   through the upstream error envelope. Always <code>upstream.close()</code> to
   avoid socket leaks (delta § 14.10).</li>
<li>On 2xx, stream the upstream body to the browser:
   <code>python
   def generate():
       try:
           for chunk in upstream.iter_content(chunk_size=_STREAM_CHUNK):
               if chunk:
                   yield chunk
       finally:
           upstream.close()</code>
   4 KiB chunks. The mimetype is read from the upstream's
   <code>Content-Type</code> header.</li>
</ol>
<h3 id="the-vercel-sidecar">The Vercel sidecar</h3>
<p>The sidecar is a Next.js app at <code>sidecar/</code> (repo subdirectory). Its
three relevant routes:</p>
<blockquote>
<p><strong><code>POST /api/hermes/ask</code></strong> — receives the JWT + question + order_id,
runs the LLM agent loop (RAG retrieval + tool calls + streaming
response).</p>
<p><strong><code>GET /api/hermes/tools</code></strong> — (called by the sidecar internally, not
by Odoo) — proxies to Odoo's <code>/api/hermes/tools</code> with the same JWT
to populate the LLM's function list. Cached per
<code>tenant::persona::tier</code> (delta § 14.8).</p>
<p><strong><code>POST /api/hermes/conversation/log</code></strong> — (called by the sidecar
internally at end of turn) — POSTs <code>{question, answer, partner_id,
scope, project_id, order_id}</code> back to Odoo's
<code>/api/hermes/conversation/log</code>, which persists as a
<code>southbrook.hermes.question</code> record (delta § 14.6 added <code>order_id</code>
plumbing).</p>
</blockquote>
<p>The sidecar's responsibilities, in order:</p>
<ol>
<li><strong>JWT verify</strong> (<code>lib/jwt.ts</code>). Extract <code>tenant, persona,
   partner_id, tier</code> claims. Fail closed: missing <code>HERMES_JWT_SECRET</code>
   env var raises 503 <code>jwt_config</code> (delta § 14.9 fixed the bare
   try/catch that was swallowing this as 401 invalid_token).</li>
<li><strong>RAG retrieval</strong> (<code>lib/rag.ts</code>). Top-K=5 over the per-tenant
   index in <code>data/index/&lt;tenant&gt;/index.json</code>. Index is built at
   sidecar deploy time from Odoo's <code>/southbrook/os.json</code>.</li>
<li><strong>Tool registry fetch</strong> (<code>lib/tools.ts</code>). Hit Odoo
   <code>/api/hermes/tools</code> with same JWT, cache by
   <code>tenant::persona::tier</code>.</li>
<li><strong>LLM call</strong> (<code>lib/ai.ts</code>). System prompt + persona-voice +
   RAG-injection messages + tool list → OpenAI through Vercel AI
   Gateway. Default model <code>openai/gpt-4o</code>; Gemini routable for
   long-context.</li>
<li><strong>Tool calls.</strong> Each <code>tools.func_call</code> from the LLM is dispatched
   back to Odoo <code>/api/hermes/tools/&lt;slug&gt;</code> with the same JWT. Results
   feed back into the next LLM turn within the agent loop.</li>
<li><strong>Stream answer.</strong> SSE / text stream of tokens back through the
   proxy to the OWL panel.</li>
<li><strong>Conversation log.</strong> Fire-and-forget <code>POST
   /api/hermes/conversation/log</code> with the full Q+A turn.</li>
</ol>
<h3 id="the-agent-loop-budget">The agent loop budget</h3>
<p>Vercel functions have a 60-second cap. The agent loop must fit within
that: RAG → LLM → tool → LLM → tool → LLM → stream out. The JWT TTL is
120s for slack (delta § 14.7). Realistic budget:
- RAG retrieval: 200ms.
- LLM round 1 (decide tools): 1–2s.
- Tool round-trip: 200–500ms each, often 2–4 tools per turn = 1–2s.
- LLM round 2 (synthesize): 2–4s.
- Stream out: variable, until model emits stop.</p>
<p>Total: typically 5–8s for a clean answer; up to ~30s for a hard
question with many tool roundtrips. &gt;60s → Vercel kills the function;
the proxy times out at 65s and returns <code>sidecar_unreachable</code> to the
panel.</p>
<h3 id="the-stub-fallback">The stub fallback</h3>
<p>The single most important fact about the system for an admin to know:</p>
<blockquote>
<p>When <code>sidecar_enabled=false</code>, the chat panel STILL WORKS. It just
returns a stub answer. The JWT was minted, persona was resolved, the
auth path was exercised. The panel renders the stub message. There's
no error — this is the intentional "before sidecar is deployed"
mode.</p>
</blockquote>
<p>This is what gives the team confidence to deploy the addon and roll
out the panel UI to users <em>before</em> the Vercel side is wired up. Users
see a "Hermes is in setup, here's a placeholder" experience rather
than a broken UI.</p>
<h2 id="your-daily-flow">Your daily flow</h2>
<h3 id="as-a-trade-partner-using-chat">As a trade partner using chat</h3>
<ol>
<li>Open your order in the portal.</li>
<li>Type your question in the chat panel's textarea.</li>
<li>Click <strong>Ask</strong> (or hit Cmd-Enter / Ctrl-Enter).</li>
<li>The assistant message starts streaming. If you see "(Stub answer
   — sidecar not yet wired.)" — the platform is in stub mode, contact
   your salesperson; the chat isn't live yet.</li>
<li>If the LLM creates a draft recommendation, the answer will say so:
   "I've queued a draft (#REC-1234)." The recommendation is now with
   your reviewer at Southbrook.</li>
<li>To ask a follow-up: type again. Each call is a fresh JWT + fresh
   tool dispatch; the conversation context is in the message history
   that the panel sends with each request (TBD — actual sidecar
   conversation memory implementation not read).</li>
</ol>
<h3 id="as-an-admin-going-live-with-chat">As an admin going live with chat</h3>
<p>The pre-flight checklist:</p>
<ol>
<li><strong>Rotate the JWT secret.</strong> Settings → Technical → System
   Parameters → <code>southbrook_hermes.jwt_secret</code> → set to a fresh
   high-entropy value. The placeholder <code>PLACEHOLDER_ROTATE_BEFORE_PRODUCTION</code>
   is actively rejected by the helper.</li>
<li><strong>Set the sidecar URL.</strong> Settings → Technical → System Parameters
   → <code>southbrook_hermes.sidecar_url</code> → set to your Vercel project's
   production URL (e.g. <code>https://hermes-sidecar.vercel.app</code> or your
   custom domain like <code>https://hermes.southbrookcabinetry.space</code>).</li>
<li><strong>Deploy the sidecar.</strong> From the repo root:
   <code>bash
   cd sidecar/
   vercel link                              # once
   vercel env add HERMES_JWT_SECRET         # matches Odoo system parameter
   vercel env add OPENAI_API_KEY            # or AI Gateway credentials
   vercel env add TENANT_REGISTRY           # JSON of tenant → odoo URL
   pnpm build:rag southbrook                # builds rag index from /southbrook/os.json
   vercel deploy --prod</code></li>
<li><strong>Smoke test.</strong> From the QNAP or your laptop:
   <code>bash
   ./scripts/smoke_hermes.sh</code>
   Exercises every Hermes route end-to-end. Green = ready to flip the
   switch.</li>
<li><strong>Flip the switch.</strong> Settings → Technical → System Parameters →
   <code>southbrook_hermes.sidecar_enabled</code> → set to <code>true</code>.</li>
<li><strong>Verify with a real chat.</strong> Sign in as a portal user, open an
   order, type "where is my kitchen?" — answer should NOT begin with
   "(Stub answer — sidecar not yet wired.)".</li>
</ol>
<h3 id="as-a-developer-redeploying-the-sidecar">As a developer redeploying the sidecar</h3>
<p>When you add a new tool to <code>tools/</code>:
1. Restart the Odoo container so the decorator re-imports.
2. The new tool now appears in <code>GET /api/hermes/tools</code> from a fresh
   JWT.
3. The sidecar caches the registry by <code>tenant::persona::tier</code> — until
   you redeploy, the LLM doesn't see the new tool.
4. <code>cd sidecar/ &amp;&amp; vercel deploy --prod</code>. The next sidecar cold start
   re-fetches the registry and the LLM gets the updated tool list.</p>
<p>When you change a tool's behavior (not its signature):
1. Restart Odoo. The tool now behaves differently.
2. No sidecar redeploy needed — the sidecar still calls
   <code>POST /api/hermes/tools/&lt;slug&gt;</code> and gets the new behavior.</p>
<p>When you change the RAG corpus (an <code>southbrook_os</code> section was
edited):
1. The sidecar's RAG index was built at deploy time from
   <code>/southbrook/os.json</code>. It's a snapshot.
2. To pick up new OS sections, <code>pnpm build:rag southbrook &amp;&amp; vercel
   deploy --prod</code>.</p>
<h2 id="common-mistakes-how-to-recover">Common mistakes + how to recover</h2>
<p><strong>"The chat panel doesn't appear at all on the Order Builder page."</strong></p>
<p>Three causes:
- The OWL bundle didn't load. Check the browser console for asset
  errors. The bundle is registered in <code>__manifest__.py</code> under
  <code>assets.web.assets_frontend</code>. If the assets bundle didn't build,
  the JS won't run.
- The mount div wasn't injected. The QWeb template
  <code>hermes_chat_inject_order_builder</code> inherits
  <code>southbrook_estimating_website.portal_order_builder</code>; if that
  parent template's XPath changed (e.g. the <code>o_southbrook_portal_header</code>
  CSS class was renamed), the XPath fails and the mount is missing.
  Check the page source for <code>data-hermes-chat-mount="1"</code>.
- The user can't see the portal page at all. Verify the partner has
  portal access and is signed in.</p>
<p><strong>"The chat returns '(Stub answer — sidecar not yet wired.)' but
<code>sidecar_enabled</code> is <code>true</code>."</strong></p>
<p><code>sidecar_url</code> is probably empty or malformed. The proxy code:</p>
<pre><code class="language-python">if not sidecar_enabled or not sidecar_url:
    # ... return stub
</code></pre>
<p>The <code>not sidecar_url</code> catches empty string, None, or any falsy. Check
the parameter is set to a URL with scheme (<code>https://...</code>).</p>
<p><strong>"The chat returns 502 <code>sidecar_unreachable</code>."</strong></p>
<p>The Odoo proxy couldn't reach the sidecar. Diagnose:
- <code>curl -I &lt;sidecar_url&gt;</code> from the Odoo container. Should return
  200 from Vercel's root.
- Check Vercel's deployment status. A failed deploy or a paused project
  returns 5xx.
- DNS issue from the container — sometimes the QNAP DNS cache poisons
  the resolution. <code>docker exec southbrook-odoo nslookup &lt;sidecar_host&gt;</code>.</p>
<p><strong>"The chat returns 503 <code>hermes_not_configured</code>."</strong></p>
<p>The JWT secret is either still the placeholder or PyJWT isn't
installed. Lesson 13.3 covers the fix.</p>
<p><strong>"The chat shows answers but they don't reference my actual order."</strong></p>
<p>The order_id isn't reaching the sidecar. Check:
- The mount div in the Order Builder page has
  <code>t-att-data-order-id="order_id or ''"</code>. If <code>order_id</code> is undefined
  in the template scope, the attribute is empty.
- The OWL component's <code>get orderId()</code> getter validates the value;
  malformed values become <code>null</code>.
- The OWL component sends <code>{q, order_id}</code> to <code>/hermes/v1/ask</code>. If
  <code>order_id</code> is null, the proxy passes it through but the LLM has no
  order context.</p>
<p><strong>"After a long answer, the last few characters are missing."</strong></p>
<p>You're on a sidecar build pre-delta-§14.10. The TextDecoder's flush
on stream end was added to recover UTF-8 multi-byte sequences split
across the final chunk. Update the OWL component to the post-delta
version; redeploy.</p>
<p><strong>"The sidecar timed out and I got a <code>sidecar_unreachable</code> error."</strong></p>
<p>The agent loop ran past 60s. Causes:
- LLM took too long to synthesize.
- Too many tool round-trips.
- A tool itself was slow (an Odoo query that wasn't indexed).</p>
<p>Diagnose by checking Vercel function logs (look for the function
that hit its time limit). Mitigation: cap the number of tool
roundtrips in the sidecar's <code>streamText</code> config; index the slow
queries; switch to a faster LLM for that turn.</p>
<p><strong>"I changed a tool but the LLM still calls the old version."</strong></p>
<p>Restart Odoo to re-import the decorator → redeploy sidecar so the
registry refreshes. Both steps required. See the "redeploying the
sidecar" daily-flow.</p>
<h2 id="what-the-system-is-doing-behind-the-scenes">What the system is doing behind the scenes</h2>
<h3 id="why-the-proxy-lives-inside-odoo-not-as-a-direct-browser-to-sidecar-call">Why the proxy lives inside Odoo, not as a direct browser-to-sidecar call</h3>
<p>Because the JWT is minted server-side from the authenticated Odoo
session. If the browser called the sidecar directly, the browser
would need to either:
- Hold a long-lived JWT (security risk; a JWT in localStorage is
  exfiltratable by XSS).
- Mint a JWT in-browser (requires shipping the secret to the
  browser — total non-starter).</p>
<p>The Odoo proxy as middleman lets the browser stay unauthenticated to
the sidecar; the JWT is short-lived (120s) and minted per request from
the trusted Odoo session.</p>
<h3 id="why-streaming-vs-json-content-type-branch-in-the-owl-component">Why streaming vs JSON content-type branch in the OWL component</h3>
<p>The sidecar streams <code>text/plain</code> (or <code>text/event-stream</code>). The stub
returns <code>application/json</code>. Same OWL component handles both: branches
on <code>resp.headers.get("content-type")</code>. This means the component can
seamlessly handle "before sidecar deployed" (stub) and "after sidecar
deployed" (stream) without a config change.</p>
<h3 id="why-force_sendfalse-in-send_spec_pdf_email-and-streaming-in-the-chat-panel">Why <code>force_send=False</code> in <code>send_spec_pdf_email</code> AND streaming in the chat panel</h3>
<p>Both are about not blocking the user's request thread on external I/O.
Email goes to a cron-driven send queue (60s cadence); chat streams
chunks as they arrive. The user sees the panel respond within ~1
second even though the full answer takes 5–8s.</p>
<h3 id="why-the-sidecar-caches-the-tool-registry-per">Why the sidecar caches the tool registry per</h3>
<p><code>tenant::persona::tier</code></p>
<p>A cold start refetches the registry. Subsequent warm-instance calls
reuse the cached list for the same tenant/persona/tier triple. Per
delta § 14.8, the original cache key was <code>tenant + last-12-chars-of-jwt</code>
— since each request mints a fresh JWT, the suffix was random and the
cache never warmed. The fix made the key persona+tier-based (which are
stable across requests).</p>
<h3 id="why-the-conversation-log-carries-order_id">Why the conversation log carries <code>order_id</code></h3>
<p>Per delta § 14.6, the original schema had <code>project_id</code> but no
<code>sale_order_id</code>. The sidecar's order context was being dropped. The
fix added <code>sale_order_id</code> (Many2one) + <code>order_id</code> kwarg on
<code>log_conversation</code> + controller plumbing.</p>
<p>This matters for two reasons: (a) the auditor can reconstruct "the
partner was looking at order X when they asked Y"; (b) the
recommendation queue can correlate proposals to the order context.</p>
<h3 id="why-the-mimetype-is-read-from-the-upstreams-content-type-not-hardcoded">Why the mimetype is read from the upstream's Content-Type, not hardcoded</h3>
<p>The sidecar might emit <code>text/plain</code> for plain text, <code>text/event-stream</code>
for SSE, or <code>application/json</code> for errors. The proxy's
<code>Response(generate(), mimetype=mimetype, ...)</code> forwards whatever the
upstream said. The OWL component then branches on the same header.
This lets the sidecar evolve (e.g. switch to SSE) without an Odoo
change.</p>
<h3 id="why-cache-control-no-store-on-the-proxy-response">Why <code>Cache-Control: no-store</code> on the proxy response</h3>
<p>The stream contains personalized data. Caching is wrong at every
layer (browser, CDN, intermediate proxy). <code>no-store</code> explicitly
prevents any cache from storing the response. Plain <code>no-cache</code> would
allow caching with revalidation, which is still wrong here.</p>
<h3 id="why-the-requestsrequestexception-catch-is-wider-than-timeout">Why the <code>requests.RequestException</code> catch is wider than <code>Timeout</code></h3>
<p>Network issues come in many flavors: connection refused (sidecar
down), DNS failure, TLS handshake error, mid-stream connection reset.
All inherit from <code>requests.RequestException</code>. Catching the parent
gives a uniform <code>sidecar_unreachable</code> response for all of them.</p>
<h2 id="quiz-5-questions-applied">Quiz (5 questions, applied)</h2>
<p><strong>1.</strong> An admin sets <code>sidecar_url='https://hermes-sidecar.vercel.app'</code>
but forgets to flip <code>sidecar_enabled</code> to <code>true</code>. A trade partner
asks a question. What do they see?</p>
<blockquote>
<p>A stub answer prefixed with "(Stub answer — sidecar not yet
wired.)" because the proxy's <code>if not sidecar_enabled or not
sidecar_url:</code> branch fires. Both must be set + truthy for the live
path. The JWT was still minted + verified (the response body's
<code>jwt_iat_seen</code> confirms), so the auth path is healthy — but the
sidecar is never called.</p>
</blockquote>
<p><strong>2.</strong> A developer adds a new tool, restarts Odoo, and tests with
<code>GET /api/hermes/tools</code> — the new tool appears. They open the chat
panel and ask a question that should trigger the tool. The LLM
doesn't call it. What's most likely wrong?</p>
<blockquote>
<p>The sidecar's tool registry cache is warm from before the addon
restart. Until the next sidecar cold start (or a redeploy), the
LLM gets the old registry. Fix: <code>vercel deploy --prod</code>. The cache
key is <code>tenant::persona::tier</code> (delta § 14.8), and a redeploy
forces a cold start that re-fetches the registry.</p>
</blockquote>
<p><strong>3.</strong> The chat panel returns 502 <code>sidecar_unreachable</code>. From the
Odoo container, <code>curl -I &lt;sidecar_url&gt;</code> returns 200. The Vercel
dashboard shows the deployment is "Ready." What's the next
debugging step?</p>
<blockquote>
<p>Check the Vercel function logs for the <code>/api/hermes/ask</code> route.
The function might be returning 5xx after the network handshake
succeeded — e.g. <code>jwt_config</code> (HERMES_JWT_SECRET unset) or
<code>corpus_missing</code> (RAG index unbuilt). The Odoo proxy's
<code>sidecar_unreachable</code> is for <code>requests.RequestException</code> (network
level); a logical 5xx from the sidecar would be passed through as
<code>sidecar_error</code>. So the question is whether you're really seeing
<code>sidecar_unreachable</code> (network) or <code>sidecar_error</code> (5xx) — verify
by inspecting the response body of the failure.</p>
</blockquote>
<p><strong>4.</strong> The trade partner sees the assistant message start to stream
("Looking up order S00…") and then the panel just stalls. No error,
no completion. What's happening?</p>
<blockquote>
<p>The streaming connection is open but no more bytes are arriving.
Causes: (a) the sidecar's LLM call is taking too long and Vercel's
60s function timeout hasn't fired yet; (b) a tool call is hanging
on the Odoo side (e.g. an unindexed query that's running for 40s);
(c) the network path is silently stalling. Wait until the 65s
proxy timeout fires — you'll get a <code>sidecar_unreachable</code> then.
Check Vercel function logs and Odoo logs for the offending tool.</p>
</blockquote>
<p><strong>5.</strong> A developer changes the OS canonical section
<code>07_partner_faq.md</code> to add a new FAQ entry. They restart Odoo,
verify the section's <code>body</code> field is updated, and ask Fabio the new
question through the chat panel. Fabio doesn't know the new
answer. Why?</p>
<blockquote>
<p>The sidecar's RAG corpus was built at deploy time from a snapshot
of <code>/southbrook/os.json</code>. Changing the OS section in Odoo updates
the published JSON, but the sidecar's RAG index isn't rebuilt
automatically. The fix is on the sidecar side: <code>cd sidecar/ &amp;&amp;
pnpm build:rag southbrook &amp;&amp; vercel deploy --prod</code>. The next
deployment pulls the fresh corpus and indexes it. The spec § 7
calls out a webhook-driven rebuild as a future enhancement; in
v1.0 it's a manual step.</p>
</blockquote>
<hr>
<h2 id="what-this-lesson-does-not-cover">What this lesson does NOT cover</h2>
<ul>
<li>The recommendation queue and its state machine — lesson 13.2.</li>
<li>JWT + persona + tier internals — lesson 13.3.</li>
<li>The tool registry and dispatch ACL — lesson 13.4.</li>
<li>The 9 read tools and 4 write tools, with their persona allowlists
  — lessons 13.5 + 13.6.</li>
<li>The reviewer's daily flow (approving recommendations) — lesson 3.2.</li>
<li>The CS perspective on the queue — lesson 6.3.</li>
<li>Sysadmin recommendations (separate operator surface at
  hermes.odooiq.com) — lesson 7.3.</li>
<li>The sidecar's internal TypeScript (<code>lib/jwt.ts</code>, <code>lib/rag.ts</code>,
  <code>lib/tools.ts</code>, <code>lib/ai.ts</code>) at file-level depth — those are
  developer-on-the-sidecar topics outside the trainee track. The
  routes consumed by Odoo (POST <code>/api/hermes/ask</code>, GET
  <code>/api/hermes/tools</code> echo, POST <code>/api/hermes/conversation/log</code>) are
  covered above.</li>
</ul>]]></field>
            <field name="is_published" eval="True"/>
        </record>
    </data>
</odoo>
