# Changelog — `southbrook_elearning_internal`

## 19.0.1.4.0 — 2026-07-10 (code review #5)

Content-only module (28 courses / ~198 lessons). v19-clean; one security fix.

### Fixed
- **SECURITY (MEDIUM):** the Sysadmin course embedded real infrastructure
  detail (SSH hosts, internal IP `192.168.68.108`, backup share path) but
  shipped `visibility=connected` + `enroll=public` — self-enrollable by any
  signed-in user incl. portal/dealer customers. Now `visibility=members` +
  `enroll=invite` (invited sysadmins only).
- **Docs:** manifest corrected — 28 courses / ~198 lessons (was "7-course,
  29-lesson"); `visibility=connected` (was mis-stated `public`), with a note
  that `connected` includes portal logins.

### Added
- `tests/test_elearning_security.py` — asserts the Sysadmin course stays
  members/invite (regression guard, since the XML is regenerated by
  `scripts/build_data_xml.py`).

### Verified clean (no change)
Full ~49K-line XML installs on v19 (correct `slide_category`/`html_content`
fields); no leaked credentials; build script is a safe, uninstalled dev tool.

### Recommendations (documented, not changed)
Redact the infra values from the build-source markdown (`docs/elearning/`);
consider members-only for the whole internal series (currently portal-visible).
