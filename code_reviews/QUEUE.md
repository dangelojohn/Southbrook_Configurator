# Southbrook v19CE — Custom Module Review Queue

**Repository:** `~/southbrook-v19cr`
**Branch:** `fix/reconcile-stale-modules-from-prod` (1 ahead of `main`, 0 behind — reconciled to deployed prod 2026-07-09)
**Total custom modules:** 46 · ~300K LOC (py/xml/js)
**Generated:** 2026-07-10

## Processing rules
- One module at a time. No module #N+1 until #N is fully repaired, validated, reported, and **user-approved**.
- Repairs applied directly. Validation: static (py_compile, XML/CSV well-formedness, manifest sanity, Odoo lint) always; install/upgrade tests run against the **local OrbStack dev clone only — never prod docker** (standing rule: no casual restart / `-u` thrash on `southbrook-odoo`).
- Reports per module in `code_reviews/<module>/` → `REVIEW_REPORT.md`, `CHANGELOG.md`, `TEST_RESULTS.md`.

## External dependencies (not in this repo — live in `~/product_graph_v19`)
`product_graph_release`, `product_graph_rfq`, `product_graph_vendor` — referenced by `southbrook_mrp_kitchen_workcenters` and `southbrook_plm_productgraph`. Flagged at audit time; not in scope of this repo's review.

## Queue (dependency order — foundations first)

### Tier 0 — leaves (only standard-Odoo deps)
1. ☑ **southbrook_os_kernel** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed, repaired, 53 tests green, reported (awaiting user sign-off)
2. ☑ **southbrook_qr_kit** — v19.0.0.14.0 → **19.0.0.15.0** — ✅ reviewed; **C1 prod-breaking dispatch bug + 3 CRITICAL security vulns fixed**; 60/60 tests green (from 15 failing); reported (awaiting user sign-off)
3. ☑ **southbrook_project** — v19.0.0.1.0 → **19.0.0.2.0** — ✅ reviewed; low-defect; N+1 fixed, docs corrected, +7 tests (from 0); 7/7 green. **Owner decision pending:** activate the dormant top-level-count kanban view (`active=False`). Reported (awaiting sign-off)
4. ☑ **southbrook_installer** — v19.0.3.0.0 → **19.0.4.0.0** — ✅ reviewed; well-built; **fixed CRITICAL dead-bus (v19 `_sendmany` removed) + added 14 record rules (crew PII scoping)** + perf (index/store/batch); 28/28 tests green (from 25). **Owner decision:** confirm crew-visibility model before deploying record rules. Reported (awaiting sign-off)
5. ☑ **southbrook_elearning_internal** — v19.0.1.3.0 → **19.0.1.4.0** — ✅ reviewed; pure data module, v19-clean; **fixed MEDIUM infra-recon exposure** (sysadmin course SSH/IP/backup visible to portal users → members+invite) + stale docs; +1 regression test. Reported
6. ☑ **product_configurator** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed; excellent 18→19 port; fixed JS-widget bug + Mako RCE gating + 6 indexes + 2 @api.depends; 0 regressions (demo-dependent tests need demo env); OCA-engine perf documented for upstream. Reported

### Tier 1 — configurator stack
7. ☑ **product_configurator_mrp** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed; clean v19 port; fixed HIGH BoM data-corruption (values2) + HIGH ACL/UI mismatch (Configure feature broken) + dead qweb key; install clean, no regressions. Reported
8. ☑ **product_configurator_sale** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed; clean v19/security/perf; fixed MEDIUM data-integrity gap (config_session_id → readonly); @api.depends override verified safe (MRO union). Reported
9. ☑ **website_product_configurator** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed; excellent v19 port; **fixed 2 CRITICALs: anonymous-RPC session takeover/mass-delete (added 3 ir.rules) + saved-config GC data-loss** + safe_eval DoS. Deeper route-level anonymous leak + base-GC gap documented. Reported
10. ☑ **southbrook_hermes_bom** — v19.0.2.3.0 → **19.0.2.4.0** — ✅ reviewed; well-built (v19-clean, excellent TOCTOU/advisory-lock); **fixed CRITICAL SSRF (AI-supplied source_urls) + HIGH response-validation robustness** + indexes; 27/27 tests green (+SSRF test). Async dispatch/throttling documented. Reported

### Tier 2 — estimating hub
11. ☑ **southbrook_estimating** — v19.0.7.9.0 → **19.0.7.10.0** — ✅ reviewed (HUB, ~24K LOC, 2 parallel audits); **fixed CRITICAL refacing-channel no-op (pricing routine #2 never wired) + HIGH broken Configure-Product ACL + HIGH unstyled report PDFs (wkhtmltopdf var()) + MEDIUM SSRF (asset importer) + dup-key labels + batched 3 create-loops + website-guard**; install clean, 276/284 green, 8 remaining = cross-module/harness artifacts (mrp_pm/kitchen_3d/OCA-dev-copy), 0 regressions. **Owner decisions:** tenant-isolation record rules (D1) + channel-write lockdown (D2). Reported
12. ☑ **southbrook_os** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (governed knowledge layer, 2 parallel audits; v19-CLEAN per compat audit); **fixed CRITICAL public-endpoint disclosure (leaked systems-topology/tiers/production/PLM + live data to anonymous; now audience-filtered) + HIGH unauthenticated write/TOCTOU (publish() moved to cron+post_init, route read-only) + MEDIUM latent-XSS (body_html sanitize) + MEDIUM loader idempotency + LOW utcnow/index**; install+upgrade clean, **26/26 green (from 2-fail/7-err baseline)**, +2 security regression tests, 0 regressions. **Owner/design:** live-vs-snapshot governance (D1), OSRO segregation-of-duties (D2), publication retention (D3). Reported

### Tier 3 — depend on estimating
13. ☑ **southbrook_kitchen_workspace** — v19.0.0.5.0 → **19.0.0.6.0** — ✅ reviewed (2 parallel audits; v19-CLEAN, no controllers/external-AI/eval → no CRITICAL); **fixed HIGH approval state-laundering (write-guard + readonly) + MEDIUM confirmed_by_human audit-stamp bypass (readonly ×3) + MEDIUM audit-record deletion (ACL unlink→manager) + MEDIUM broken operator-email CTA (dead action xmlid) + LOW deloop/indexes/mail-context/sudo**; install+upgrade clean, **23/23 green** (+approval-guard regression test), 0 regressions. **Owner policy:** approval group-gating/approver_type (D1), per-designer isolation (D2). Reported
14. ☑ **southbrook_hardware_catalog** — v19.0.2.1.0 → **19.0.2.2.0** — ✅ reviewed (2 parallel audits; v19-CLEAN + no controllers/sudo/eval/SSRF, ACL complete → no CRITICAL); **fixed HIGH resolve() hot-path N+1 (batched) + MEDIUM wizard rejected valid end_panel/filler/corner_mech (stale category set) + MEDIUM phantom savepoint (real per-row isolation + IntegrityError) + MEDIUM double-search/row-cap + LOW indexes/stock-dep**; install+upgrade clean, **45/45 green** (+2 regression tests, fixed brittle brand-count test), 0 regressions. **Documented:** pricing groups=/SKU-unique-constraint (D1/D2). Reported
15. ☑ **southbrook_plm** — v19.0.1.3.0 → **19.0.2.0.0** — ✅ reviewed (2 parallel audits; v19-CLEAN + best-governed module yet: group-scoped ACL, approver-gated actions); **fixed HIGH ECO terminal-state forge (write-guard hole) + HIGH ungated reset wipes approval provenance + HIGH _apply_bom TOCTOU (two active BoMs) + MEDIUM archived-BoM apply + MEDIUM ungated reject + MEDIUM confirm-snapshot N+1**; install+upgrade clean, **21/21 green** (+3 regression tests), 0 regressions. **Owner/design:** cut-spec tolerance bounds (D1), activation-race lock (D3), stage-seed noupdate (D2), bom_id domain (D5). Reported
16. ☑ **southbrook_kitchen_3d_configurator** — v19.0.5.6.15 → **19.0.5.7.0** — ✅ reviewed (2 parallel audits + live validation; ~8K LOC, JS/OWL v19-CLEAN, no auth=public routes); **fixed CRITICAL v19 install-blocker (groups.xml obj() NameError — caught by validation, missed by both agents) + HIGH save_design money-vector (client price → quote; now server-repriced) + HIGH cross-tier force_production_release crash (guarded) + MEDIUM IDOR/check_access + MEDIUM N+1 + MEDIUM index + type=json→jsonrpc**; **module now installs on v19 CE (didn't before)**, install+upgrade clean, **28/28 green** (+money-vector regression test), 0 regressions. **Owner policy:** manager-sweep isolation (D1). Reported
17. ☑ **southbrook_configurator_ux** — v19.0.1.7.0 → **19.0.1.8.0** — ✅ reviewed (2 parallel audits + HEAD baseline; public shop configurator, ~6K LOC; pricing server-side + no XSS + JS v19-clean); **fixed HIGH anon-session sharing/IDOR (HTTP-session binding) + type=json→jsonrpc**; install clean, **0 regressions** (+1 passing IDOR regression test). ⚠ **15 PRE-EXISTING test failures** (confirmed on HEAD) — test_select_commit SKU-XXX + incomplete_configuration (seed adds required attrs the fixtures predate); needs follow-up fixture pass. **Documented:** value-id filter reverted-D1 (broke legit picks), import CSRF-D3, /select DoS-D2. Reported
18. ☑ **southbrook_estimating_website** — v19.0.31.3.0 → **19.0.31.4.0** — ✅ reviewed (2 parallel audits + HEAD baseline; LARGEST module ~22K LOC, 3 controllers; only 2 safe auth=public routes, pricing server-side + IDOR closed + no XSS, JS v19-clean); **fixed HIGH send_to_manufacturing portal priv-esc (staff-only + approval gate) + MEDIUM set-customer PII backfill (staff-only) + type=json→jsonrpc ×34 + auth_passkey dep**; install+upgrade clean, **0 regressions** (+1 passing priv-esc regression test; 2 pre-existing cross-module failures confirmed on HEAD). **Documented:** variant-rule bypass HIGH-2 (caught at confirm), docs-DoS/N+1/CSRF-GET. Reported

### Tier 4
19. ☑ **southbrook_freecad_bridge** — v19.0.0.4.0 → **19.0.0.5.0** — ✅ reviewed (2 parallel audits; render-service callback; no shipped secret/no SSRF/restricted write, v19-clean install); **fixed HIGH timing-attack secret compare (hmac.compare_digest) + HIGH-partial (6,0)→(4,id) no-wipe + MEDIUM state-guard (forged-regression 409) + MEDIUM input-validation 400/cap + LOW mt_note/401-JSON**; install clean, all callback tests pass (+2 regression tests). 2 pre-existing failures = shared southbrook_dims parity (TOEKICK_FAMILIES drift + dims_js harness), unrelated. **Documented:** full attachment-ownership filter (needs bridge upload flow), async render POST. Reported
20. ☑ **southbrook_config_engine** — v19.0.0.1.0 → **19.0.0.2.0** — ✅ reviewed (2 parallel audits; placement rules engine, no controllers/JS; v19-CLEAN, no eval/injection, GAP-02 gate enforced server-side, zero sudo, ACL complete, no N+1); **fixed CRITICAL exponential packer DoS (292M calls→memoised DP, identical output) + MEDIUM rule-payload semantic validation (infinite-recursion/500→ValidationError) + LOW blank=all rule scoping**; install+upgrade clean, **18/18 green** (suite 0.11s; +4 regression tests), 0 regressions. **Documented:** rule-write on Sales-not-engineering group (S3), flag-vs-hash AI gate (S2). Reported
21. ☑ **southbrook_ai_design** — v19.0.0.2.0 → **19.0.0.3.0** — ✅ reviewed (2 parallel audits; Gemini room-analysis client; no SSRF/no-shipped-secret/no-eval/GAP-02-gate-held, v19-clean); **fixed HIGH API-key-leak-into-logs (key→x-goog-api-key header + HTTPStatusError→UserError) + MEDIUM Retry-After DoS cap + MEDIUM consume_gemini_analysis validator-bypass + LOW get_by_code UserError**; install+upgrade clean, **19/19 green** (+1 regression test), 0 regressions. **Documented:** paid-API rate-limit/cost (M1), async queue-offload (M2), dimension bounds/image-cap/indexed-dedup. Reported
22. ☑ **southbrook_kitchen_mrp** — v19.0.1.5.1 → **19.0.1.6.0** — ✅ reviewed (2 parallel audits + HEAD baseline; well-defended: nesting-IO dict-only/no-SSRF, BoM-cron private/idempotent, QR HMAC-signed+ACL-gated, no eval/SQL/XSS, ACL complete, resolve() batched); **fixed HIGH cabinet-label PDF crash-on-every-print (hasattr→'in o._fields') + MEDIUM missing ir.sequence (all records "New") + MEDIUM cross-module record_scan AttributeError guard + LOW silent scrap-except**; install+upgrade clean, HIGH label ERROR→PASS, **0 regressions** (1 pre-existing southbrook_dims empty-cutlist failure, confirmed on HEAD). **Documented:** cross-module floor_traveler sudo WO-finish, multi-company, RPC-hardening. Reported
23. ☑ **southbrook_plm_productgraph** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed (combined audit; ECO→ProductGraph bridge, ext-dep validated against 14-addon PG suite; CLEAN: no sudo/eval/HTTP/SSRF, no priv-esc — release ACL-gated at PG layer, graceful degradation); **fixed savepoint isolation (DB-level PG failure no longer rolls back the authoritative ECO apply) + pre-existing SoD test-fixture bug (submitter≠releaser → setUpClass errored, 0 tests ran)**; install clean, **4/4 green**. **Documented:** MEDIUM api_key_duration=1825 on base.group_user (5-yr keys all users — deliberate D6; recommend dedicated service group). Reported
24. ☑ **southbrook_agent_gateway** — v19.0.1.0.0 → **19.0.1.1.0** — ✅ reviewed (2 parallel audits + live validation; public AI-agent gateway; input handling EXCELLENT: strong tokens/hmac-compare, email_normalize, no mass-assign, server-side pricing, IDOR-closed, no XSS); **fixed HIGH pre-existing bug — /quote-request 500'd for EVERY new customer (_find_or_create_partner returned bare recordset; caught by validation, both agents missed it) + CRITICAL anti-abuse mitigation (verify-email daily-cap + kill-switch + per-recipient cooldown)**; install+upgrade clean, **35/35 green** (from 2-fail/8-err), +1 regression test. **Documented:** IP-spoof rate-limit (H1), CRM pollution/defer-to-verify (H2), API-key/CAPTCHA. Reported
25. ☑ **southbrook_room_capture** — v19.0.3.0.1 → **19.0.3.1.0** — ✅ reviewed (2 parallel audits + live validation; AI room-geometry estimator + QR-scan resolve; **unusually well-defended** — AI output NEVER persists/writes production geometry (human-gated wizard + validate_geometry only), QR IDOR closed (double-ownership), API-key sudo-only/never-logged/header-auth, no eval/XSS, HMAC constant-time, PII photos never persisted, header-only ACL CSV all AbstractModels, v19 JS correct); **fixed v19 `type=json`→`jsonrpc` (3 routes) + HIGH cost-abuse mitigation (global daily Anthropic-call cap + kill-switch, cross-worker, mirrors #24)**; install+upgrade clean, **46/46 green**, +3 regression tests. **Documented:** per-worker rate-limiter needs shared store (R1), 60 s sync call needs async offload (R2, both infra — same class as #21), auto-lead gating (R3), QR-TTL (R4), base64-before-cap (R5). Reported
26. ☑ **southbrook_room_chat** — v19.0.2.2.1 → **19.0.2.3.0** — ✅ reviewed (2 parallel audits + independent verify + live validation; Claude tool-calling conversational room-builder; draft-only mutation NEVER writes prod geometry (human-gated wizard + validate_geometry), tool allowlist + bounds-checked, no XSS, key sudo-only/never-logged, race-safe session, v19-clean incl. no sibling-500 bug); **fixed HIGH H2 IDOR/gate-bypass — public `handle_turn` was call_kw-dispatchable (sudo inside) → any portal user could drive it against ANY order bypassing ownership+rate-limit; renamed `_handle_turn` (non-RPC) + HIGH H1 cost-abuse (global daily cap+kill-switch, per-real-call to bound the 12× tool-loop fan-out; module reused #25's key but dropped its cap) + v19 type=json→jsonrpc**; install+upgrade clean, **47/47 green**, +5 regression tests. **Documented:** M1 lead/order pin to guessed existing partner (identity-trust policy), M2 per-user lead cap, M3 async offload (infra), M4 PII record-rule scoping, R1 shared-store limiter. Reported

### Tier 5
27. ☑ **southbrook_mrp_pm** — v19.0.1.12.4 → **19.0.1.13.0** — ✅ reviewed (2 parallel audits + independent verify + HEAD baseline + live validation; production-mgmt/shop-floor 6.8kLOC; v19-CLEAN per data-audit — no install/web/correctness defects, already trap-hardened); **fixed HIGH F1 approval-gate state-laundering (no write-guard → any salesman could RPC-write production_approval_state=approved / self-grant force_production_release, defeating SoD; added value-aware write() guard + F1b approver-group check in action_approve_production) + HIGH F2 Order Builder send_to_production exposed sudo MO-create to portal SHARE users (parent's send_to_manufacturing rejected share+checked approval; this override did neither; +catch AccessError) + MED F4 unbounded retention-cron unlink→batched+committed (10M-row table) + bypass-audit at MO-create gate**; repaired **7 pre-existing baseline failures→0** (2 stale approval tests→MO-create-gate design, 2 w051 set→list unhashable-`in`-leaf, w056 v19 `.original_routing` drift, w057 stored-computed date_done flush + TestCursor-forbids-commit, w019 cross-module MI skip), +3 security regression tests; install+upgrade clean, **32/32 green**. **Documented:** F3 floor kiosk per-station authz (needs operator→WC mapping), F5 kiosk/shop-daily N+1 (read_group), F6 dead _check_production_approval_gate, dep send_to_manufacturing cross-module precedence artifact. Reported
28. ☑ **southbrook_api** — v19.0.1.1.0 → **19.0.2.0.0** — ✅ reviewed (2 parallel audits + independent verify + live validation; REST `/api/v1/*` G6/Flutter surface; v19-CLEAN per contract-audit — installs/upgrades clean, all cross-module refs traced to source, `type=http` no jsonrpc needed, v19-safe json body accessor; core security strong — hashed keys/secrets/header-auth/runs-as-key-user/scoped idempotency/upload caps); **fixed HIGH F1 API-key forgery→privesc (group_user had create+write on southbrook.api.key + NO record rule → any employee forges key_hash→admin user_id & auths as admin; ACL→read-only + own-keys ir.rule + action_revoke ownership check) + MED F2/F3 idempotency stash poisoned-cursor (savepoint) + MED F4 missing GC cron (added _gc_expired+ir.cron) + MED F6 AI-analysis daily cap+kill-switch + LOW F8 empty-partner IDOR + LOW F9 health db-name leak**; +2 F1 regression tests; fixed pre-existing cutlist NESTING_SCHEMA v1→v2 drift; install+upgrade clean, **35/35 green**. **Documented:** F2/F3 deeper (body-hash+reserve-before-execute), F5 login-timing (Odoo-inherent), F6 login brute-force throttle (shared store), F7 credential sprawl. Reported
29. ☑ **southbrook_project_mrp** — v19.0.1.3.0 → **19.0.1.4.0** — ✅ reviewed (2 parallel audits + independent verify + HEAD baseline + live validation; project.task↔MRP coordination, 2411-line task model; NO security holes — state coordination sound, @api.depends registry-safe, no injection); **fixed HIGH C1 v19 field-removal (project_task read removed `mrp.workcenter.equipment_ids` → AttributeError in 16+ tests; port fixed sibling mrp_workorder but missed this → forward-path maintenance.request.equipment_id.workcenter_id) + HIGH C2 web-client-break (Bottleneck Contention menu threw: non-stored `current_bottleneck_workcenter_id` used in action domain+group_by → store=True; +domain non-searchable production_count→production_ids) + HIGH P1 perf (3 STORED readiness-state fields had search= doing full-table search([]).filtered() defeating index — v19 calls search= even when stored → removed)**; repaired **20 err+1 fail baseline→0** (equipment_ids, W029 ×3, mrp_pm-gate, MI-skip, phase1-regex); install+upgrade clean, **58/58 green**. **Documented:** P2 non-stored boolean readiness fields' filter-click query storm (store them — correctness-sensitive), P3 mission-control compute, P4 data-quality global scans, P5/P6 kanban/line-count, S1/S2 sudo. Reported
30. ☑ **southbrook_mrp_kitchen_tools** — v19.0.0.4.0 → **19.0.1.0.0** — ✅ reviewed (2 parallel audits + independent verify + live validation; tool/consumable/crib/kit control, 162-record seed; install/data v19-CLEAN, baseline 67/67; strong QR crypto — hmac.compare_digest, XSS-escaped, PIN rate-limit); **fixed HIGH V1 v19 runtime crash (button_start override dropped v19 `raise_on_invalid_state` kwarg → TypeError on every UI Start; tests missed it) + HIGH H1 QR double-checkout (no open-usage guard + asset state never set → tool checked out twice AND still counted available; guard + lifecycle_state=checked_out+holder) + HIGH H2 checkout attributed OdooBot not operator (sudo default; set checked_out_by explicitly) + HIGH H3 consumable qty/unit_cost unconstrained (negative qty silently un-wore tools via _apply_to_asset_life + deflated MO cost; @api.constrains + server-source unit_cost) + MED M2 readiness category one-level child_ids vs 3-level tree → false BLOCKED (child_of)**; +7 regression tests, removed dead name_get; install+upgrade clean, **74/74 green**. **Documented:** M1 lifecycle state-guard, M3 company_id/record rules, L1 readiness N×R, L2 New-code collision. Reported
31. ☑ **southbrook_mrp_kitchen_workcenters** — v19.0.4.48.7 → **19.0.5.0.0** — ✅ reviewed (largest module, 13.3kLOC; 2 parallel audits + independent verify + HEAD baseline + live validation; install/data/views/report/demo v19-CLEAN, no RCE/SSRF — OPC-UA stub-only); **fixed HIGH D1 v19 dropped mail.thread from mrp.workorder → +mail.thread inherit (~12 message_post errors) + HIGH C1 subcontract wizard selected_vendor_id required=True made open-empty create() always fail (8 errors; confirm already validates) → removed + HIGH M2 DefectQrKind IDOR/privesc (stateless kind, no check_access, sudo-create NCR w/ attacker WO id, portal-reachable via never-expiring signed payload → reject share + validate workorder_id) + MED M3 report-problem wizard wrote invalid downtime-reason Selection keys → ValueError aborted atomic submit (4/6 reasons) → key mapping + MED C2 form-open-log unbound-request crash**; repaired **35 baseline failures→4** (mail.thread, subcontract, lot_producing_ids, _onchange_move_raw, field.related-string, test_enable-context, test-user-group), **zero regressions**; install clean, 136 pass. **Documented:** H1 CSRF-able mutating-GET+no-TTL QR (root=qr_kit scan_get), M4 per-transition authz, M5 OPC-UA state-write (stub), LOWs; 4 remaining pre-existing test-harness (w018 HTTP-context, w026 malformed-PNG). Reported

### Tier 6 — upper stack
32. ☑ **southbrook_hermes** (Fabio) — v19.0.4.7.2 → **19.0.5.0.0** — ✅ reviewed (2 audits + independent verify incl. live tool-path probe + live validation; LLM tool-calling AI recommendation queue w/ JWT+HTTP-proxy; auth primitives SOUND — JWT alg-pinned/no-SSRF/claim-bound-args/read-IDOR-safe/human-gated-apply); **fixed HIGH S1 draft_customer_email sudo-write IDOR (scope=own_order declared but sudo-browsed ANY order, no check_access; LLM prompt-injection-reachable → drop sudo + check_access) + HIGH V2 get_order_status unguarded wrong project.task field names (southbrook_top_blocker→top_blocker etc., 500 when kitchen job linked) + HIGH V3 _count_mos sale_order_line_id→sale_line_id (ValueError 500) + MED V5 option_ids→design_option_ids + LOW S7 JWT require exp**; baseline 2fail+4err→0, 86/86 green; PyJWT installed in container (module lazy-imports+degrades). **Documented:** S2 per-user cost cap, S3 auto_apply gate, S4 SoD, S5 os_section allowlist, S6 conv-log ownership, V1 check_access_rights deprecation (shims WORK in this build — v19 agent over-claimed removal, disproven by probe), read_group/manifest-external_dependencies. Reported
33. ☑ **southbrook_customer_portal** — v19.0.0.7.0 → **19.0.0.8.0** — ✅ reviewed (2 audits + independent verify + live validation; small 905-LOC customer /my/kitchen-projects portal; WELL-BUILT + v19-CLEAN — correct IDOR/partner_id ownership (collapse-to-404), record-rule-scoped, CSRF on every POST, no t-raw/XSS, no mass-assign, approve state-gated, Fabio customer-scope isolated); **fixed MED F1 select_option had NO server-side state gate (template only hid the button → customer w/ own CSRF token could POST /select/<other_option> AFTER approval, re-pointing selected concept, desyncing quote/MO/spec-sheet, no re-approval/audit → added state gate) + LOW F3 date_decided always None (http.fields.Datetime — odoo.http has no fields attr → fields.Datetime.now())**; baseline 9/9→10/10 green (+F1 regression test). **Documented:** F2 spec-sheet PDF relies on sale.order portal record rule (no access token), sidebar count placeholders (cosmetic), approve-create bypasses write-guard (design). Reported
34. ☑ **southbrook_dealer_portal** — v19.0.0.1.0 → **19.0.1.0.0** — ✅ reviewed (2 audits + independent verify + live validation; dealer-channel portal, KD+installation-PDF export; channel gate implemented+tested, v19-CLEAN, 458-line model has ZERO computes); **fixed CRITICAL C1 KD-export IDOR + CRITICAL C2 installation-PDF IDOR (both routes gated only the dealer CHANNEL then sudo-browsed a client-supplied pkg_id with NO ownership check — code comment admits 'Phase 1 we accept any package the dealer can name' → any dealer enumerates pkg_id & downloads EVERY customer's panel dims/substrate/hardware SKUs/hole coords; fix = _fetch_owned_package → package._belongs_to_partner tracing sale_order_line/mo.sale_line→order→commercial_partner) + HIGH H2 record rule domain_force=[] (empty=matches ALL → every portal user reads all packages; scoped to own-orders) + LOW L1 Content-Disposition filename injection**; baseline 11→13 green (+2 IDOR regression tests, the export routes were UNTESTED for object-auth). L2 selection-callable = NOT a bug (v19 agent traced static selections). **Documented:** companion cutlist/hardware portal-read ACL should also be scoped/removed. Reported
35. ☑ **southbrook_manufacturing_intelligence** — v19.0.3.6.4 → **19.0.4.0.0** — ✅ reviewed (2 audits + independent verify + live validation; MI engine + deviation-waiver governance + FAI gate + auto-remediate; v19-CLEAN per audit); **fixed HIGH V1 v19 groups_id in action_approve (crashed waiver approval) + CRITICAL C1 waiver-approval bypassable by raw write() (QC has ORM write, all gates only in action_approve → write({state:approved}) ships out-of-spec w/ forged approver; write-guard) + CRITICAL C2 mi.check raw-writable to ship_with_deviation (launders failing check to ship w/ NO waiver; write-guard) + HIGH H1 5-min cron delete-all destroyed FAI checks+manual NCRs + HIGH H2 recompute FK-crashed on waivered check (ondelete=restrict) → scoped _unlink to engine cats + non-waivered + HIGH H3 SoD void (compared NCR create_uid=OdooBot; → waiver requester) + LOW L1 fai_status not_required vs passed (active_test)**; baseline 18fail+2blocked-classes → 4 (52/56), install clean. **Documented:** M1 FAI role gate, M2 unattended auto-remediate, M3 KPI N+1, mrp.production.production_approval_state phantom-field cross-module gap, 4 pre-existing behavioral test failures (auto-fix recompute-churn ×2, cron delta-write idempotency, eco-reversion). Reported
36. ☑ **southbrook_finance_pack** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify + live validation; CE-native Canadian accounting: CCA asset register + quarterly HST return + budget-vs-actual + WIP + Finance Snapshot tiles; NO injection/sudo/SQL/eval/cron, all @api.depends resolve, models.Constraint, model_create_multi — otherwise CLEAN); **fixed HIGH-1 NBV collapse (accumulated summed ALL projected schedule lines ignoring `posted` → net_book_value ≈ 0 the instant a schedule generated, materially misstated book value → sum posted-only + depends schedule_ids.posted; test that baked wrong behavior rewritten) + HIGH-2 filed HST silently mutable (store=True totals @api.depends period only → editing period on filed/paid return overwrote remitted figures + Compute unguarded pulled back-dated invoices → 3-layer lock: view readonly off-draft + action_compute state guard + _compute_totals skips non-draft snapshot) + F1 dead post_init_hook (account.account company_id→company_ids v19 M2M; broad except swallowed the raise → CoA never auto-loaded) + F2 Budget Pivot crash-on-open (non-stored actual_amount/variance as pivot measures → _read_group can't aggregate → dropped, kept stored budget_amount) + F3 uninstantiable Finance Snapshot (mi_tiles perm_create=0 both groups + no seed → menu empty/New AccessError → grant mgr create + noupdate seed) + MEDIUM-4 cross-company tile aggregation (cash/rev/WIP/budget/HST searches unscoped → scoped _compute_tiles to env.company) + CCA-round Class-29 $0.01 residual (10000/3×3=9999.99 → last year absorbs residual)**; test_budget company_id→company_ids (search+create). Baseline 1fail+2err/15 → **0/0 of 15**, -i clean (~66s) -u clean, F3 tile-probe live (cash=4190.93/rev30=76324). **Documented (tax-law/policy — needs accountant sign-off):** HIGH-3 Class-29 should be CRA 25/50/25 not equal-thirds (rate_pct=50 ignored, half-year unapplied), MEDIUM-1 AII 1.5× not date-aware (phase-out 2024-2028), MEDIUM-2 no company ir.rule, MEDIUM-3 state raw-writable (figures already immutable via HIGH-2), LOW×4 disposal recapture/cost-ceiling-NBV/variance-sign/WIP reserved-qty. Reported
37. ☑ **southbrook_payroll_ca** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. hand-traced payslip + bracket arithmetic + live validation; hand-rolled CE Canadian payroll: CRA 2026 fed+ON brackets/CPP/EI/WSIB/EHT, payslip→balanced JE, T4/ROE preview, cert-expiry cron; UNUSUALLY CLEAN — installs/upgrades clean, registry-safe, NO sudo/SQL/eval, NO PII leak (payroll_user NOT implied by base.group_user, no self-service), core math correct for uniform earner per T4127); **fixed HIGH H4 additional-tax free-text digit-concat over-withholding ("$25.00…2026"→$252,026 → re.search first numeric token + depends contract_id.additional_federal_tax_request) + HIGH PAY-1 paid-run recompute (action_compute blocked only 'posted' not 'paid' → recompute unlinked paid payslips & orphaned JE → block posted+paid) + MED M7 double-post mints 2nd orphaned move (state raw-writable back to computed → action_post now refuses if move_id set) + MED stale deduction computes (depended on contract_id M2O not sub-fields → +pay_periods_per_year/wsib_rate_pct) + MED M8 stale stored expiry_status (@api.depends(expires_at) only → cron recomputes nightly) + MED M5 cert cron dup-todo-spam + no per-row isolation (dedup vs open activity + try/except) + LOW L1 bracket cumulative constants arith-wrong (fed 58686.775→.725, ON 12445.4765→.5965 + 20957→.5965; Python + seed XML) + mi_tiles perm_create=0 both groups+no seed dead feature (grant mgr create + noupdate seed)**; baseline 19/19 → **21/21** (+2 regression: paid-run guard, additional-tax parse), -i clean ~63s -u clean, mi_tiles probe live (headcount=21). **Documented (tax-law/v1-scope/policy — accountant sign-off):** H1 no-YTD CPP/EI as-if-uniform cap (KEY follow-up; mid-year hire under-deducts ~$2074 CPP), H2/H3 T4/ROE mutable (explicitly PREVIEW/filing-out-of-scope → lock when filing built), M1 employer CPP/EI match uncomputed, M2 TD1 amounts dead, M3 ON surtax+OHP omitted, M4 CPP2 tier missing, M6 T4 box24/26 uncapped, L2 JE-left-draft+employer-costs-unbooked, L3 EHT projection, L4 ROE ordering, clerk contract-write policy. Reported
38. ☑ **southbrook_quality** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. hand-checked Cpk formula + live create_date-write probe + live validation; CE-native NCR + SPC + Cpk(SQL-view) + supplier-defect; v19-CLEAN + secure — no sudo/SQL-inj/eval, no portal, master data mgr-gated, and CORRECTLY seeds its mi_tiles singleton unlike finance/payroll); **fixed CRITICAL S1 Cpk used STDDEV_POP (population ÷n) not STDDEV_SAMP → understates σ → INFLATES Cpk → passes bad parts (1.41× overstate at n=2; true 0.94 reads 1.33) → STDDEV_SAMP ×3 (COALESCE already handles n=1 NULL→0) + HIGH N1 NCR state launderable via raw write() (transition graph+disposition gate only in action buttons; quality user has perm_write → write({state:released}) skips quarantine+disposition+closed_at → write() guard routes state thru _ensure_transition, actions bypass via ctx flag) + HIGH N2 critical-NCR use-as-is release needed no sign-off (any user ships safety-defect → action_release requires quality_manager when severity=critical) + HIGH TTC time_to_close off create_date = tiny NEGATIVE/non-determ (v19 SILENTLY IGNORES write() to create_date — probe-confirmed — + create_date DB-txn-ts vs closed_at app-clock skew → added writable opened_at SLA-start + max(0) clamp) + MED C1 OOS→NCR not idempotent (dup NCRs on re-run → skip if ncr_ids) + MED S5 cpk_window_days param DEAD (hardcoded 30 → init() reads param int-cast into view DDL) + CPK-flush test (SQL view reads spc_sample TABLE, ORM inserts unflushed → empty; test flush)**; baseline 2fail/14 → **0/16** (+2 regression: raw-write guard, critical-release gate; +2 repaired: time_to_close via opened_at, cpk via flush), -i clean ~63s -u clean. **Documented (statistical-scope/policy):** S2 "Cpk" is really Ppk (overall σ no subgroups), N3 no SoD (raiser can close), S3 MAX(usl)/MIN(lsl) tolerance-widen, S4 zero-variance→Cpk0, A1 no multi-company rule, X1 refresh-view DDL reachable by readers, M1 mi_tiles action no res_id, M2 FPY can go negative. Reported
39. ☑ **southbrook_mes_mps** — v19.0.1.0.1 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. traced OEE calc + res.groups field probe + live validation; CE-native rolling-13wk MPS + OEE(from mrp.workcenter.productivity) + workcenter capacity + live Bottleneck report + 2 daily crons; v19-structural-CLEAN + secure — no sudo/SQL/eval, crons APPEND/upsert not delete-all, master data mgr-gated, OEE factor math correct w/ perf clamp ≤1.0, 13wk/ISO-Monday correct); **fixed HIGH V1 res.groups.users REMOVED in v19→user_ids (bottleneck cron AttributeError'd every run) + HIGH V2 MPS pivot to_supply_qty non-stored as type=measure → _read_group crash on pivot-open (dropped, kept list sum col; mirrors finance F2) + HIGH O1 automated OEE ALWAYS 0 (cron hardcoded units_produced/target/rejected=0 → _compute_oee forced perf&qual=0 → every snapshot OEE 0%/'unacceptable' + poisoned mi_tiles 7d-avg → derive units from workorders the productivity rows reference, qty_produced/qty_production) + HIGH PCT percentage-widget 100× (all *_pct store 0-100 but views used widget=percentage → 150% shows '15000%' → removed widget from 5 *_pct fields; KEPT on OEE fields which correctly store 0-1) + MED B2 inline bottleneck fallback summed done/cancel WOs no state filter → inflated load vs capacity path (added blocked/ready/progress) + TILE mi_tiles no seed → MES Snapshot menu empty (seed singleton; perm_create was 1 so no F3)**; baseline 1fail/8 → **0/8** (fixed test_top_bottleneck: fixture seeded capacity.planned_hours which is COMPUTED from mrp.workorder→overwritten to 0→all load_pct 0→max returned first/low; rewrote to stage real workorders 10/30/60h). -i clean ~66s -u clean, mi_tiles probe live. **Documented (algorithmic/policy):** M1 no time-phased projected-on-hand (_compute_to_supply subtracts CURRENT on-hand in EVERY 13 weekly rows, no POH cascade → net req wrong for weeks≥2; core MPS-semantics redesign), B1 header top(load_pct) vs line _order(bottleneck_score) differ, C1 crons no per-row try/except, demand-by-date_order not delivery, mi_tiles perm_write for users, dead search_default_group_by. Reported
40. ☑ **southbrook_cmms_wms** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. stock.move/res.groups field probes + live validation; CE-native CMMS+WMS: MTBF/MTTR from maintenance.request + breakdown MO-flag + service-contract expiry cron + oversize-permit surface + landed-cost templates; v19-install-CLEAN + secure — no sudo-on-id/SQL/eval, writes mgr-gated, avoids mrp.workcenter.equipment_ids trap, stored graph measure); **fixed HIGH V1 res.groups.users REMOVED in v19 → all_user_ids (service-contract expiry cron crashed 100% of runs, escaped CI as untested) + CRITICAL C1 days_to_expiry/state FROZEN (store=True computes keyed on date_end not 'today' → contract created 90d out reports 90 forever, 'expired' unreachable, cron selects wrong rows → daily cron invalidate+force-recompute) + MED M6 expiry cron re-alerted EVERY day + no per-row isolation (added last_alert_date weekly throttle + try/except) + MED M7 MTBF cron created fresh report/equipment/day unbounded + 30d tile counted each eq ~30× (upsert per equipment+as_of_date) + MED M9 backdated MTBF over-counted downtime (open req end=today past window → clamp outage to [period_start,as_of_date]) + LOW H3-copy false 'Blocked N MOs' UI claim (action only message_posts, nothing blocked → reworded to 'Flagged') + M8 mi_tiles no seed + user perm_create=0 → Snapshot menu empty (seed singleton)**; baseline 1err/7 → **0/8** (baseline err = stock.move.name REMOVED in v19, test move dict {name:} → KeyError in picking precompute; dropped name from move dicts; +1 regression test expiry-cron-dedup). -i clean ~67s -u clean, mi_tiles probe live. **Documented (policy/cross-module/statistical):** H2+F2 oversize-permit INERT (no dep provides product_length/width/height → is_oversize_load always False) AND unenforced (no button_validate override) — paired feature decision; H3 breakdown hard-block enforcement model (advisory only; MO-mapping imperfect so blanket hard-block risky); H4 zero-failure MTBF reports period_hours not ∞; H5 MTTR 24h resolution (Date fields → same-day repair=0 downtime); L11 landed-cost account name-ilike + cost line needs product_id (test skips); L12 breakdown MO mapping manual/operation-based; F3 landed-cost company_ids; F4 unused MI/kitchen_tools deps. Reported
41. ☑ **southbrook_integrations** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. read MCP controller + the southbrook_api login route it trusts + live validation; MCP-into-Odoo read-only tool API + Homag sim + 3PL ASN + IoT label printers; MCP executor WELL-DESIGNED — NO arbitrary-method dispatch/eval/exec/SQL, write-cat blocked, fields allow-listed, token hashed+indexed-equality timing-safe, Homag no-eval, IoT print no-op so no SSRF); **fixed CRITICAL C1 portal-customer→API-key→sudo-read ALL payroll/finance/NCR (invoke() read via env[model].sudo() bypassing ACL/record-rules + endpoint accepted ANY valid api.key + southbrook_api public login issues keys to PORTAL users → customer gets key, calls list_payroll_runs, reads every wage; update_env decorative under sudo → DROP sudo (reads as invoking principal, ACL applies) + reject non-internal users at auth (_is_internal→403)) + HIGH H1 Integrations-Manager registers tool on ir.config_parameter → sudo-read database.secret/jwt_secret/api-key-hashes → admin escalation (sudo-drop bounds it + _BLOCKED_MODELS constrains: config_parameter/mail_server/res.users/api.key/ir.model.data) + HIGH F-DATA headline feature shipped EMPTY (data/mcp_tools.xml OMITTED from manifest → 4 seed tools never created → /invoke 404'd every documented tool + 3 heavy deps quality/payroll/finance dead weight → added to manifest, 4 tools now seed) + MED M1 caller-controlled extra_domain sudo domain-injection/boolean-oracle (removed extra_domain, domain_json is only scope) + MED M2 no limit cap + advisory-only rate-limit (clamp limit[1,500]+offset≥0 + controller enforces 429) + MED M3 audit log no principal (added user_id to mcp_call_log + set on every create) + TILE mi_tiles F3 (perm_create=0 BOTH groups + no seed → grant mgr create + seed singleton)**; baseline 1err/11 → **0/15** (baseline err = stock.move.name REMOVED in v19, test_asn_3pl setUpClass move dict {name:} → KeyError blocked whole class → dropped name; +2 regression: sensitive-model blocklist, principal-logging). -i clean ~66s -u clean, 4 MCP tools + tile probe live. **Documented:** L1 southbrook_api.key.verify() no user.active check (module #28, internal-gate partially mitigates), L2 CORS *, L3 IoT ZPL _resolve getattr repr leak ({env}/{pool}→repr, no exec, mgr-only). Reported
42. ☑ **southbrook_exec_dashboard** — v19.0.1.0.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. read controller + api.key schema + live validation; single-pane exec morning briefing: /exec/morning controller → OWL morning_briefing.js + mobile, 12 KPIs incl REVENUE/CASH across mrp/sale/stock/account/quality/finance; backend/OWL path correctly ACL-gated but the HTTP controller was the unguarded twin); **fixed CRITICAL C1 /exec/morning leaked ALL company financials to ANY logged-in user incl PORTAL customers (route auth=public + _authenticate returns any session.uid + payload computed under .sudo() + NO has_group check → portal dealer w/ website session curls endpoint → CFO revenue/cash; .with_user(uid).sudo() misleading, sudo=superuser regardless of uid → added exec-group+_is_internal gate BEFORE sudo compute, 403 else) + CRITICAL C2 API-key path BROKEN+unscoped (ApiKey.search([(key,=,api_key)]) but model has NO 'key' field only key_hash → runtime error + bypassed timing-safe verify; no scope/expiry/active → use verify()) + HIGH H1 NCR counts NOT company-scoped (every other KPI filters company_id; NCR search_counts didn't → exec of A sees B's critical-NCR count + FPY numerator all-companies vs company-A denom understates/zeros FPY → add company_id guarded) + HIGH F1 dashboard rendered BLANK (useService('user') REMOVED in v19 → threw in setup() → component never mounts → import {user} from @web/core/user) + MED F2/F3 Critical-NCR tile always 0 (state filter open/new/in_progress don't exist on southbrook.ncr=draft/quarantine/rework/scrap/released/cancelled → matched nothing → use non-terminal draft/quarantine/rework; OWL drill-through same + add severity=critical) + MED F4 WIP tile always $0 (field-guess tuple missed real field total_wip_value → added first) + MED M3 snapshot table grew UNBOUNDED (OWL called create({}) every open/Refresh → route thru get_or_create_today 5min-dedup, made it return int id for RPC) + LOW L3 cash double-count (2 journals sharing default_account_id → dedup account ids)**; baseline 3/3 → **4/4** (+1 negative regression: non-exec→403; adjusted existing to grant admin exec-group). -i clean ~64s -u clean. SQL parameterized (safe), no eval/exec, exec group correctly restrictive. **Documented:** M1 tz day-boundary (context_today local + Datetime.to_datetime UTC-midnight → ~5h shift misattributes near-midnight MOs; multi-window tz refactor), M2 no rate-limit + full recompute/call (store=False, get_or_create_today dedups only row not compute → cache payload JSON + throttle), M4 revenue excludes credit notes (out_invoice only, no out_refund → overstates; CFO net-vs-gross call), L1 takt finished-vs-scheduled disjoint MO sets, L2 empty-day=100% perfect, F5 missing web_icon, F6 bottleneck soft-deps mes_mps (not in depends→n/a), F7 dead action_exec_snapshot_list, F8 viewer perm_create (needed by get_or_create_today). Reported
43. ☑ **southbrook_premium_orchestration** — v19.0.4.7.0 → **19.0.4.8.0** — ✅ reviewed (2 audits + independent verify incl. read wall.py + both AI activators + sibling gate chain + live validation; 4928 LOC "close-the-loop": sale.order.action_confirm spine override + 6 crons + MO availability gate + Gemini/FreeCAD activators + public shop-floor wall feed; v19-CLEAN + 2 strong positives: NO auto-release-to-production cron [ECO cron only PROPOSES state=open] + NO delete-all-recreate/cr.commit in any cron); **fixed HIGH 3a Gemini API-key LEAK into log + base.group_user-readable field (200-path redacted but EXCEPTION path logged/stored str(exc) which embeds the ?key= URL → send key in x-goog-api-key HEADER not query + redact both paths) + HIGH 3b public sudo-MUTATORS = privesc (action_set_api_key/enable_real_calls/health_check on gemini + freecad url/enable/health/render are public RPC-reachable + sudo config internally + base.group_user has read → ANY employee overwrites Gemini key/enables live billing/repoints CAD bridge → added env.is_system() _check_admin guard to every mutator) + HIGH 1 public wall feed leaks customer PII (/southbrook/wall/<station>.json auth=public + stock.picking.sudo() → 50 outbound rows w/ customer name+SO ref+carrier+tracking#+schedule to ANY unauth caller; module docstring admits 'effectively public, open privacy question before production', Caddy allowlist not yet in place; station is dict-allowlisted so no injection → added OPTIONAL wall_token config-param gate constant-time, no-op if unset [preserves POC], lets owner lock down w/o network change) + 3c SSRF folded into 3b + MED 2a ECO cron no user_id (pin root) + MED 2b readiness cron no per-row try/except (root-run sweep aborts on 1 bad task → per-task except) + MED create-sym project.task.create didn't apply an EXPLICITLY-set job template though write() did (task created WITH template spawned no subtasks → symmetric create; phase3 idempotency test caught it)**; baseline 2fail+14err → **8fail+0err** (cleared ALL 14 gate-block ERRORs via test bypasses: this module's MO availability gate [bypass_availability_gate ctx / mo_availability_gate.enabled=0 param] + southbrook_mrp_pm approval gate [force_production_release] across phase1/phase2/p1_auto_emit/golden_path; phase3 idempotency FIXED via create-sym). -i clean ~66s -u clean, no regression. **Documented — 8 PRE-EXISTING assertion failures (masked by the gate ERRORs at baseline, NOT regressions):** 3 test-isolation seed-collision (spine→project(1), tool-asset→asset(1) resolvers pick lowest-id seeded record not fixture) + 5 cross-module behavioral drift (SKU grammar no slide-brand divergence, cutlist 6 vs ≥7 panels, WO duration 1596 vs 10min, lifecycle 10%-threshold activity 0 vs 1, lifecycle 100 vs 80) — sibling estimating/kitchen_tools/cutlist, out of single-module scope. **Also documented:** 2c ECO description_html unescaped free-text (safe iff eco.description is Html-sanitized in plm — verify), 3d no AI cost cap (sibling gemini.client), 5 ops.event.emit public+sudo feed-spoofing, 6a cut.spec.override base.group_user-writable + activator read-to-all, 4 _backlink_orphan_mos no company scope, 1b wall error text to anon. Reported
44. ☑ **southbrook_floor_traveler** — v19.0.1.2.1 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. read scan endpoint + record_scan sudo path + v19 type=json alias source + live validation; QWeb QR traveler PDF per sb.production.package + scan endpoint /southbrook/api/floor-traveler/scan that advances next mrp.workorder via existing button_finish + tool-consumption + scan-log; v19-CLEAN — installs/runs clean, QWeb report works, type=json is a WORKING deprecated ALIAS in v19 [http.py auto-converts to jsonrpc], not a break); **fixed CRITICAL C1/H1 scan-endpoint IDOR + portal reachability (auth=user admits ANY session incl PORTAL customers + legacy unsigned sb-package:<id> format is a bare client int printed cleartext on every traveler QR + sb.production.package.sudo().browse(id) + record_scan → wo.sudo().button_finish() runs advance+tool-debit as SUPERUSER w/ NO id/ownership/group/company check → portal dealer POSTs {qr_payload:sb-package:1} & finishes ANY customer's production; iterate ids to sweep every package → gate to internal mrp.group_mrp_user [_is_internal()+has_group] → forbidden else, BEFORE any work; shop-floor scanning is GROUP-authorized [any operator, any job at their station] so group gate not per-object is correct model, sudo now reachable only by authorized operators) + MED M1 scan-log attribution destroyed by sudo (qr.scan.log.sudo().create left create_uid/user_id=OdooBot on a production-mutation audit log → set user_id:env.uid) + LOW type=json→jsonrpc future-proofing**; baseline 1err/6 → **7/7** (baseline err = premium_orchestration MO availability gate in setUp; unmasked asset seed-COLLISION asset(1)≠asset(45) [resolver picks seeded melamine-blade asset in shared cat_blade_melamine] fixed by isolating test to a FRESH tool category — same class as #43; +1 HttpCase regression non-mrp→forbidden). -i clean ~70s -u clean. Injection CLEAN (int() coercion + signed-path hmac.compare_digest). **Documented (design/follow-up):** C2 record_scan sudo KEPT INTENTIONALLY (group-auth shop-floor model + legit cross-model consumption write [BUG 1 fix]; dropping breaks 'any operator scans any job' + regresses fix; gate is the boundary), C3 replay/no-idempotency (authorized-operator re-scan double-advances/debits; signed payload expired flag ignored + qr_kit hardcodes it False → needs per-package nonce/TTL+rate-limit w/ qr_kit), retire unsigned sb-package format (requires re-issuing every printed traveler PDF to signed sb://pkg/<id>?s=hmac), LOW1 scan-log readable by all internal users. Reported
45. ☑ **southbrook_training_hub** — v19.0.1.3.0 → **19.0.2.0.0** — ✅ reviewed (2 audits + independent verify incl. search_for_user gate model + Hermes dispatch env-binding + install probes; discovery layer over e-learning: OWL Help systray/panel + 2 auth=user HTTP routes + Hermes find_training T0 tool + curator-managed training.item/tag/menu_link JTBD search; WELL-BUILT + v19-CLEAN — OWL correct [no removed useService, t-esc throughout, valid systray reg], NO injection/eval/SQL/t-raw, q→ORM ilike only, limit capped, items CURATOR-WRITE-ONLY [base.group_user read-only so no low-priv content injection]); **fixed MED S1 Hermes find_training evaluated role gate as SUPERUSER (search_for_user builds role_group_ids gate from self.env.user but the tool called it .sudo() → self.env.user=superuser [in every group] → gate matched EVERY item → trade_partner persona gets internal-role-gated training; controllers correctly .with_user() but tool omitted → dropped the .sudo(); verified Hermes dispatch binds env to persona via request.env(user=user.id) so un-sudo'd call evals gate against real caller) + LOW-MED S2 fail-open default (role_group_ids empty=everyone) + portal reach (search_for_user sudo's the actual search bypassing model ACL → portal/share user pages every un-gated item incl auto-seeded internal runbooks → added _is_internal() 403 gate on both HTTP routes; trade-partner path is the Hermes tool not these routes) + LOW S3 url field no scheme validation (rendered into window.open/act_url, javascript: latent vector; curator-only write → @api.constrains http(s)/leading-slash) + MED-cosmetic V1 name_get REMOVED in v19 (training.tag.name_get silently dead → tags showed bare name → _compute_display_name @api.depends(kind,name))**; NO test suite ships (0 tests) → validated by -i clean ~65s (url constraint PASSED post_init seed, display_name compute registered no registry break) + -u clean + code review. **Documented:** V2 ir.ui.menu computed-m2m declares relation table name == the real training_menu_link model table (inert since store=False, landmine if flipped store=True → drop explicit relation args), V3 unused menu-scoped search path. Reported

### Tier 7 — apex
46. ☑ **southbrook_command_center** — v19.0.1.1.1 → **19.0.2.0.0** (APEX, depends on ~everything) — ✅ reviewed (2 audits + independent verify incl. scoring sudo path + bus channel model + adversarial call_kw reachability + live validation; apex aggregation: /command_center/bootstrap 5-panel controller + read-time scoring AbstractModel [ZERO stored fields, ZERO foreign @api.depends] + command.exception queue + bus.bus live layer; IMPRESSIVELY DEFENSIVE — v19-CLEAN [zero-foreign-@api.depends verified true by AbstractModel design, EVERY cross-module field across ~15 modules verified exists, OWL/bus API correct], every panel read runs AS THE CALLER in try/except so model ACL is the real gate + non-members degrade to empty, NO eval/SQL, margin/PO-risk NOT exposed [test-only callers, adversarially confirmed — scoring needs recordset arg unreachable via call_kw]); **fixed HIGH S1 client-trusted company_id + NO ir.rule (cid=int(company_id) verbatim, no check vs user.company_ids + command.exception had no company record rule + _flow/_recommendations/_alerts ignored company entirely → multi-company user reads another company's exception queue+flow+alerts → _validated_company_id clamps to env.user.company_ids + company domains on the 3 panels field-guarded + GLOBAL multi-company ir.rule on command.exception) + MED-HIGH S3 no route group gate (auth=user any session incl portal; factory_health/flow served to non-CC-members + factory_health_score→exec snapshot get_or_create_today WRITES from a read endpoint → _require_cc_member internal+any-of-6-CC-groups AccessError on both bootstrap+help_lookup [also closes training enumeration to portal]) + HIGH-LATENT S2 bus alerts leaked impact_summary CLEARTEXT on guessable unauthenticated company string channel sb_cc_alerts_<cid> [Odoo doesn't per-user-authorize arbitrary string channels] → dropped impact_summary from bus payload, client re-fetches by res_id via ACL-checked ORM + LOW V1 bus lifecycle events connect/disconnect/reconnect → BUS:CONNECT/etc (v19; live badge wouldn't flip + no auto-resync) + LOW V2 type=json→jsonrpc**; baseline 19/19 → **21/21** (+2 HttpCase gate regressions: non-member→error envelope, member→result). -i clean ~80s -u clean. **Documented:** bus channel re-scoping to per-user RECORD channels (Odoo authorizes those; Phase-3 websocket design), role param is presentation-hint-not-auth (all CC groups read whole model; derive server-side if role-sep needed), exception owner-SoD ir.rule (group_command_center_user has model-wide write; 5 role groups read-only mitigating), bottleneck_line_ids sub-panel always empty (field absent, guarded), 3 dormant scoring methods. Reported

## 🎉 CAMPAIGN COMPLETE — all 46 modules reviewed/repaired/validated (2026-07-12).

## Status legend
☐ queued · ◐ in progress · ☑ complete + approved
