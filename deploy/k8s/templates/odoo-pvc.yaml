apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kitchenforge.fullname" . }}-odoo-data
  labels:
    {{- include "kitchenforge.labels" . | nindent 4 }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
